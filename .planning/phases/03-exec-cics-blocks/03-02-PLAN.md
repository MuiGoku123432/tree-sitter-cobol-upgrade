---
phase: 03-exec-cics-blocks
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - grammar.js
  - src/parser.c
  - src/grammar.json
  - src/node-types.json
  - queries/cics.scm
  - test/corpus/exec_cics.txt
  - run_cics_query_capture.sh
  - forest-shim/cobol/parser.c
  - forest-shim/cobol/grammar.json
  - forest-shim/cobol/cics.scm
autonomous: true
requirements: [CICS-01, CICS-02, CICS-05, CICS-06]

estimate:
  tokens: 100000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All four measured argument forms parse inside an option: quoted literal, plain data name, LENGTH OF <name>, and numeric literal (D-29)."
    - "TRANSID, PROGRAM, MAP and MAPSET operands emit cics_transaction_name, cics_program_name, cics_map_name and cics_mapset_name respectively, each wrapping whatever is inside the parens whether quoted literal or data name (D-21/D-22/D-23)."
    - "END-EXEC immediately following an unmodelled option is consumed as the terminator and is NOT absorbed into cics_unparsed_tail (RESEARCH Pitfall 1 / A1)."
    - "An unmodelled option form is absorbed into a named cics_unparsed_tail node — never an ERROR node, never silently dropped (D-19)."
    - "A bare option with no parentheses parses, and a block with zero options parses; the option list is repeat(), not repeat1() (D-28b/D-28c)."
    - "A multi-line EXEC CICS block with nothing after the opening on the first line parses identically to its single-line equivalent, with no change to src/scanner.c (D-28d)."
    - "An EXEC SQL block parses no worse than it did before this phase — its parse is unchanged or strictly improved, never degraded (RESEARCH Pitfall 2 / A2)."
    - "A program containing no EXEC CICS block yields zero captures from queries/cics.scm and parses identically to before the phase."
    - "A lowercase spelling of an option keyword and its uppercase spelling produce identical parse trees; an operand value is captured whether it is a quoted literal or a data name."
    - "node_modules/.bin/tree-sitter query queries/cics.scm <fixture> emits all four named operand captures plus the command capture."
    - "The generated src/grammar.json conflicts array is still length 0 after all operand rules are added."
    - statement: "Commits in the grammar series are each independently green under `tree-sitter test -e '^comment$'`; a reordering leaving an intermediate commit red violates separability."
      verification: backstop
  artifacts:
    - grammar.js
    - queries/cics.scm
    - test/corpus/exec_cics.txt
  key_links:
    - "cics_unparsed_tail leaf vocabulary vs the END-EXEC terminator token — the highest residual risk in the phase"
    - "queries/cics.scm four operand captures -> the CICS-02 acceptance criterion"
    - "prec(1, ...) on each operand rule -> keeps the conflicts array empty"
  prohibitions:
    - statement: "MUST NOT widen the cics_unparsed_tail rule until the census reports zero. The tail exists to make the coverage gap countable; tuning it until it stops firing defeats its purpose (D-30)."
      status: unverified
      verification: judgment
    - statement: "MUST NOT hard-code the 25-name corpus-derived option vocabulary into this generic-dialect grammar; only the four criterion operands get named nodes (D-21, the site-specific-hack line PROJECT.md draws)."
      status: unverified
      verification: judgment
---

<objective>
Expand the proven tracer into full option and operand coverage: all four measured argument forms,
the four named operand nodes, the unparsed-tail escape, and the four remaining mandatory fixtures
(`END-EXEC` boundary, `EXEC SQL` no-regression, multi-line, bare/no-option shapes).

Purpose: CICS-01 and CICS-02 are only satisfied when the operands the criterion names are
extractable and unmodelled content degrades gracefully instead of ERRORing.
Output: the complete `exec_cics_statement` grammar surface, the complete `queries/cics.scm`, and a
corpus file carrying every early-signal case RESEARCH identified.
</objective>

<execution_context>
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-exec-cics-blocks/03-CONTEXT.md
@.planning/phases/03-exec-cics-blocks/03-RESEARCH.md
@.planning/phases/03-exec-cics-blocks/03-PATTERNS.md
@.planning/phases/03-exec-cics-blocks/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Option list, all four argument forms, and the unparsed tail</name>
  <precondition>Plan 03-01's D-32 proof case is green — `exec_cics_statement` exists and parses the
  four colliding command words. If 03-01 recorded the D-18 fallback, the command arm here is the
  explicit `choice()` form, not `field('command', $.WORD)`.</precondition>
  <files>grammar.js, test/corpus/exec_cics.txt</files>
  <read_first>
    grammar.js — the `exec_cics_statement` block as plan 03-01 committed it,
    grammar.js:2385-2399 (`_idms_operand_token` / `idms_unparsed_tail`, quoted verbatim in
      PATTERNS.md §D — copy the structure),
    grammar.js:3491 (`_WORD` — its regex matches the terminator word exactly; this is the Pitfall 1
      mechanism),
    grammar.js:2286-2299 (the measured-volume inline comment convention, PATTERNS.md §B),
    .planning/phases/03-exec-cics-blocks/03-PATTERNS.md §B and §D,
    .planning/phases/03-exec-cics-blocks/03-RESEARCH.md § Common Pitfalls 1 and 2,
    .planning/phases/03-exec-cics-blocks/03-FINDINGS.md (the continuation-line answer — if it
      reported any occurrence, stop and escalate before editing the grammar)
  </read_first>
  <behavior>
    - A quoted-literal argument parses (1,993 measured occurrences).
    - A plain data-name argument parses (2,041).
    - A length-of-name argument form parses (527).
    - A numeric-literal argument parses (81).
    - A bare option with no parentheses parses (covering the 1,586 / 450 / 438 bare forms, D-28c).
    - A block with zero options parses (1,251 / 43%, D-28b).
    - An option form the grammar does not model, placed immediately before the terminator, is
      absorbed into `cics_unparsed_tail`, and the terminator is still consumed as the terminator.
  </behavior>
  <action>
    Model the option list per D-29: all four argument forms up front, in one private argument rule.
    This is a stated departure from D-09's two-round minimal-then-census process, justified because
    the census D-09 deferred has already run — CICS's uniform keyword-plus-argument syntax made it
    cheap in a way IDMS's positional forms never were. The expected consequence is that
    `cics_unparsed_tail` measures near zero on first census, which becomes the evidence that
    coverage is complete rather than a to-do list.

    Keep the option list a `repeat()`. A `repeat1()` would reject the no-option shape that is 43% of
    measured blocks.

    Add `cics_unparsed_tail` copying the PATTERNS.md §D structure exactly: a private leaf-token
    choice rule plus a `repeat1()` over it. The IDMS vocabulary already carries the parenthesis
    tokens, which is most of what CICS needs. The bounding differs and this is the phase's highest
    residual risk: IDMS bounds at the literal period; CICS must bound at the terminator, and the
    terminator word matches the `_WORD` regex in full, so the tail is a candidate consumer of it.
    Do not assume the arbitration; Task 2's fixture decides it. If the fixture shows the tail
    consuming the terminator, the standard remedies are a token precedence on the terminator or a
    tail vocabulary that excludes the terminator token explicitly — apply one and record which.

    Place `optional($.cics_unparsed_tail)` as the last element before the terminator, mirroring the
    D-19 analog placement PATTERNS.md §B highlights.

    Write inline comments citing the measured counts that justify each modelled form, following the
    house convention: the four argument-form counts above and the option counts (1,586 / 610 / 594 /
    450 / 438 / 415 / 411 / 33). RESEARCH A5 notes these counts were measured during the discussion
    and not independently reproducible from this repo; cite them as such rather than as re-derivable
    facts.

    Add fixture cases in `test/corpus/exec_cics.txt` for each `<behavior>` bullet, in the exact
    format PATTERNS.md reproduces, with invented neutral names only.

    Do not add a `conflicts:` entry. Do not touch `src/scanner.c` — newlines and comments are
    already `extras`, which is why the multi-line shape is free.
  </action>
  <verify>
    <automated>node_modules/.bin/tree-sitter generate &amp;&amp; node_modules/.bin/tree-sitter test -e '^comment$'</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/.bin/tree-sitter test -e '^comment$'` exits 0.
    - A fixture case whose expected tree contains `cics_unparsed_tail` exists, and that case's
      expected tree shows the terminator token as a sibling of the tail, not inside it.
    - Fixture cases exist for the quoted-literal, data-name, length-of, numeric-literal, bare-option
      and zero-option shapes; each expected tree contains `exec_cics_statement` and no ERROR node.
    - `node -e 'process.exit(require("./src/grammar.json").conflicts.length===0?0:1)'` exits 0.
    - `git diff --name-only` for this task's commit does not include `src/scanner.c`.
  </acceptance_criteria>
  <done>
    Every measured option and argument shape parses; unmodelled content lands in a named tail node
    rather than an ERROR; the terminator boundary is settled by a fixture rather than assumed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The four named operand nodes and the remaining mandatory fixtures</name>
  <reversibility rating="costly">D-21 fixes `cics_transaction_name`, `cics_program_name`,
  `cics_map_name` and `cics_mapset_name` as the node names, and CONTEXT.md rates the decision costly
  because the node names are the query contract: `queries/cics.scm` reads them directly, the
  vendored `forest-shim/cobol/cics.scm` mirrors that file, and `run_cics_query_capture.sh` asserts
  the capture names. Renaming any of the four later means a coordinated change across the grammar,
  the published query, the vendored copy and the fork-local gate.</reversibility>
  <files>grammar.js, test/corpus/exec_cics.txt</files>
  <read_first>
    grammar.js:2372-2383 (the `prec(1, ...)` operand rules and their written justification, quoted
      verbatim in PATTERNS.md §C — copy both the rules and the comment form),
    grammar.js — the option rule as Task 1 committed it,
    test/corpus/idms_session.txt:1-38 (the fixture format),
    .planning/phases/03-exec-cics-blocks/03-PATTERNS.md §C,
    .planning/phases/03-exec-cics-blocks/03-RESEARCH.md § Common Pitfalls 2 and 3
  </read_first>
  <behavior>
    - A transaction option emits `cics_transaction_name` wrapping the paren contents.
    - A program option emits `cics_program_name`, whether the contents are a quoted literal or a
      data name (D-22: 30 of 33 measured program operands are data names).
    - A map option emits `cics_map_name`.
    - A mapset option emits `cics_mapset_name` (D-23, 610 measured occurrences, 1:1 with map).
    - A multi-line block with nothing after the opening on the first line parses to the same shape
      as its single-line equivalent.
    - An `EXEC SQL` block parses no worse than before this phase.
  </behavior>
  <action>
    Add the four operand keyword terminals following the two-part convention: private
    case-insensitive regex in the `_KEYWORD` block plus a public single-line alias, each
    alphabetically placed.

    Add `cics_transaction_name`, `cics_program_name`, `cics_map_name` and `cics_mapset_name` as
    their own named rules — not `field()` labels. Two independent forces require this and both are
    verified. First, depth: `field()` binds only direct children of the declaring rule, so an
    operand nested inside an option sub-rule is unreachable. Second, the consumer: gortex's
    `runQuery` iterates matches and copies captures with no predicate evaluation whatsoever, so a
    predicate-based query compiles and is then silently ignored, matching every option against every
    capture. Predicate-based extraction fails silently, which is worse than failing loudly.

    Use these four node names verbatim — CONTEXT.md fixes them as the `queries/cics.scm` contract.

    Wrap `prec(1, ...)` around each, exactly as the IDMS operand rules do, and carry the same
    written justification PATTERNS.md §C quotes: a bare word immediately after a modelled keyword is
    ambiguous between reducing as the named operand or as the first token of the tail, both being
    single-token wrappers around the same underlying token, and static precedence prefers the
    modelled operand. Per D-22 each operand body is wider than the IDMS bare-word form because it
    wraps whatever is inside the parens — reuse Task 1's argument rule rather than duplicating the
    four forms.

    Add the remaining mandatory fixtures: the multi-line shape, and an `EXEC SQL` regression case.
    The `EXEC SQL` case is load-bearing for Phase 4: the corpus of record contains zero `EXEC SQL`,
    so the recall number cannot detect a degradation there and only a fixture or the estate
    differential can. Record in the fixture case name or a comment what the pre-phase `EXEC SQL`
    parse was, so a later reader can tell improvement from degradation.

    All fixture names invented and neutral per D-32.
  </action>
  <verify>
    <automated>node_modules/.bin/tree-sitter generate &amp;&amp; node_modules/.bin/tree-sitter test -e '^comment$'</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/.bin/tree-sitter test -e '^comment$'` exits 0.
    - `grep -c cics_transaction_name grammar.js`, `grep -c cics_program_name grammar.js`,
      `grep -c cics_map_name grammar.js` and `grep -c cics_mapset_name grammar.js` are each at
      least 1.
    - `test/corpus/exec_cics.txt` contains a case whose expected tree includes `cics_program_name`
      wrapping a data name and another whose expected tree includes `cics_map_name` wrapping a
      quoted literal.
    - A multi-line fixture case and an `EXEC SQL` fixture case both exist and are green.
    - `node -e 'process.exit(require("./src/grammar.json").conflicts.length===0?0:1)'` exits 0.
  </acceptance_criteria>
  <done>
    All four criterion-1 operands are named nodes reachable at any nesting depth, and every
    mandatory early-signal fixture RESEARCH identified is committed and green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Complete the query, extend the capture gate, refresh and vendor</name>
  <files>
    queries/cics.scm,
    run_cics_query_capture.sh,
    src/parser.c,
    src/grammar.json,
    src/node-types.json,
    forest-shim/cobol/parser.c,
    forest-shim/cobol/grammar.json,
    forest-shim/cobol/cics.scm
  </files>
  <read_first>
    queries/idms.scm (the full file — style, comment form, one-pattern-per-capture-role),
    queries/cics.scm as plan 03-01 committed it,
    run_cics_query_capture.sh as plan 03-01 committed it,
    .planning/phases/03-exec-cics-blocks/03-PATTERNS.md — the `queries/cics.scm` section and the
      "Regenerated + vendored artifacts" refresh.sh step table
  </read_first>
  <action>
    Extend `queries/cics.scm` with one pattern per operand capture role — transaction, program, map,
    mapset — alongside the command capture plan 03-01 shipped. One pattern per role, not one pattern
    with alternations. No predicates of any kind.

    Extend `run_cics_query_capture.sh` to assert all five capture names appear in the query output
    against a fixture exercising all four operands.

    Run `forest-shim/refresh.sh` and commit the regenerated `src/` artifacts alongside the refreshed
    `forest-shim/cobol/` files. Do not edit `binding.go`, `plugin.go` or `go.mod` — Step 5 hard-fails
    on any difference against the forest module cache, and reaching for the drift-override
    environment variable when Step 5 fails would mask a real problem. The query reaches the shim
    through Step 3's existing loop; no pipe change is needed.

    Expect Step 2 to report that the regenerated `src/` differs from the committed output. The script
    itself documents that as a finding about which CLI patch produced the committed artifacts, not a
    failure. Check whether the difference predates this change before treating it as a regression.

    Record the D-22 disclosure in the query file's leading comment: criterion 1 is satisfied
    literally, and the program capture generally yields a variable rather than a resolvable edge
    target because 30 of 33 measured program operands are data names. Also record the D-24 finding
    that this file is a published contract with no reader today.
  </action>
  <verify>
    <automated>sh run_cics_query_capture.sh &amp;&amp; forest-shim/refresh.sh &amp;&amp; node_modules/.bin/tree-sitter test -e '^comment$'</automated>
  </verify>
  <acceptance_criteria>
    - `sh run_cics_query_capture.sh` exits 0 and its output names five distinct captures.
    - `grep -v '^;' queries/cics.scm | grep -c '#eq?'` is 0.
    - `forest-shim/refresh.sh` exits 0.
    - `diff queries/cics.scm forest-shim/cobol/cics.scm` reports no difference.
    - `git status --porcelain forest-shim/cobol/go.mod forest-shim/cobol/binding.go forest-shim/cobol/plugin.go` is empty.
  </acceptance_criteria>
  <done>
    The published query exposes all five captures, the fork-local gate asserts them, and the
    vendored shim carries the current grammar so any downstream measurement reads the new parser
    rather than the old one.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
Created by THIS plan (excluded from drift verification — these are new symbols):

**Grammar rules / nodes:** `cics_transaction_name`, `cics_program_name`, `cics_map_name`,
`cics_mapset_name`, `cics_unparsed_tail`, plus the private option/argument/leaf-token rules whose
exact names are the executor's discretion per CONTEXT.md (a `cics_option`-shaped rule with name and
value fields, a private argument rule, and a private leaf-token choice feeding the tail)
**Grammar terminals:** private case-insensitive regex entries and public aliases for the
transaction, program, map and mapset option keywords
**Modified files:** `queries/cics.scm` (four operand patterns added),
`test/corpus/exec_cics.txt` (all remaining cases), `run_cics_query_capture.sh` (five-capture
assertion)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| COBOL source text -> grammar | Untrusted, arbitrarily malformed input; the grammar is the validator. |
| estate/ -> tracked fixtures | Proprietary source must never cross into a tracked path. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `test/corpus/exec_cics.txt`, `queries/cics.scm` | high | mitigate | Every fixture name invented and neutral per D-32; the estate is never a fixture source; the pre-push estate-leak guard backstops. Task 1 and Task 2 acceptance criteria name the fixtures explicitly so a reviewer can read all of them. |
| T-03-02 | Denial of Service (V5) | `cics_unparsed_tail` | medium | mitigate | The tail is `repeat1()` over a fixed leaf vocabulary — linear, bounded by the terminator, no external-scanner loop. Malformed block content degrades to a bounded named node rather than unbounded backtracking. Asserted by the Pitfall-1 boundary fixture in Task 1. |
| T-03-03 | Tampering | `forest-shim/cobol/{binding.go,plugin.go,go.mod}` | medium | mitigate | Never edited; `refresh.sh` Step 5 hard-fails on drift and the override is not used. Task 3 acceptance criterion asserts those three files are unmodified. |
| T-03-SC | Tampering (V14 supply chain) | `refresh.sh` Step 1 | low | accept | Zero external packages introduced; `npm ci` against the committed lockfile only. No legitimacy checkpoint applies. |
</threat_model>

<commit_path_constraints>
This plan produces **(b) grammar commits** and one **(c) fork-local gate commit**.

- **(b)** may touch: `grammar.js`, `src/parser.c`, `src/grammar.json`, `src/node-types.json`,
  `queries/cics.scm`, `test/corpus/exec_cics.txt`, `forest-shim/cobol/{parser.c,grammar.json,cics.scm}`.
- **(c)** — the `run_cics_query_capture.sh` extension — is a separate commit from (b).
- No commit in this plan touches `docs/`, `.planning/`, or `~/repos/mine/GoApps/gortex`.
- Each grammar commit must be independently green under `tree-sitter test -e '^comment$'`.
</commit_path_constraints>

<verification>
- `node_modules/.bin/tree-sitter test -e '^comment$'` green after every task.
- `sh run_cics_query_capture.sh` exits 0 with five captures.
- `forest-shim/refresh.sh` exits 0 with no shim Go-surface drift.
- `src/grammar.json` conflicts array still length 0.
</verification>

<success_criteria>
CICS-01 and CICS-02 are demonstrably satisfied at the fixture and query level: every measured CICS
shape parses to named nodes with no ERROR, unmodelled content lands in a countable named tail, and
all four criterion operands are extractable as named captures.
</success_criteria>

<output>
Create `.planning/phases/03-exec-cics-blocks/03-02-SUMMARY.md` when done.
</output>
