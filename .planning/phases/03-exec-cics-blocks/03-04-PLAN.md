---
phase: 03-exec-cics-blocks
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - docs/baseline.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/03-exec-cics-blocks/03-FINDINGS.md
autonomous: false
requirements: [CICS-04, CICS-05, CICS-06, IDMS-05]

estimate:
  tokens: 85000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Phase 1 paragraph_header baseline is reconstructed by checking out the recorded pre-Phase-2 SHA, running forest-shim/refresh.sh, and re-running the probe — and the number is recorded in docs/baseline.md with a verbatim log-line attestation."
    - "The reconstruction runs and is recorded BEFORE the Phase 3 delta is read; a Phase 3 number measured against a stale or un-restored shim is void."
    - "The working tree is restored to the Phase 3 head and forest-shim/refresh.sh is re-run green after the reconstruction, so no later measurement reads the historical parser."
    - "The reconstructed baseline is recorded as an actual number even when it equals the Phase 2 value; an absent reconstruction blocks IDMS-05 closure rather than being reported as a zero delta."
    - "Grammar-alone .cbl paragraph_header is measured at or above 16,732 — 16,731 fails this gate and 16,733 passes; a measured value of exactly 16,732 PASSES, the comparison being inclusive."
    - "paragraph_header counts are integers produced by an exact node-type string match — no rounding anywhere; the gap-closure fraction in docs/baseline.md shows its arithmetic inline in the §4 style."
    - "The zero-reclassification differential runs on BOTH the DCC corpus and the estate, using the wider NODE_TYPE_REGEX recorded in 03-FINDINGS.md, and reports zero reclassifications on each."
    - "run_nist_cobol85.sh gains no failure beyond the 11 entries already in skip_tests.txt."
    - "A cics_unparsed_tail census is run and reported; if the count is non-zero a written cause naming what is in the tail is recorded."
    - "IDMS-05 and CICS-04 are both closed on the paragraph_header axis, with the reconstructed before/after that closure depends on."
    - statement: "Each commit's `git show --stat` path set falls entirely within exactly one of the four declared per-commit path constraints; no commit spans two."
      verification: backstop
    - statement: "A commit series with zero fork-local file changes still satisfies FORK-03 — the constraint is that no grammar commit touches a fork-local-only path, not that fork-local commits must exist."
      verification: backstop
    - statement: "No name in any tracked file of this phase is byte-equal to an estate identifier; fixture names are ASCII, invented and neutral."
      verification: backstop
  artifacts:
    - docs/baseline.md
    - .planning/REQUIREMENTS.md
  key_links:
    - "03-FINDINGS.md pre-Phase-2 SHA -> the reconstruction checkout -> IDMS-05's before number"
    - "03-FINDINGS.md NODE_TYPE_REGEX -> run_accept_differential.sh snapshot/compare -> D-31's hard gate"
    - "the reconstructed baseline -> the partition of the 1,409 paragraph gap between IDMS and CICS"
  prohibitions:
    - statement: "MUST NOT report a paragraph_header delta without the written cause when the magnitude falls short of the target. The rule is hard-on-direction and explained-on-magnitude; a shortfall is reported and explained, never quietly rounded up to a pass (D-15/D-16c)."
      status: unverified
      verification: judgment
    - statement: "MUST NOT re-tune the grammar to chase the number after the measurement is taken. The measurement is a report on the work, not a target the work is fitted to."
      status: unverified
      verification: judgment
    - statement: "MUST NOT write any estate inventory, differential output, or census artifact under this repository root; run_accept_differential.sh refuses this by design and that refusal must not be worked around."
      status: unverified
      verification: judgment
  flagged_assumptions:
    - "Edge probe row CICS-05/unclassified stayed unresolved: the probe could not classify the requirement onto a shape category. Planner assumption: CICS-05's real edge is the EXEC SQL / EXEC DLI no-regression risk, which is covered by the fixture in plan 03-02 and by the estate differential in this plan — the DCC recall number cannot detect it because the corpus of record contains zero EXEC SQL. Surfaced, not dismissed."
---

<objective>
Measure the phase, reconstruct the missing Phase 1 baseline that both CICS-04 and IDMS-05 depend
on, run every regression gate, and record the results in `docs/baseline.md` — then close the two
requirements the axis re-scope left open.

Purpose: criterion 4 is the terminal condition on the `paragraph_header` axis, and it cannot be
read at all until the Phase 1 number that never existed is reconstructed. That reconstruction also
partitions the 1,409-item gap between what IDMS already closed and what CICS must close, which
sharpens this phase's own claim rather than merely rescuing a requirement.
Output: `docs/baseline.md` §5, a closed IDMS-05 and CICS-04, and green NIST and differential gates.
</objective>

<execution_context>
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-exec-cics-blocks/03-CONTEXT.md
@.planning/phases/03-exec-cics-blocks/03-RESEARCH.md
@.planning/phases/03-exec-cics-blocks/03-PATTERNS.md
@.planning/phases/03-exec-cics-blocks/03-FINDINGS.md
@.planning/phases/03-exec-cics-blocks/03-03-SUMMARY.md
@docs/baseline.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconstruct the Phase 1 paragraph_header baseline (D-26)</name>
  <precondition>`.planning/phases/03-exec-cics-blocks/03-FINDINGS.md` records the pre-Phase-2 SHA
  with its derivation, and the working tree is clean with every plan 03-01 through 03-03 commit
  landed. A dirty tree makes the historical checkout unsafe.</precondition>
  <files>docs/baseline.md</files>
  <read_first>
    .planning/phases/03-exec-cics-blocks/03-FINDINGS.md (the recorded SHA),
    docs/baseline.md §2 (the exact probe invocation to reproduce verbatim) and §4 (the measured-cells
      table, the verbatim-log-line attestation convention, and the gap-closure fraction block),
    .planning/phases/03-exec-cics-blocks/03-PATTERNS.md — the `docs/baseline.md` section, which
      quotes all three recording formats verbatim,
    forest-shim/refresh.sh (the six steps),
    .planning/phases/03-exec-cics-blocks/03-RESEARCH.md § Runtime State Inventory — the paragraph
      warning that this reconstruction is a deliberate, controlled version of the stale-shim hazard
  </read_first>
  <action>
    Reconstruct the missing Phase 1 number. `docs/baseline.md` records the `paragraph_header` pair
    only at Phase 2 completion; there is no Phase 1 number, and that absence is what makes IDMS-05
    unreadable.

    Sequence, in this order and no other:
    1. Confirm the tree is clean, then record the current head to `.git/PHASE3_PRE_CHECKOUT_HEAD`
       (`git rev-parse HEAD > .git/PHASE3_PRE_CHECKOUT_HEAD`) so the restore can be asserted
       mechanically rather than by memory. The path is inside `.git/`, so it is never tracked and
       survives the historical checkout unchanged.
    2. Check out the recorded pre-Phase-2 SHA.
    3. Run `forest-shim/refresh.sh` so the vendored shim carries the historical parser.
    4. Run the probe using the §2 invocation verbatim — same command, same corpus path, same flags,
       so the number is comparable by construction rather than by argument.
    5. Capture the raw log line.
    6. Return the tree to the recorded head.
    7. Run `forest-shim/refresh.sh` again and confirm it exits 0.

    Step 7 is not optional. Every subsequent number in this phase is void if the shim is left
    carrying the historical parser, and the failure is silent — gortex simply measures the old
    grammar and reports a plausible result.

    Record the reconstruction in `docs/baseline.md` as a new section, following the §4 conventions
    PATTERNS.md quotes: cite the pre-Phase-2 SHA as a citable constant, reproduce the §2 invocation,
    present the numbers in the measured-cells table form, and attest each with the verbatim raw log
    line in the existing `<file>:<line>: <raw log line>` form.

    Then partition the 1,409-item gap: how much of it Phase 2's IDMS work already closed, and how
    much remains for CICS. State the arithmetic inline, in the §4 gap-closure-fraction style.

    Record the reconstructed number even if it equals the Phase 2 value. An equal number is a
    finding about IDMS's contribution; an absent number blocks IDMS-05 entirely.
  </action>
  <verify>
    <automated>test -f .git/PHASE3_PRE_CHECKOUT_HEAD &amp;&amp; [ "$(git rev-parse HEAD)" = "$(cat .git/PHASE3_PRE_CHECKOUT_HEAD)" ] &amp;&amp; forest-shim/refresh.sh &amp;&amp; node_modules/.bin/tree-sitter test -e '^comment$' &amp;&amp; grep -qi 'paragraph_header' docs/baseline.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/baseline.md` contains a new section citing the pre-Phase-2 SHA, the reproduced probe
      invocation, and a Phase 1 `paragraph_header` number attested by a verbatim raw log line.
    - The section states the IDMS-vs-CICS partition of the 1,409-item gap with the arithmetic shown
      inline.
    - `git rev-parse HEAD` matches the value recorded in `.git/PHASE3_PRE_CHECKOUT_HEAD` before the
      checkout — asserted mechanically by the automated verify, not by inspection.
    - `forest-shim/refresh.sh` exits 0 after the restore, and
      `node_modules/.bin/tree-sitter test -e '^comment$'` is green — proving the current parser, not
      the historical one, is vendored.
    - No estate path or estate source text appears in the new section.
  </acceptance_criteria>
  <done>
    IDMS-05 has a real before number, CICS-04 has a readable delta, and the working tree and shim
    are restored to the Phase 3 head.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm the D-16d corpus target as the terminal condition on the paragraph_header axis
  before the measurement is recorded as the phase's pass/fail gate.</decision>
  <context>
    D-16d is rated **one-way** in CONTEXT.md. It sets the Phase 3 corpus target at grammar-alone
    `.cbl` `paragraph_header` >= 16,732, closing 100% of the measured 1,409-item gap, and it has
    already replaced ROADMAP criterion 4 and restated both CICS-04 and IDMS-05. Reverting it after
    Phase 4 is planned against it would require re-baselining both phases.

    Task 1 has just produced the one piece of evidence that did not exist when D-16d was locked: the
    reconstructed Phase 1 baseline and the IDMS-vs-CICS partition of the gap. If that partition shows
    IDMS already closed a large share, the residual CICS share — and therefore what a 100% target
    actually demands of this phase — is different from what the decision assumed.

    This checkpoint exists because the door is one-way, not because the decision is in doubt. D-16d
    is locked; the purpose is to look at the new evidence before the gate is cited as pass or fail.
  </context>
  <options>
    <option id="confirm">
      <name>Confirm D-16d unchanged</name>
      <pros>Locked decision honored; Phase 4 plans against a stable axis; no re-baselining.</pros>
      <cons>If the partition shows the residual CICS share is far smaller than 1,409, the target
      measures more than this phase's own contribution.</cons>
    </option>
    <option id="confirm-with-note">
      <name>Confirm, and record the partition as an explicit qualifier on the target</name>
      <pros>Keeps the locked target while making the IDMS-vs-CICS split part of the record, so a
      later reader is not misled about what the number attributes to this phase.</pros>
      <cons>Slightly more to write into docs/baseline.md.</cons>
    </option>
    <option id="escalate">
      <name>Escalate — the reconstruction contradicts D-16d's premise</name>
      <pros>Correct response if the partition falsifies the assumption that the paragraph gap is
      essentially all CICS.</pros>
      <cons>Re-opens a one-way decision and blocks Phase 4 planning until re-scoped.</cons>
    </option>
  </options>
  <resume-signal>Select: confirm, confirm-with-note, or escalate</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run every measurement and regression gate</name>
  <precondition>The working tree is at the Phase 3 head and `forest-shim/refresh.sh` has exited 0
  since the historical checkout was restored. The estate is present and git-excluded.</precondition>
  <files>docs/baseline.md, .planning/phases/03-exec-cics-blocks/03-FINDINGS.md</files>
  <read_first>
    .planning/phases/03-exec-cics-blocks/03-FINDINGS.md (the chosen NODE_TYPE_REGEX and its
      justification),
    run_accept_differential.sh (the header — the snapshot/compare contract, the third argument, the
      same-source-row qualifier heuristic, and the estate guard),
    run_nist_cobol85.sh and skip_tests.txt (the 11 entries),
    docs/baseline.md §2 (the probe invocation),
    .planning/phases/03-exec-cics-blocks/03-RESEARCH.md § Common Pitfalls 3 and 5
  </read_first>
  <action>
    Run four gates and record each result.

    (1) The corpus recall measurement. Use the §2 probe invocation verbatim against the DCC corpus
    of record, and read the grammar-alone `.cbl` `paragraph_header` figure. The axis is the exact
    node-type string match in `neutralize_test.go`, deliberately named to distinguish it from the
    different and much smaller counter elsewhere in the probe package. The gate is hard on direction
    and explained on magnitude: a fall is a failure outright; a rise short of the target is reported
    with a written cause naming why.

    (2) The zero-reclassification differential, on both the DCC corpus and the estate, using the
    wider regex recorded in 03-FINDINGS.md. Phase 2's default would not detect the reclassification
    this gate exists to catch, because the collision surface is the four COBOL verb tokens adjacent
    to the command field. Take the before snapshot against the pre-Phase-3 parser and the after
    snapshot against the current one, then compare. Zero reclassifications is a hard gate on both.
    Write every snapshot and inventory outside this repository root — the script refuses to write
    under it by design and that refusal is a leak guard, not an inconvenience.

    Note in the write-up that the clean-versus-trailing-error qualifier is derived from a
    same-source-row heuristic that assumes one statement per line, which the multi-line block shape
    violates. The qualifier is therefore not evidence about multi-line blocks; the reclassification
    tally is the actual gate and is unaffected.

    (3) The NIST regression. Run `run_nist_cobol85.sh` and confirm no failure appears beyond the 11
    already listed in `skip_tests.txt`.

    (4) The `cics_unparsed_tail` census, using the same differential tool with a regex scoped to the
    tail node — PROJECT.md forbids building a parallel harness and the script's third argument exists
    precisely so this reuse is possible. Because coverage was censused up front, a non-zero tail is
    genuinely informative: it means the discussion's measurement missed a form. If the census is
    non-zero, write a cause naming what is actually in the tail, exactly as Phase 2 did for its own
    census. Do not widen the tail rule in response — that would make the count stop firing without
    making the coverage complete.

    Record all four results, with the raw command and the raw output line for each.
  </action>
  <verify>
    <automated>node_modules/.bin/tree-sitter test -e '^comment$' &amp;&amp; sh run_nist_cobol85.sh</automated>
  </verify>
  <acceptance_criteria>
    - The recorded grammar-alone `.cbl` `paragraph_header` value is stated as an integer with its
      verbatim raw log line, and compared against 16,732 with the comparison shown.
    - `sh run_nist_cobol85.sh` exits 0 with no failure beyond the 11 in `skip_tests.txt`, and the
      failure count is recorded.
    - The differential comparison reports zero reclassifications on the DCC corpus and zero on the
      estate; both raw output lines are recorded.
    - The recorded differential command shows the wider regex from 03-FINDINGS.md, and the output
      path lies outside this repository root.
    - The `cics_unparsed_tail` census count is recorded; if non-zero, a written cause naming the
      contents accompanies it.
    - If the measured value falls short of 16,732, a written cause is recorded and the shortfall is
      reported as a shortfall.
  </acceptance_criteria>
  <done>
    Every CICS-04 and CICS-05 gate has been run against the current parser and its result recorded
    with a reproducible command and raw output.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record the results and close IDMS-05 and CICS-04</name>
  <files>docs/baseline.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    docs/baseline.md §4 (the measured-cells table, verbatim-log-line attestation, gap-closure
      fraction block, and written-cause section form),
    .planning/REQUIREMENTS.md — the IDMS-05 entry and the CICS-01..06 block,
    .planning/ROADMAP.md — Phase 3 criterion 4 as amended by commit 7ffb4de,
    .planning/phases/03-exec-cics-blocks/03-PATTERNS.md — the `docs/baseline.md` section
  </read_first>
  <action>
    Record the Phase 3 delta in `docs/baseline.md` following the §4 conventions exactly: the
    measured-cells table row, the verbatim raw-log-line attestation, and the gap-closure fraction
    with its arithmetic shown inline on the `paragraph_header` axis rather than the retired one.

    Close IDMS-05 on the re-scoped axis, citing the reconstructed before number from Task 1 and the
    Phase 3 after number from Task 2. This is the requirement the axis re-scope was performed to
    rescue, and it is closed here rather than transferred, so no requirement dangles into the next
    milestone.

    Close CICS-04 with the same evidence, on the same axis.

    Mark the remaining CICS checkboxes according to the evidence produced across plans 03-01 through
    03-04 — CICS-01 and CICS-02 from the corpus fixtures and the query capture gate, CICS-03 from the
    gortex parity gate, CICS-05 from the NIST and differential results, CICS-06 from the per-commit
    path review. Do not mark a checkbox whose evidence is missing; report it instead.

    Update STATE.md's current position and ROADMAP's Phase 3 plan list to reflect completion.

    Record the D-22 disclosure alongside the CICS-02 closure: the criterion is satisfied literally,
    and the program-name capture generally yields a variable rather than a resolvable edge target
    because 30 of 33 measured program operands are data names. State it here rather than leaving it
    for whoever consumes the graph to discover.

    Record the D-24 finding alongside it: the published query has no reader today, since gortex's
    forest extractor requests exactly one query kind. CICS-02 is satisfied by the fork-local capture
    gate, which is what the criterion actually asks for.

    If the checkpoint returned `confirm-with-note`, record the IDMS-vs-CICS partition as an explicit
    qualifier on the target.
  </action>
  <verify>
    <automated>grep -q '^- \[x\] \*\*IDMS-05\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -q '^- \[x\] \*\*CICS-04\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -qi 'paragraph_header' docs/baseline.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/baseline.md` contains a Phase 3 measured-cells row on the `paragraph_header` axis with a
      verbatim raw-log-line attestation and a gap-closure fraction whose arithmetic is shown inline.
    - `.planning/REQUIREMENTS.md` IDMS-05 is marked complete and cites both the reconstructed before
      number and the Phase 3 after number.
    - `.planning/REQUIREMENTS.md` CICS-04 is marked complete and cites the same measurement.
    - Every CICS checkbox that is marked complete names the artifact or command that evidences it.
    - The D-22 program-operand disclosure and the D-24 no-reader finding both appear in the record.
    - `.planning/STATE.md` current phase and status reflect Phase 3 completion.
  </acceptance_criteria>
  <done>
    The phase's numbers are on file in the baseline of record, and IDMS-05 and CICS-04 are closed on
    the axis the re-scope moved them to.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
Created by THIS plan (excluded from drift verification — these are new symbols):

**New `docs/baseline.md` section:** the Phase 1 `paragraph_header` reconstruction record plus the
Phase 3 delta, gap-closure fraction, and IDMS-vs-CICS gap partition (recorded as a new section
rather than an amendment to §2, per CONTEXT.md discretion)
**New content in `03-FINDINGS.md`:** the four gate results with their raw commands and raw output
lines
**No new code symbols, no new files outside those named above.**
</artifacts_this_phase_produces>

<verification_ledger>
Reconciled tally for the phase-3 edge probe (corrected 2026-09-09; an earlier tally claimed
4 backstop markers and did not reconcile).

- **17** probe edges in.
- **15** of them are resolved: **11** by explicit `must_haves.truths` statements and **4** by
  `verification: backstop` statements.
- **6** backstop markers exist across the plan set — 1 in 03-01, 1 in 03-02, 1 in 03-03, 3 in
  03-04. Four of the six cover probe rows; the remaining **2** (the FORK-03 zero-fork-local-change
  reading and the byte-equality neutrality statement, both in 03-04) were authored beyond the 17
  probe rows as planner-added invariants. They are stated here rather than folded into the count.
- **2** rows stayed `unresolved` — CICS-01/`unclassified` and CICS-05/`unclassified` — and are
  surfaced as `flagged_assumptions` in 03-01 and 03-04 respectively.

11 + 4 + 2 = 17. No edge was dropped and nothing was dismissed.
</verification_ledger>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| estate/ -> repository root | The differential reads the estate; no output may be written under the repo root. |
| historical checkout -> current measurement | A stale vendored shim silently invalidates every number. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | the estate differential and its snapshots, `docs/baseline.md` | high | mitigate | `run_accept_differential.sh` refuses to write an inventory under the repo root and that refusal is not worked around (asserted by Task 2 acceptance criteria); `.git/info/exclude` keeps `estate/` untracked; the pre-push estate-leak guard backstops; Task 1 and Task 3 acceptance criteria assert no estate path or source text reaches `docs/baseline.md`. |
| T-03-04 | Tampering (measurement integrity) | the vendored shim across the historical checkout | high | mitigate | Task 1 step 1 records the pre-checkout head to `.git/PHASE3_PRE_CHECKOUT_HEAD`; step 7 restores and re-refreshes; the automated verify hard-asserts `git rev-parse HEAD` against that recorded value in an `&amp;&amp;` chain, plus a green refresh and corpus run. Task 2's `<precondition>` re-asserts it before any number is read. Without this the failure is silent and every recorded number is wrong. |
| T-03-05 | Repudiation | `docs/baseline.md` | medium | mitigate | Every number is attested with its verbatim raw log line and the exact command that produced it, following the existing §4 convention — the record is reproducible rather than asserted. |
| T-03-SC | Tampering (V14 supply chain) | `refresh.sh` Step 1 | low | accept | Zero external packages introduced; `npm ci` against the committed lockfile only. No legitimacy checkpoint applies. |
</threat_model>

<commit_path_constraints>
This plan produces **docs and planning commits only** — the remaining half of constraint (a).

- The docs amendment commit for the six pre-planning amendments **ALREADY LANDED as `7ffb4de`**.
  Do not re-plan, re-do, or amend it.
- **docs commit** may touch: `docs/baseline.md` only.
- **planning commit** may touch: `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`,
  `.planning/STATE.md`, `.planning/phases/03-exec-cics-blocks/03-FINDINGS.md`. Separate from the
  docs commit.
- No commit in this plan touches `grammar.js`, `src/`, `queries/`, `test/corpus/`, `forest-shim/`,
  or `~/repos/mine/GoApps/gortex`. If the historical checkout leaves any of those modified, the
  restore in Task 1 step 6 did not complete and must be fixed before committing.
- The four constraint sets across the phase are: (a) docs — `docs/` and `.planning/`, this plan;
  (b) grammar — `grammar.js`, `src/`, `queries/`, `test/corpus/`, `forest-shim/cobol/`, plans 03-01
  and 03-02; (c) fork-local gates — `run_cics_query_capture.sh` and `.git/info/exclude`, plans 03-01
  and 03-02; (d) cross-repo — `cascade_test.go` in gortex, plan 03-03. No commit spans two.
</commit_path_constraints>

<verification>
- `node_modules/.bin/tree-sitter test -e '^comment$'` green.
- `sh run_nist_cobol85.sh` green at no more than the 11 known skips.
- Both differential comparisons report zero reclassifications.
- `git rev-parse HEAD` after Task 1 matches `.git/PHASE3_PRE_CHECKOUT_HEAD`.
- `git show --stat` per commit matches exactly one path constraint.
</verification>

<success_criteria>
CICS-04 is measured and recorded on the `paragraph_header` axis against a reconstructed Phase 1
baseline; CICS-05's NIST and differential gates are green; IDMS-05 is closed rather than
transferred; and every number in `docs/baseline.md` is reproducible from a recorded command.
</success_criteria>

<output>
Create `.planning/phases/03-exec-cics-blocks/03-04-SUMMARY.md` when done.
</output>
