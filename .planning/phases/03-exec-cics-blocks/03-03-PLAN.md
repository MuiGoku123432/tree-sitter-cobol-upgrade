---
phase: 03-exec-cics-blocks
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - ~/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go
autonomous: true
requirements: [CICS-03, CICS-06]

estimate:
  tokens: 55000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "TestErrorCascade injects four representative EXEC CICS shapes after paragraph 1: a paren-option form, a no-option form, a bare-option form, and a multi-line block (D-28 a-d)."
    - "Each of the four shapes reaches paragraph parity with the clean control — 20 of 20 paragraphs surviving against a measured baseline of 1 of 20."
    - "Each of the four shapes reaches data-item parity with the clean control — 20 of 20 — so a paragraph fix cannot silently break the DATA DIVISION (D-27)."
    - "An EXEC CICS block with no options injected after paragraph 1 leaves all following paragraphs and all data items at parity (the zero-option edge case)."
    - "The clean-control assertion still fires first: if the control moves off 20/20 the test fails before any parity comparison is read."
    - "Parity assertions are independent of injection order — each shape is measured against the same immutable clean control, not against the previous shape."
    - "The SCHEMA SECTION case remains logged-only and unasserted, per locked decision D3."
    - "The WORKING-STORAGE negative control still asserts that an error is reported, not parity."
    - statement: "A `-count=2` rerun of TestErrorCascade yields identical counts; the cases run sequentially in one test function against an immutable clean control with no shared mutable state."
      verification: backstop
  artifacts:
    - "~/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go"
  key_links:
    - "forest-shim/cobol (this fork) -> gortex go.work `use` list -> cobolprobe reads the vendored parser"
    - "cascade_test.go countAll -> the exact-string paragraph_header and data_description counters"
  prohibitions:
    - statement: "MUST NOT weaken or scope-narrow an assertion to make the gate pass. If a shape does not reach parity, the grammar is wrong and returns to plan 03-02 — the gate is not the thing to adjust."
      status: unverified
      verification: judgment
    - statement: "MUST NOT introduce any estate-derived program, map, transaction or record name into the injected shapes; the file's own inline disclaimer states every name is invented and neutral."
      status: unverified
      verification: judgment
---

<objective>
Turn the harness's logged `EXEC CICS` cascade line into a hard, four-shape assertion in gortex's
`cascade_test.go` — the blocking gate for CICS-03.

Purpose: CICS-03's measured baseline is 1 of 20 paragraphs surviving an `EXEC CICS`. The
criterion is only real when the harness fails the build if parity is lost. The file's own comment
at the assertion site pre-sanctions this edit as Phase 3's gate, so this is a planned hand-off,
not an unplanned cross-repo reach.
Output: four representative shapes injected, logged, and asserted at both paragraph and data-item
parity.
</objective>

<execution_context>
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-exec-cics-blocks/03-CONTEXT.md
@.planning/phases/03-exec-cics-blocks/03-PATTERNS.md
@.planning/phases/03-exec-cics-blocks/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject the four D-28 shapes and log them, without asserting</name>
  <precondition>`forest-shim/refresh.sh` has run green after plan 03-02's final grammar commit, so
  gortex's `go.work` resolution reads the current parser. A skipped refresh means every count in
  this plan is measured against the old grammar and silently reports a null result.</precondition>
  <files>~/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go</files>
  <read_first>
    ~/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go — the whole file,
    .planning/phases/03-exec-cics-blocks/03-PATTERNS.md — the `cascade_test.go` section, which
      quotes the existing EXEC CICS case, the IDMS injection idiom with its `const` statement and
      invented-names disclaimer, the recovery summary block, the parity-assertion pattern, the
      negative control, and the counter switch — all verbatim,
    .planning/phases/03-exec-cics-blocks/03-RESEARCH.md § Common Pitfalls 7
  </read_first>
  <action>
    Add three injected shapes alongside the paren-option form already present, following the IDMS
    injection idiom PATTERNS.md quotes: a `const` for the statement text, the invented-and-neutral
    disclaimer inline, and reuse of the existing `lines` split so all four inject in exactly the same
    place and are directly comparable.

    The three new shapes, chosen for measured coverage rather than for variety:
    (b) a no-option form — the corpus's most common shape at 1,251 blocks / 43%, and the form most
    likely to break a repeat-based body rule;
    (c) a bare-option form, covering the bare options measured at 1,586 / 450 / 438;
    (d) a multi-line block, covering the 51 blocks with nothing after the opening on the first line.

    Add one summary line per new shape to the recovery summary block, matching the existing
    column-aligned format so the four read as a set.

    Do NOT add assertions in this task. Land the shapes and observe them logging first. This is the
    safer of the two sequences RESEARCH identifies: the shapes can be landed and read before they
    become a hard gate, so a surprising number is diagnosed rather than turning into a red build of
    ambiguous origin.

    Do not change `countAll` — all four shapes reuse it, and its counters are exact string matches
    on the node types the criterion is measured on.
  </action>
  <verify>
    <automated>cd ~/repos/mine/GoApps/gortex &amp;&amp; go test ./internal/parser/forest/cobolprobe/ -run TestErrorCascade -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/parser/forest/cobolprobe/ -run TestErrorCascade -v` exits 0.
    - The verbose output contains four distinct recovery summary lines for the four `EXEC CICS`
      shapes, each reporting a paragraph count and a data-item count.
    - All four reported paragraph counts equal the clean-control paragraph count.
    - All four reported data-item counts equal the clean-control data-item count.
    - `git diff` in the gortex repo shows changes confined to `cascade_test.go`.
  </acceptance_criteria>
  <done>
    Four representative `EXEC CICS` shapes are injected and their recovery numbers are visible in
    the test log, read before any of them can fail a build.
  </done>
</task>

<task type="auto">
  <name>Task 2: Turn the four shapes into blocking parity assertions</name>
  <files>~/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go</files>
  <read_first>
    ~/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go as Task 1 left it,
    .planning/phases/03-exec-cics-blocks/03-PATTERNS.md — the parity-assertion pattern and the
      pre-sanctioning comment block, both quoted verbatim,
    .planning/phases/03-exec-cics-blocks/03-CONTEXT.md D-27
  </read_first>
  <behavior>
    - Each of the four shapes asserts paragraph parity against the clean control.
    - Each of the four shapes asserts data-item parity against the clean control.
    - The clean-control guard still runs before every parity comparison.
    - Removing the grammar change would make each of the eight new assertions fail — this is the
      test's fail-first property and it is what makes the gate real.
  </behavior>
  <action>
    Add a paragraph-parity assertion and a data-item-parity assertion for each of the four shapes,
    copying the assertion shape PATTERNS.md quotes verbatim — the same comparison against the clean
    control, the same message form naming what got and what was wanted.

    The data-item assertions are not redundant. Data items already sit at parity today; asserting
    them means a future paragraph fix cannot silently break the DATA DIVISION.

    Amend the comment block that currently scopes the assertions to the IDMS cases only. It states
    that this case is a later gate and that hard-asserting it would fail the build on a known-open
    problem. That statement is no longer true for this construct and leaving it in place would
    mislead the next reader. Rewrite it to record which cases are now asserted and which remain
    deliberately logged-only, and why.

    Leave the `SCHEMA SECTION` case logged-only. Locked decision D3 assigns it to preprocessing in a
    different repository, and asserting it would fail the build on a problem this phase does not fix.

    Leave the `WORKING-STORAGE` negative control unchanged. It asserts an error is still reported
    rather than asserting recovery, and it is the precedent for how a must-still-fail case is
    written.
  </action>
  <verify>
    <automated>cd ~/repos/mine/GoApps/gortex &amp;&amp; go test ./internal/parser/forest/cobolprobe/ -run TestErrorCascade -v -count=2</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/parser/forest/cobolprobe/ -run TestErrorCascade -v -count=2` exits 0 and
      both runs report identical counts.
    - `cascade_test.go` contains eight new parity assertions — four on the paragraph counter and
      four on the data-item counter — one pair per `EXEC CICS` shape.
    - The comment block preceding the assertions no longer describes the `EXEC CICS` case as
      unasserted, and names the cases that remain logged-only with their reason.
    - The `SCHEMA SECTION` case has no assertion.
    - The `WORKING-STORAGE` negative control still asserts a non-zero error count.
    - Fail-first evidence: with this fork's `forest-shim/cobol` reverted to the pre-Phase-3 parser,
      the four paragraph-parity assertions fail. Record the observed pre-Phase-3 paragraph count in
      the plan summary.
  </acceptance_criteria>
  <done>
    CICS-03's blocking gate is real: four representative `EXEC CICS` shapes fail the gortex build if
    the paragraphs or the data items following them stop surviving at parity.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
Created by THIS plan (excluded from drift verification — these are new symbols):

**New test constants/locals in `cascade_test.go`:** the three new injected-statement constants and
their per-shape count variables (exact identifier spelling at the executor's discretion, following
the file's existing `idmsStmt` / `e4, d4, pa4, ce4` naming convention)
**New assertions:** eight parity assertions, four paragraph and four data-item
**Modified file:** `~/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go`
No new function names, no new files, no new Go dependency.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| estate/ -> gortex test source | Injected shapes are hand-written; proprietary source must never cross. |
| this fork's vendored shim -> gortex | `go.work` resolution; a stale shim silently changes every measured number. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | the four injected shape constants | high | mitigate | Every injected name is invented and neutral; the file already carries the inline disclaimer stating this, and the new shapes extend it. gortex is a separate repository and the estate is never read here. |
| T-03-04 | Tampering (measurement integrity) | `go.work`-resolved shim | medium | mitigate | Task 1's `<precondition>` asserts the refresh ran green after 03-02's final commit; without it every count is measured against the old parser and reports a false result. |
| T-03-SC | Tampering (V14 supply chain) | gortex `go.mod` | low | accept | No Go module is added; the edits use only stdlib plus already-vendored imports. No legitimacy checkpoint applies. |
</threat_model>

<commit_path_constraints>
This plan produces **(d) cross-repo gortex commits ONLY**.

- Both commits land in `~/repos/mine/GoApps/gortex`, touching `cascade_test.go` and nothing else.
- These commits are NOT part of this fork's separable grammar series and must never be mixed with
  it. FORK-03 scopes the separability property to this repository; a cross-repo commit is outside it.
- Two commits, not one: the shapes land and are observed logging before the assertions turn them
  into a hard gate. The two edits have different failure modes and the safer sequence separates them.
- No commit in this plan touches any path in `tree-sitter-cobol-upgrade`.
</commit_path_constraints>

<verification>
- `go test ./internal/parser/forest/cobolprobe/ -run TestErrorCascade -v -count=2` green.
- `git -C ~/repos/mine/GoApps/gortex show --stat` for each commit lists only `cascade_test.go`.
- `git -C <this fork> status --porcelain` is unchanged by this plan.
</verification>

<success_criteria>
CICS-03 is satisfied by a blocking gate rather than by a log line: four representative `EXEC CICS`
shapes each hold 20 of 20 paragraphs and 20 of 20 data items against the clean control, and the
build fails if any of them regresses.
</success_criteria>

<output>
Create `.planning/phases/03-exec-cics-blocks/03-03-SUMMARY.md` when done.
</output>
