---
phase: 01-delivery-pipe-measurement-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - forest-shim/cobol/go.mod
  - forest-shim/cobol/binding.go
  - forest-shim/cobol/plugin.go
  - forest-shim/cobol/alloc.h
  - forest-shim/cobol/array.h
  - forest-shim/cobol/sample.scm
  - forest-shim/cobol/_keep.scm
  - forest-shim/cobol/parser.c
  - forest-shim/cobol/parser.h
  - forest-shim/cobol/scanner.c
  - forest-shim/cobol/grammar.json
  - forest-shim/cobol/smoke_test.go
autonomous: true
requirements: [VEND-01, VEND-02]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "gortex resolves `github.com/alexaandru/go-sitter-forest/cobol` to `forest-shim/cobol/` in this fork — `go list -m -f '{{.Dir}}'` prints the fork path, not a GOMODCACHE path (VEND-02, D-05)"
    - "`cobolprobe`'s inline-fixture tests (TestErrorCascade, TestHypothesisIdentificationParagraphs, TestHypothesisIDMS) pass end-to-end against the shim with the UNMODIFIED grammar (VEND-02)"
    - "The shim exposes the same three exported functions forest exposes — GetLanguage, GetQuery, Info — so a later gortex change cannot fail as an undefined symbol at the resolution boundary (VEND-01, D-03)"
    - "gortex is demonstrably loading THIS fork's grammar and not forest's stale copy: Info() returns the fork's 428,580-byte grammar.json, whereas forest v1.9.1 ships a 327-byte placeholder (VEND-01)"
    - "The pipe is switchable in one filesystem operation — deleting gortex's go.work returns gortex to forest v1.9.1 with zero committed changes to gortex (D-05)"
  artifacts:
    - path: "forest-shim/cobol/go.mod"
      provides: "The drop-in module identity"
      contains: "module github.com/alexaandru/go-sitter-forest/cobol"
    - path: "forest-shim/cobol/binding.go"
      provides: "cgo language binding, byte-faithful mirror of forest v1.9.1 (D-03)"
      contains: "func GetLanguage() unsafe.Pointer"
    - path: "forest-shim/cobol/plugin.go"
      provides: "plugin-tagged variant of the same surface (D-03)"
      contains: "//go:build plugin"
    - path: "forest-shim/cobol/parser.c"
      provides: "This fork's generated parse tables, flattened and include-rewritten (D-04)"
      contains: "#include \"parser.h\""
    - path: "forest-shim/cobol/scanner.c"
      provides: "This fork's external scanner, flattened and include-rewritten (D-04)"
      contains: "#include \"parser.h\""
    - path: "forest-shim/cobol/grammar.json"
      provides: "This fork's grammar, embedded and surfaced by Info()"
      min_lines: 1
    - path: "forest-shim/cobol/smoke_test.go"
      provides: "Committed regression pin on the drop-in contract"
      contains: "func TestShimExposesForestSurface"
  key_links:
    - from: "~/repos/mine/GoApps/gortex/go.work"
      to: "forest-shim/cobol/"
      via: "go work use — workspace member resolution, bypasses go.sum and the module cache entirely"
      pattern: "forest-shim/cobol"
    - from: "forest-shim/cobol/binding.go"
      to: "forest-shim/cobol/parser.c"
      via: "cgo — the C symbol tree_sitter_COBOL declared above import \"C\""
      pattern: "tree_sitter_COBOL"
    - from: "forest-shim/cobol/binding.go"
      to: "forest-shim/cobol/grammar.json"
      via: "go:embed — Info() returns the embedded grammar.json bytes"
      pattern: "go:embed"
  prohibitions:
    - statement: "MUST NOT let the shim become a second, hand-edited copy of the grammar — every C/JSON file in forest-shim/cobol/ is a mechanical copy of this fork's src/ output, never hand-authored or hand-patched, so what gortex measures is always what this fork actually generates"
      category: transparency
---

<objective>
Build the drop-in Go module that impersonates `github.com/alexaandru/go-sitter-forest/cobol`
inside this fork, and wire gortex to resolve that import to it — proving the delivery pipe
end-to-end with the **unmodified** grammar so any failure is unambiguously the pipe, not the
grammar (CONTEXT.md `<domain>`).

Purpose: gortex today imports a third party's photocopy of upstream. Edits in this fork cannot
reach gortex through that dependency without an upstream merge *and* a forest re-scrape —
"months, or never" (SPEC-001 §8). This plan closes that gap locally, before any grammar work
exists to be stranded behind it (locked decision D5).

Output: `forest-shim/cobol/` (11 vendored files + a committed smoke test), and an uncommitted,
gitignored `go.work` in the gortex checkout that switches the pipe on.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-delivery-pipe-measurement-baseline/01-CONTEXT.md
@.planning/phases/01-delivery-pipe-measurement-baseline/01-RESEARCH.md
@.planning/phases/01-delivery-pipe-measurement-baseline/01-PATTERNS.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols and files created by Phase 1 (the whole phase, so the source-grounding pass does not
flag them as drift). **Bold** entries are created by this plan.

**Files — the shim (this plan):**
- **`forest-shim/cobol/go.mod`**, **`binding.go`**, **`plugin.go`**, **`alloc.h`**, **`array.h`**,
  **`sample.scm`**, **`_keep.scm`**, **`parser.c`**, **`parser.h`**, **`scanner.c`**,
  **`grammar.json`**, **`smoke_test.go`**

**Go symbols exposed by the shim package `cobol` (this plan):**
- **`GetLanguage() unsafe.Pointer`**, **`GetQuery(kind string, opts ...byte) (out []byte)`**,
  **`Info() string`**
- **`NvimFirst`**, **`NativeFirst`**, **`NvimOnly`**, **`NativeOnly`** (byte constants),
  **`nvimts`** (unexported const), **`files`** (unexported `embed.FS`)
- **`TestShimExposesForestSurface`**, **`TestShimEmbedsThisForksGrammar`** (in `smoke_test.go`)

**Files — later plans in this phase:**
- `forest-shim/refresh.sh` (plan 03) and its env knobs `TOP_DIR`, `SHIM_DIR`, `TREE_SITTER`,
  `FOREST_CACHE`, `REFRESH_LOG`
- `forest-shim/pipe-probe.sh` (plan 03) and its env knobs `GORTEX_DIR`, `PROBE_CAPTURE_DIR`,
  `PROBE_ABORT_AFTER`
- `.githooks/pre-push`, `.githooks/estate-guard.sh`, `.githooks/estate-guard-selftest.sh`,
  `.githooks/commit-separability.sh` (plan 02)
- `docs/vendoring.md` (plan 04), `docs/baseline.md` (plan 05)
- `.planning/phases/01-delivery-pipe-measurement-baseline/COVERAGE.md` (plan 04)

**Outside this repo (uncommitted, already gitignored by the consumer):**
- `~/repos/mine/GoApps/gortex/go.work`, `go.work.sum`
</artifacts_this_phase_produces>

<flagged_assumptions>
## Edge Coverage — unresolved rows (flagged assumptions, not dropped)

The spec-less edge probe returned two `unclassified` rows for this plan's requirements. They are
recorded here rather than silently dropped; neither is auto-resolved.

| Requirement | Probe | Why it stays unresolved |
|---|---|---|
| VEND-01 | unclassified — review manually | The probe's shape taxonomy (adjacency / empty / ordering / encoding) has no purchase on "a Go module mirrors a third party's file layout". The drop-in contract is pinned instead by the committed smoke test and by `go list -m`, both explicit `must_haves.truths` above. No further edge is claimed. |
| VEND-02 | unclassified — review manually | Same: "a consumer resolves an import to a local path" is a resolution fact, not a data-shape edge. Pinned by the `go list -m -f '{{.Dir}}'` assertion and the three inline-fixture cobolprobe tests. |
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "a grammar edit in this fork is what gortex parses with" — one path only</name>
  <precondition>`$(go env GOMODCACHE)/github.com/alexaandru/go-sitter-forest/cobol@v1.9.1/binding.go` exists (forest v1.9.1 is in the module cache) and `~/repos/mine/GoApps/gortex/go.work` does not yet exist (the pipe is currently off).</precondition>
  <reversibility rating="costly">Per D-01 the shim's directory path is named by gortex's `go.work`, the refresh script's copy targets, and every doc that cites it; relocating it later means moving all of those together, though nothing breaks silently.</reversibility>
  <files>forest-shim/cobol/go.mod, forest-shim/cobol/binding.go, forest-shim/cobol/plugin.go, forest-shim/cobol/alloc.h, forest-shim/cobol/array.h, forest-shim/cobol/sample.scm, forest-shim/cobol/_keep.scm, forest-shim/cobol/parser.c, forest-shim/cobol/parser.h, forest-shim/cobol/scanner.c, forest-shim/cobol/grammar.json</files>
  <read_first>
    - `.planning/phases/01-delivery-pipe-measurement-baseline/01-PATTERNS.md` — section "`forest-shim/cobol/binding.go` and `plugin.go`" holds the full 79-line source to mirror, the cgo directive to preserve, and the exact `go.mod` text
    - `.planning/phases/01-delivery-pipe-measurement-baseline/01-RESEARCH.md` — sections "The drop-in contract (VEND-01)", "How gortex resolves the shim (VEND-02, D-05, D-06)", "The refresh sequence, corrected", and "Pitfall 1"
    - `.planning/phases/01-delivery-pipe-measurement-baseline/01-CONTEXT.md` — decisions D-01 through D-06
    - `$(go env GOMODCACHE)/github.com/alexaandru/go-sitter-forest/cobol@v1.9.1/binding.go` — the byte-faithful source for the mirror
    - `$(go env GOMODCACHE)/github.com/alexaandru/go-sitter-forest/cobol@v1.9.1/plugin.go`
    - `$(go env GOMODCACHE)/github.com/alexaandru/go-sitter-forest/cobol@v1.9.1/go.mod`
    - `src/parser.c` (first line only — the include form) and `src/scanner.c` (first line only — the other include form)
    - `~/repos/mine/GoApps/gortex/go.mod` line 28 and `~/repos/mine/GoApps/gortex/.gitignore` lines 23-24
    - `~/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go` — confirms `TestErrorCascade` uses inline fixtures and needs no corpus flag
  </read_first>
  <action>
Wire ONE path from this fork's generated parser all the way to gortex's parse output. No second
consumer, no batching, no refresh automation — that is plan 03's job.

**Step 1 — create the shim directory.** `forest-shim/cobol/` at the repo root, per D-01: a
dedicated fork-local directory, NOT `bindings/go/` (which stays free for a genuine Go binding of
this grammar) and NOT the repo root. The directory name is honest about what the package does:
it impersonates a third party's module.

**Step 2 — mirror the Go surface byte-for-byte (D-03).** Copy these files unmodified from
`$(go env GOMODCACHE)/github.com/alexaandru/go-sitter-forest/cobol@v1.9.1/`:
`binding.go` (1,151 bytes, build tag `!plugin`, `package cobol`), `plugin.go` (1,149 bytes, build
tag `plugin`, `package main`), `alloc.h` (985 bytes), `array.h` (10,431 bytes), `sample.scm`
(183 bytes), `_keep.scm` (0 bytes, present on purpose), and `go.mod` (63 bytes). Do not paraphrase,
do not reformat, do not "improve" them. `go.mod` must keep the module path
`github.com/alexaandru/go-sitter-forest/cobol` verbatim — that line IS the drop-in contract — and
keep its `go 1.22.2` directive; only bump the `go` line if the build rejects it, and record that in
the SUMMARY if you do. gortex calls only `GetLanguage()` today, but mirroring `GetQuery()` and
`Info()` too means a later gortex change cannot fail as an undefined symbol at the resolution
boundary, which is a confusing place to debug (D-03).

`alloc.h` and `array.h` are inert today — nothing in this fork's generated `parser.h` references
them. Copy them anyway for file-set fidelity; do not treat their presence as proof the mirror is
exercised.

**Step 3 — copy and flatten this fork's generated output (D-04).** Physically copy (never symlink —
cgo compiles only `.c` files sitting in the package directory and `//go:embed` refuses to follow
symlinks): `src/parser.c` → `forest-shim/cobol/parser.c` (30,660,349 bytes), `src/scanner.c` →
`scanner.c`, `src/grammar.json` → `grammar.json` (428,580 bytes), and `src/tree_sitter/parser.h` →
`forest-shim/cobol/parser.h` (flattened out of its `tree_sitter/` subdirectory, because forest's
layout has the header at the package root).

**Step 4 — rewrite the include directives in the two copied C files.** This is a required step, not
cleanup: flattening the header's *location* does not rewrite what the C files *ask for*, and the
cgo build fails at the C compile stage with a missing-header error until both are rewritten. The
two files use two different include syntaxes for the same header — `src/parser.c` line 1 uses the
double-quoted form, `src/scanner.c` line 1 uses the angle-bracket form — so one sed pattern will
not catch both. Read both first lines, then rewrite each to include the flattened `parser.h`
sitting next to it. The exact two `sed` invocations are given verbatim in PATTERNS.md under
"Critical pattern — the `#include` rewrite".

**Step 5 — build the shim standalone.** `go build ./...` from inside `forest-shim/cobol/`. This
compiles a 30 MB `parser.c` through cgo; the first build takes minutes, later builds are cached.
A missing-header error here means step 4 was incomplete.

**Step 6 — wire gortex (D-05, D-06).** In `~/repos/mine/GoApps/gortex` (the checkout that holds
`internal/parser/forest/cobolprobe/`; the checkout at `~/repos/else/gortex` is NOT the target),
run a single `go work init . <absolute-path-to-forest-shim/cobol>`. Make **no** committed change to
gortex: do not touch its `go.mod`, do not add a `replace` (gortex's existing `go-pointer` /
`renameio` replaces point at in-tree paths; ours would be a relative path outside the repo and
would break every other checkout, gortex CI included). `go.work` and `go.work.sum` are already in
gortex's `.gitignore` at lines 23-24, so the whole pipe is zero committed bytes in the consumer and
toggles off by deleting one file.

**Step 7 — prove the path end-to-end.** From gortex: `go env GOWORK` must print the workspace file's
absolute path; `go list -m -f '{{.Dir}}' github.com/alexaandru/go-sitter-forest/cobol` must print
the fork's shim directory rather than a GOMODCACHE path; and
`go test ./internal/parser/forest/cobolprobe/ -run 'TestErrorCascade|TestHypothesis' -v` must pass.
Those three cobolprobe tests build their COBOL fixtures inline and take no corpus flag, so they run
in seconds and genuinely exercise gortex → shim → this fork's parse tables. Do not run
`TestCobolParseRate` / `TestNeutralizedParseRate` here — those need the corpus flags and are the
20-minute phase-gate measurement owned by plan 05.

Commit the shim per D-02: all artifacts tracked, including the 30.6 MB `parser.c`. The trade-off is
accepted deliberately — a clean clone builds with no prerequisite refresh step, and a shim diff is
the proof that a grammar edit actually propagated. Follow D-12: this commit touches only fork-local
paths, so it must not be mixed with any `grammar.js` or `test/corpus/*` change (there are none in
this phase, so simply never `git commit -a`).
  </action>
  <verify>
    <automated>cd ~/repos/mine/GoApps/gortex && test -n "$(go env GOWORK)" && go list -m -f '{{.Dir}}' github.com/alexaandru/go-sitter-forest/cobol | grep -q 'forest-shim/cobol' && go test ./internal/parser/forest/cobolprobe/ -run 'TestErrorCascade|TestHypothesis' -v</automated>
  </verify>
  <acceptance_criteria>
    - `forest-shim/cobol/go.mod` contains `module github.com/alexaandru/go-sitter-forest/cobol`
    - `forest-shim/cobol/binding.go` contains `//go:build !plugin` and `func GetLanguage() unsafe.Pointer` and `func GetQuery(kind string, opts ...byte) (out []byte)` and `func Info() string`
    - `forest-shim/cobol/plugin.go` contains `//go:build plugin`
    - `diff -q forest-shim/cobol/binding.go "$(go env GOMODCACHE)/github.com/alexaandru/go-sitter-forest/cobol@v1.9.1/binding.go"` reports no difference (D-03 byte-faithful mirror)
    - `diff -q forest-shim/cobol/plugin.go "$(go env GOMODCACHE)/github.com/alexaandru/go-sitter-forest/cobol@v1.9.1/plugin.go"` reports no difference
    - `cmp -s forest-shim/cobol/parser.h src/tree_sitter/parser.h` exits 0 (flattened, content unchanged)
    - `grep -c '#include "parser.h"' forest-shim/cobol/parser.c` is at least 1 and `grep -c '#include "parser.h"' forest-shim/cobol/scanner.c` is at least 1 (both include forms rewritten to the flattened header)
    - `cmp -s forest-shim/cobol/grammar.json src/grammar.json` exits 0
    - `ls forest-shim/cobol/` lists all of: `_keep.scm alloc.h array.h binding.go go.mod grammar.json parser.c parser.h plugin.go sample.scm scanner.c`
    - `cd forest-shim/cobol && go build ./...` exits 0
    - `cd ~/repos/mine/GoApps/gortex && go env GOWORK` prints a non-empty path ending in `go.work`
    - `cd ~/repos/mine/GoApps/gortex && go list -m -f '{{.Dir}}' github.com/alexaandru/go-sitter-forest/cobol` prints a path under this fork containing `forest-shim/cobol`
    - `cd ~/repos/mine/GoApps/gortex && go test ./internal/parser/forest/cobolprobe/ -run 'TestErrorCascade|TestHypothesis' -v` exits 0
    - `cd ~/repos/mine/GoApps/gortex && git status --porcelain` reports no modification to `go.mod`, `go.sum`, or any tracked file (the pipe costs the consumer zero committed bytes)
  </acceptance_criteria>
  <done>gortex's COBOL parse output comes from this fork's `src/`, proven by three passing inline-fixture cobolprobe tests, and the whole shim is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the drop-in contract with a committed shim smoke test</name>
  <files>forest-shim/cobol/smoke_test.go</files>
  <read_first>
    - `forest-shim/cobol/binding.go` — the exact signatures and the `//go:embed grammar.json *.scm` block the test asserts against
    - `forest-shim/cobol/sample.scm` — the 183-byte query file `GetQuery("sample")` must return
    - `.planning/phases/01-delivery-pipe-measurement-baseline/01-RESEARCH.md` — "tree-sitter-cli version reality", which records that forest v1.9.1 ships a 327-byte `grammar.json` placeholder while this fork's is 428,580 bytes; that gap is the discriminator this test uses
    - `.planning/phases/01-delivery-pipe-measurement-baseline/01-VALIDATION.md` — the VEND-01 row calls for exactly this build+smoke check
  </read_first>
  <behavior>
    - `TestShimExposesForestSurface`: `GetLanguage()` returns a non-nil `unsafe.Pointer`; `GetQuery("sample")` returns a non-empty byte slice; `GetQuery("sample.scm")` returns the same bytes (the function trims and re-appends the suffix); `Info()` returns a non-empty string.
    - `TestShimEmbedsThisForksGrammar`: `Info()` parses as JSON via `encoding/json` into a map whose `name` key equals `COBOL`, and its length is far above forest v1.9.1's 327-byte placeholder — assert `len(Info()) > 100000`. This is the assertion that fails loudly if a future refresh ever embeds forest's stale grammar instead of this fork's.
    - Edge: `GetQuery("does-not-exist")` returns an empty slice and does not panic.
  </behavior>
  <action>
Add `forest-shim/cobol/smoke_test.go` in `package cobol`, holding the two tests described in
`<behavior>` plus the missing-query edge case. Write the assertions first and confirm they express
the contract before filling in any helper.

Use only the standard library — `testing`, `encoding/json` — so the shim module keeps a bare
`go.mod` with no `require` block, exactly as forest's does. Do not add a `require` line and do not
create a `go.sum`.

Keep the file to `_test.go` so it is never compiled into gortex or any other consumer: the drop-in
contract is about the non-test surface, and plan 03's drift check diffs only `binding.go`,
`plugin.go`, and `go.mod` against the module-cache copy, so this file is additive and does not
break the D-03 mirror.

Do not assert an exact byte length for `Info()`. The threshold form (`> 100000`) is what survives a
legitimate grammar change in phase 2 while still catching a regression to forest's placeholder.
  </action>
  <verify>
    <automated>cd forest-shim/cobol && go test ./... -v</automated>
  </verify>
  <acceptance_criteria>
    - `forest-shim/cobol/smoke_test.go` contains `package cobol` and `func TestShimExposesForestSurface(` and `func TestShimEmbedsThisForksGrammar(`
    - `cd forest-shim/cobol && go test ./... -v` exits 0 and its output names both test functions as PASS
    - `forest-shim/cobol/go.mod` still has no `require` block: `grep -c require forest-shim/cobol/go.mod` is 0
    - `ls forest-shim/cobol/go.sum` fails (no checksum file was introduced)
    - `diff -q forest-shim/cobol/binding.go "$(go env GOMODCACHE)/github.com/alexaandru/go-sitter-forest/cobol@v1.9.1/binding.go"` still reports no difference (the test file did not require touching the mirror)
    - `cd ~/repos/mine/GoApps/gortex && go build ./internal/parser/forest/...` exits 0 (the added test file does not leak into the consumer's build)
  </acceptance_criteria>
  <done>A committed test in the shim package fails if the drop-in surface or the embedded grammar ever regresses; `go test ./...` inside the shim is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this fork's `src/` → `forest-shim/cobol/` | Generated build artifacts are copied across a directory boundary and then hand-rewritten (the include fix). Anything wrong here is compiled straight into the consumer. |
| `forest-shim/cobol/` → gortex build | A local directory takes over a module path that Go would otherwise resolve from a checksum-verified registry. Workspace `use` removes the module from `go.sum` verification entirely. |
| local working tree → public GitHub remote | The shim adds ~31 MB of new tracked files; every new tracked path is a new opportunity for proprietary `estate/` content to ride along. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `forest-shim/cobol/` supplanting a checksum-verified module | medium | mitigate | Resolution is via `go.work` (D-05), which is uncommitted, gitignored in the consumer, and reverted by deleting one file. The `go list -m -f '{{.Dir}}'` acceptance criterion makes the substitution explicit and observable rather than silent; `go env GOWORK` is the one-line "is the pipe on" check documented for every later plan. |
| T-01-02 | Spoofing | Shim silently embedding forest's grammar instead of this fork's | high | mitigate | `TestShimEmbedsThisForksGrammar` asserts `len(Info()) > 100000` against forest v1.9.1's 327-byte placeholder, and `cmp -s forest-shim/cobol/grammar.json src/grammar.json` pins the copy. A wrong-source refresh fails the committed test rather than silently reporting a bogus recall number downstream. |
| T-01-03 | Information Disclosure | Proprietary `estate/` source reaching the public fork alongside the new tracked shim paths | high | mitigate | This plan adds only files copied from `src/` and from the module cache; no `estate/` path is read or referenced. `/estate/` remains excluded via `.git/info/exclude`. Enforcement is plan 02's pre-push guard (FORK-02); this plan's contribution is to add no new estate-adjacent path. |
| T-01-SC | Tampering | npm/Go package installs | high | mitigate | No package is installed by this plan: the Go surface is copied from an already-cached module and the C files from this fork's own tree. RESEARCH.md's `## Package Legitimacy Audit` records the phase's single package, `tree-sitter-cli`, as verdict OK and a pre-existing pin; no `[ASSUMED]` or `[SUS]` package exists, so no blocking human checkpoint is required. |
</threat_model>

<verification>
- `cd forest-shim/cobol && go build ./... && go test ./... -v` exits 0.
- From gortex: `go env GOWORK` non-empty, `go list -m -f '{{.Dir}}' github.com/alexaandru/go-sitter-forest/cobol` resolves into this fork, and `go test ./internal/parser/forest/cobolprobe/ -run 'TestErrorCascade|TestHypothesis' -v` exits 0.
- `cd ~/repos/mine/GoApps/gortex && git status --porcelain` shows no tracked-file change.
- Deleting `~/repos/mine/GoApps/gortex/go.work` and re-running `go list -m -f '{{.Dir}}' github.com/alexaandru/go-sitter-forest/cobol` prints a GOMODCACHE path again — the pipe is a one-file switch. Recreate `go.work` afterwards; later plans in this phase need it on.
</verification>

<success_criteria>
- VEND-01: a Go module in this fork wraps this fork's generated parser with forest's file layout and exported surface, and is a drop-in substitute.
- VEND-02: gortex consumes this fork's grammar through a local override and `cobolprobe` runs end-to-end against it with the unmodified grammar.
</success_criteria>

<output>
Create `.planning/phases/01-delivery-pipe-measurement-baseline/01-01-SUMMARY.md` when done.
</output>
