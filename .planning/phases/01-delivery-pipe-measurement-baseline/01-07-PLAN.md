---
phase: 01-delivery-pipe-measurement-baseline
plan: 07
type: execute
wave: 2
depends_on: ["01-06"]
gap_closure: true
files_modified:
  - .githooks/estate-guard.sh
  - .githooks/pre-push
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [FORK-02]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A push whose range adds a file under an `estate/` path in one commit and removes it in a later commit is REJECTED — the guard walks every commit in the range rather than diffing the range's two endpoint trees, so the ordinary accidental commit-then-revert sequence that transmits the blob in the pushed object closure can no longer produce a green verdict (CR-01)"
    - "An `estate/` path whose bytes contain a non-ASCII sequence or an embedded double-quote character is REJECTED — path enumeration is NUL-delimited end to end and no pathspec is ever re-derived from human-readable, C-quoted diff output (CR-02)"
    - "A commit range the guard cannot resolve produces an explicit failure naming the range, and never check 1's affirmative verdict — the guard fails CLOSED, because a leak control that cannot evaluate its input must block the push rather than affirm that nothing bad was found (CR-03)"
    - "A new-branch push is bounded against the remote-tracking refs of the remote actually being pushed to, forwarded from git's own pre-push argument, and the guard fails closed when it is not told which remote — commits already present on a second remote such as `upstream` are no longer excluded from the scan of a push to the public `origin` (CR-04)"
    - "The guard's header claim that every command which can fail is checked explicitly is TRUE: every git invocation and the temporary-directory creation have their exit status checked, and no failure path reports a passing verdict (IN-08, CR-03)"
    - "`pre-push` runs the self-test on every push, so a future regression in the guard's own checks cannot ship silently, at a per-push cost measured and recorded rather than assumed (WR-13)"
    - "The preserved behaviours still hold: the sequence-area check remains scoped to diff-ADDED lines only so the NIST fixtures under `test/cobol85/src/*.CBL` never false-positive, byte-column extraction stays pinned under `LC_ALL=C`, and check 2 still asserts `/estate/` is declared in `.git/info/exclude` and absent from `.gitignore`"
    - "The full self-test suite that plan 01-06 proved FAILING against the unfixed guard now passes end to end, and the zero-check stand-in is still rejected — the guard was fixed, not the assertions relaxed"
  artifacts:
    - path: ".githooks/estate-guard.sh"
      provides: "A leak guard that walks every commit in the range, enumerates paths NUL-delimited, fails closed on any unresolvable input, and scopes a new-branch scan to the remote being pushed to"
      contains: "diff-tree"
    - path: ".githooks/pre-push"
      provides: "The enforcement point, now forwarding git's remote-name argument to the guard and running the guard's own self-test before trusting it"
      contains: "estate-guard-selftest.sh"
  key_links:
    - from: ".githooks/pre-push"
      to: ".githooks/estate-guard.sh"
      via: "git's remote-name argument forwarded as an explicit --remote option, scoping the new-branch exclusion bound"
      pattern: "--remote"
    - from: ".githooks/pre-push"
      to: ".githooks/estate-guard-selftest.sh"
      via: "the hook runs the guard's self-test before the push is allowed, so a regressed guard cannot report green unchallenged"
      pattern: "estate-guard-selftest.sh"
    - from: "git log over the push range"
      to: "git diff-tree per commit"
      via: "per-commit path enumeration replacing the endpoint-only two-dot diff"
      pattern: "diff-tree"
  prohibitions:
    - statement: "MUST NOT close any self-test case by relaxing, deleting, or narrowing an assertion added in plan 01-06 — every case goes green by fixing the guard. Relaxing an assertion re-creates CR-05 under a different name"
      category: correctness
    - statement: "MUST NOT convert the sequence-area check into a whole-file scan — the NIST fixtures under `test/cobol85/src/*.CBL` are public, upstream-owned, 80-column fixed-format files that legitimately carry a populated sequence area, and a full-file scan would false-positive on roughly a thousand files this fork never touches"
      category: correctness
    - statement: "MUST NOT use `eval`, MUST NOT leave any expansion unquoted in command position, and MUST NOT hand any diff-derived content back to the shell for re-interpretation — the guard processes attacker-influenceable input, since any commit shapes both the paths and the content it reads"
      category: security
    - statement: "MUST NOT report a passing verdict on any path where a git invocation failed — a guard that cannot evaluate must block, never affirm"
      category: security
    - statement: "MUST NOT touch `forest-shim/`, `docs/`, `grammar.js`, `src/`, `test/`, or `.githooks/commit-separability.sh` — the other seven requirements in this phase are verified and delivered"
      category: scope
---

<objective>
Fix the four Critical false-negative paths in the estate-leak guard and wire its self-test into the
enforcement point, turning plan 01-06's recorded red transcript green.

This is the GREEN half of a deliberate red-then-green sequence. Plan 01-06 is the RED half and is a
hard prerequisite: it produced a self-test that fails against the guard as shipped, with the
failure recorded in `01-06-RED-EVIDENCE.md`. That file is this plan's falsifiable target.

Purpose: FORK-02 is reopened because the guard reports green while a proprietary blob can reach a
public remote. The closing condition for this gap is that the guard actually blocks a leak — not
that the tests are green. The tests going green is how we know, and 01-06 is what makes that a
real signal rather than a restatement.
Output: a guard whose three checks hold under commit-range walking, adversarial path spellings,
unresolvable input, and multi-remote pushes; and a `pre-push` hook that re-proves the guard on
every push.

**Every self-test case goes green by fixing the guard.** If a case cannot be made to pass, that is
a finding about the fix, not about the case. Do not edit `.githooks/estate-guard-selftest.sh` in
this plan — it is not in `files_modified`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-delivery-pipe-measurement-baseline/01-VERIFICATION.md
@.planning/phases/01-delivery-pipe-measurement-baseline/01-REVIEW.md
@.planning/phases/01-delivery-pipe-measurement-baseline/01-CONTEXT.md
@.planning/phases/01-delivery-pipe-measurement-baseline/01-PATTERNS.md
@.planning/phases/01-delivery-pipe-measurement-baseline/01-06-RED-EVIDENCE.md
@.planning/phases/01-delivery-pipe-measurement-baseline/01-06-SUMMARY.md
@.githooks/estate-guard.sh
@.githooks/pre-push
@.githooks/estate-guard-selftest.sh
</context>

<constraints>
**Shell house style** (matches `run_nist_cobol85.sh`, `test/check_tests.sh`): no `set -e`, no
`set -u`. Accumulate the failure counter and exit with the aggregate at the end, not on the first
failure. Check `$?` explicitly after every command that can fail. `tee`-style dual reporting via
the existing `report()` helper. Quote every path expansion.

Note the tension 01-REVIEW.md IN-08 raises: the guard's own header (lines 27-31) claims "every
command that can fail is checked explicitly", and that claim is the stated justification for
omitting `set -e`. It is currently untrue at lines 51, 103, 124 and 207. This plan makes the claim
true rather than weakening it.

**Organization secure-coding standard.** No `eval`. No unquoted expansion in command position. No
diff content reaching a shell. The guard reads attacker-influenceable input: any commit shapes both
the paths and the file content it processes. ASVS V5 input validation is the applicable category.

**Data handling.** `estate/` is proprietary production source and this fork is PUBLIC on GitHub.
No `estate/` content may be read, quoted, or reproduced during this work. All exercise of the guard
happens through the self-test's scratch repository under `$(mktemp -d)`.

**Commit separability (D-12, one-way).** `.githooks/*` is fork-local family. Never mix with
`grammar.js`, `src/`, or `test/corpus/*`. Never `git commit -a`.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Walk every commit, enumerate paths NUL-delimited, and fail closed — CR-01, CR-02, CR-03, IN-08</name>
  <files>.githooks/estate-guard.sh</files>

  <precondition>`/bin/bash` exists and reports version 3.2 or later (`/bin/bash --version`), because this task changes the guard's interpreter to bash for NUL-safe reads.</precondition>

  <read_first>
    - `.githooks/estate-guard.sh` lines 1-52 (header, `COBOL_PATH_RE`, `SEQ_AREA_FIRST_BYTE`,
      counters, temp-directory creation) and lines 94-149 (`check_paths_and_seq_area` in full —
      this is the function being rewritten)
    - `01-REVIEW.md` sections CR-01, CR-02, CR-03 and IN-08 — CR-01 and CR-02 each carry a verbatim
      shell fix sketch; use them as the starting shape rather than inventing a third
    - `.githooks/estate-guard-selftest.sh` cases 10, 11, 12 and 13 as written by plan 01-06 — these
      are the assertions this task must satisfy
    - `01-06-RED-EVIDENCE.md` — confirm which of those cases are currently red before starting
  </read_first>

  <behavior>
    - Case 10 (CR-01): a range whose first commit adds a file under an `estate/` path and whose
      second commit removes it must make check 1 report a rejection.
    - Case 11 (CR-02a): an `estate/` path containing a non-ASCII UTF-8 byte sequence must make
      check 1 report a rejection.
    - Case 12 (CR-02b): an `estate/` path containing an embedded double-quote character must make
      check 1 report a rejection.
    - Case 13 (CR-03): a range naming a commit that does not exist must make check 1 report a
      rejection naming the range, and must never emit check 1's affirmative verdict.
    - Cases 5, 8 and 9 must remain green: short-line fixtures, an untouched pre-existing fixture
      with a populated sequence area, and the 72-byte / tab / UTF-8 encoding edge cases.
  </behavior>

  <action>
    Change the guard's interpreter to bash. The enforcement point `.githooks/pre-push` and
    `test/check_tests.sh` already use bash, so this stays inside house style, and a NUL-delimited
    read is the only fully-correct way to consume the path list that CR-02 requires. Update the
    header's mode documentation accordingly.

    Check the temporary-directory creation. If it fails, or the resulting directory is not present
    and writable, report a failure line and exit non-zero immediately — every other check writes
    into that directory, so a guard without it cannot evaluate anything and must block the push
    rather than continue. This is the line 51 half of IN-08.

    Rewrite the path enumeration inside `check_paths_and_seq_area` so it walks every commit in the
    range instead of comparing the range's two endpoint trees. For the staged mode keep a single
    cached diff. For a commit range, enumerate the commits with `git log` producing bare hashes,
    then for each commit enumerate its touched paths with `git diff-tree` in recursive mode, with
    the merge-decomposing flag so a merge commit yields paths instead of nothing, with rename
    detection disabled so a rename out of an `estate/` path still surfaces the source path, and
    with NUL-delimited output so no path is ever C-quoted. Accumulate into a file under the guard's
    temporary directory. Quote every expansion of the diff-spec variable — it is currently bare at
    the check 1 invocation and that is an unquoted expansion in command position.

    Check the exit status of every git invocation in that walk. If the commit enumeration fails, or
    any per-commit path enumeration fails, report a check 1 failure whose text names the range that
    could not be evaluated and includes the captured standard error, increment the failure counter,
    and return without emitting check 1's affirmative verdict. There must be no code path on which
    a failed git invocation is followed by a passing report.

    Feed check 1 from the accumulated NUL-delimited list, translating NUL to newline only at the
    point of the anchored match, and report every matching path in the failure line as it does
    today.

    Feed check 3 from the same accumulated list rather than re-deriving a pathspec from
    human-readable diff output. Read the list NUL-delimited so paths containing spaces, quotes or
    non-ASCII bytes survive intact. For each path matching the COBOL path pattern, take the
    per-commit patch for that commit and that path only, passing the path after the double-dash
    separator and instructing git to treat pathspecs literally so no byte in the path can be
    interpreted as pathspec magic. Check the exit status of each of those invocations; a per-file
    diff that fails is a check 3 failure naming the path, never a silent skip.

    Leave the sequence-area awk program itself completely unchanged: the added-lines-only filter,
    the `LC_ALL=C` byte pinning, the offset constant pinning content byte 73 to raw diff-line byte
    74, and the short-line exemption are all correct and are load-bearing for the NIST
    false-positive case. Only the source of the patch text feeding it changes.

    Once every git invocation in the file has its status checked, the header's claim about explicit
    checking becomes true. Leave that claim in place — it is now accurate.
  </action>

  <reversibility rating="reversible">
    Interpreter change and enumeration rewrite are confined to one tracked file with no persisted
    state; `git revert` of this commit restores the prior guard exactly.
  </reversibility>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; bash .githooks/estate-guard-selftest.sh &gt; /tmp/gsd-t1.txt 2&gt;&amp;1; test "$(grep -Ec '^Case (10|11|12|13) .*: PASS' /tmp/gsd-t1.txt)" -eq 4 &amp;&amp; test "$(grep -Ec '^Case [5-9] .*: PASS' /tmp/gsd-t1.txt)" -eq 5 &amp;&amp; echo T1-GREEN</automated>
  </verify>

  <acceptance_criteria>
    - Cases 10, 11, 12 and 13 now pass:
      `bash .githooks/estate-guard-selftest.sh 2>&1 | grep -Ec '^Case (10|11|12|13) .*: PASS'` is 4.
    - Cases 5-9 still pass:
      `bash .githooks/estate-guard-selftest.sh 2>&1 | grep -Ec '^Case [5-9] .*: PASS'` is 5.
    - The endpoint-only enumeration is gone and the per-commit walk is present:
      `grep -c 'diff-tree' .githooks/estate-guard.sh` is at least 2, and
      `grep -c 'name-only \$DIFF_SPEC' .githooks/estate-guard.sh` is 0.
    - Enumeration is NUL-delimited: `grep -c 'no-renames' .githooks/estate-guard.sh` is at least 2.
    - No git invocation swallows an error other than a genuine boolean existence probe:
      `grep -n '2>/dev/null' .githooks/estate-guard.sh | grep -vc 'rev-parse'` is 0.
    - The interpreter is bash: `head -1 .githooks/estate-guard.sh` is exactly `#!/bin/bash`.
    - The preserved behaviours are intact, checked against the file with comment lines excluded so
      the header prose cannot satisfy the grep by itself:
      `grep -v '^[[:space:]]*#' .githooks/estate-guard.sh | grep -c 'LC_ALL=C'` is at least 2, and
      `grep -v '^[[:space:]]*#' .githooks/estate-guard.sh | grep -c 'SEQ_AREA_FIRST_BYTE'` is at least 2.
    - The zero-check stand-in is still rejected:
      `printf '#!/bin/sh\nexit 3\n' > /tmp/gsd-stub-guard.sh && chmod +x /tmp/gsd-stub-guard.sh && ESTATE_GUARD_PATH=/tmp/gsd-stub-guard.sh bash .githooks/estate-guard-selftest.sh; test $? -ne 0`
      exits 0.
    - No secure-coding regression: `grep -c 'eval' .githooks/estate-guard.sh` is 0.
  </acceptance_criteria>

  <done>
    An add-then-remove commit pair, a non-ASCII path, an embedded-quote path, and an unresolvable
    range are each rejected by the specific check that should reject them. No git failure anywhere
    in the guard produces a passing verdict.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scope the new-branch bound to the remote actually being pushed to, and fail closed without one — CR-04</name>
  <files>.githooks/estate-guard.sh, .githooks/pre-push</files>

  <read_first>
    - `.githooks/estate-guard.sh` lines 151-224 (the argument dispatch and the stdin ref-update
      loop, including the new-branch bound and the silent `continue` at lines 208-210)
    - `.githooks/pre-push` lines 16-35 — the guard invocation, in both the populated-stdin and the
      empty-stdin shapes; git supplies the remote name as the hook's first positional argument and
      its URL as the second, and neither is currently read
    - `.githooks/estate-guard-selftest.sh` cases 14 and 15 as written by plan 01-06
    - `01-REVIEW.md` section CR-04 for the reproduction and the fix sketch
  </read_first>

  <behavior>
    - Case 14 (CR-04): a new-branch push to one remote, where the `estate/`-bearing commit is
      already reachable from a second remote, must make check 1 report a rejection.
    - Case 15 (CR-04b): the stdin new-branch shape with no remote supplied must make the guard fail
      closed with an explicit failure naming the missing remote.
    - The existing `--check-exclude`, `--range` and `--staged` modes must be unaffected — none of
      them needs a remote, and none may start requiring one.
  </behavior>

  <action>
    Replace the single-shot argument dispatch with a parsing loop that accepts an explicit remote
    option carrying the remote name as its value, in any position, alongside the existing mode
    selectors. Preserve the existing behaviour of every mode: an unrecognised argument is still an
    error, the range selector still requires its range argument, and no argument at all still means
    the stdin ref-update mode. Store the remote name in a variable used only by the new-branch
    branch of the stdin loop.

    In the new-branch branch, if no remote name was supplied, report a check 1 failure stating that
    the guard cannot determine which remote it is scanning against, increment the failure counter,
    and move to the next ref-update line. Failing closed is required: without a remote name the
    only available fallback excludes commits reachable from every configured remote, and this
    repository has two, so that fallback is precisely the defect being fixed.

    When a remote name is supplied, bound the new-branch scan by excluding only that remote's
    remote-tracking refs — the exclusion must carry an explicit `=<remote-name>` suffix rather than
    the bare form that means every remote. Check the exit status of that invocation: a failure is a
    check 1 failure naming the remote, not an empty result.

    Distinguish the two empty outcomes that the current code collapses. If the enumeration
    succeeded and returned nothing, that is a definitive answer — nothing in this ref update is new
    to that remote — so emit an explicit report line saying so, count it as a check that ran, and
    continue without incrementing the failure counter. The current silent `continue` emits no line
    and increments no counter, which makes a whole ref update vanish from the report; that must not
    survive this task, because a check that produces no visible result is indistinguishable from a
    check that never ran.

    In `.githooks/pre-push`, forward git's first positional argument to the guard as the remote
    option, in both the populated-stdin and the empty-stdin invocation shapes. Quote it. Add a
    comment recording that git supplies the remote name as the first argument and its URL as the
    second, so a future editor does not drop the forwarding as apparently unused.
  </action>

  <reversibility rating="reversible">
    The remote option is additive to the guard's argument surface and the forwarding is two lines in
    the hook; both files revert together in one commit.
  </reversibility>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; bash .githooks/estate-guard-selftest.sh &gt; /tmp/gsd-t2.txt 2&gt;&amp;1; test "$(grep -Ec '^Case (14|15) .*: PASS' /tmp/gsd-t2.txt)" -eq 2 &amp;&amp; bash .githooks/estate-guard.sh --check-exclude &gt;/dev/null 2&gt;&amp;1 &amp;&amp; echo T2-GREEN</automated>
  </verify>

  <acceptance_criteria>
    - Cases 14 and 15 now pass:
      `bash .githooks/estate-guard-selftest.sh 2>&1 | grep -Ec '^Case (14|15) .*: PASS'` is 2.
    - The bare every-remote exclusion is gone:
      `grep -Ec -- '--not --remotes[^=]' .githooks/estate-guard.sh` is 0.
    - The scoped form is present:
      `grep -Ec -- '--remotes=' .githooks/estate-guard.sh` is at least 1.
    - The hook forwards git's remote-name argument:
      `grep -Ec -- '--remote "\$1"' .githooks/pre-push` is at least 2 (both invocation shapes).
    - The silent skip is gone — the guard emits a report line on the nothing-new path, checked with
      comment lines excluded:
      `grep -v '^[[:space:]]*#' .githooks/estate-guard.sh | grep -Ec 'report .*new to remote'` is at least 1.
    - Pre-existing modes are unaffected and still exit 0 on the real repository:
      `bash .githooks/estate-guard.sh --check-exclude; test $? -eq 0` exits 0, and
      `bash .githooks/estate-guard.sh --range HEAD~1..HEAD; test $? -eq 0` exits 0.
    - `bash -n .githooks/estate-guard.sh && bash -n .githooks/pre-push` exits 0.
  </acceptance_criteria>

  <done>
    A new-branch push to the public remote is scanned relative to that remote alone, commits sitting
    on `upstream` are no longer excluded from it, and a guard that was not told which remote it is
    protecting blocks the push instead of guessing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run the self-test from pre-push with its per-push cost measured and recorded, and take the green gate — WR-13</name>
  <files>.githooks/pre-push, .planning/REQUIREMENTS.md</files>
  <!-- planner-region-allow: estate-guard-selftest.sh -->
  <!-- The only assertion on this literal in this task is POSITIVE (the hook must invoke the
       self-test). The zero-count criteria below target runtime output under /tmp and this
       repository's commit history, never .githooks/pre-push. -->

  <read_first>
    - `.githooks/pre-push` in full, particularly the header's documented `--no-verify` residual
      limitation and the existing failure accumulation into a single exit status
    - `.planning/phases/01-delivery-pipe-measurement-baseline/01-06-SUMMARY.md` — the measured
      wall-clock runtime of the strengthened suite, recorded there for exactly this decision
    - `01-REVIEW.md` section WR-13 for the fix sketch and its gating note
  </read_first>

  <action>
    Measure the strengthened suite's wall-clock runtime on this machine before deciding anything.
    The pre-strengthening suite measured 1.22 seconds for nine cases plus two real-repository
    checks; six cases were added, so record the current number rather than reusing that one.

    The tradeoff, to be recorded explicitly in the hook's header and in the SUMMARY, not left
    implicit: running the suite on every push costs the measured seconds locally, against a network
    push that costs seconds anyway, and buys the property that a regression in the guard's own
    checks cannot ship — which is the failure mode that produced this entire gap. The cheaper
    alternatives considered and their reason for rejection: a documented manual cadence relies on a
    human remembering, which is what already failed here; and a CI check fires only after the push
    has reached GitHub, which is detection rather than prevention and is the exact reasoning D-17
    used to choose `pre-push` over CI in the first place.

    Wire the suite into the hook accordingly. Run it before the guard, so a regressed guard is
    caught before its verdict on this push is trusted. Capture its output to a temporary file, check
    its exit status, and on failure print a line stating plainly that the leak guard is not
    trustworthy, dump the captured output, and set the hook's failure flag. Remove the temporary
    file on every path. Extend the hook's existing trap rather than installing a second one that
    would displace it.

    Provide one documented environment-variable escape hatch for the genuine emergency, named in
    the header alongside the existing `--no-verify` residual limitation, and state in that same
    comment that setting it disables the only automated evidence that the guard has teeth. Record
    the measured runtime in the comment so a future reader can re-evaluate the tradeoff against a
    number rather than an impression.

    Then take the green gate. Run the full suite and require it to pass end to end. Run the
    zero-check stand-in and require it to still be rejected. Confirm the working tree is clean and
    that nothing `estate/`-shaped exists anywhere in this repository's index or history. Record all
    of it in the SUMMARY, alongside the same six-defect table from `01-06-RED-EVIDENCE.md` now
    showing each case green, so the red-to-green transition is legible in one place.

    Update `.planning/REQUIREMENTS.md` to restore FORK-02's completion mark, with a one-line note
    citing this plan and the closure evidence. That mark was reverted when the requirement was
    reopened, and restoring it is part of closing the gap.
  </action>

  <reversibility rating="reversible">
    The self-test invocation is an additive block in the hook behind a documented escape hatch;
    removing the block restores the prior push cost exactly.
  </reversibility>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; bash .githooks/estate-guard-selftest.sh &gt; /tmp/gsd-green.txt 2&gt;&amp;1; test $? -eq 0 &amp;&amp; test "$(grep -c 'estate-guard-selftest.sh' .githooks/pre-push)" -ge 1 &amp;&amp; echo GREEN-GATE-HELD</automated>
  </verify>

  <acceptance_criteria>
    - Green gate: `bash .githooks/estate-guard-selftest.sh > /tmp/gsd-green.txt 2>&1; test $? -eq 0`
      exits 0. The whole suite passes.
    - Every case is green and none was dropped:
      `grep -Ec '^Case [0-9]+ .*: PASS' /tmp/gsd-green.txt` is at least 15, and
      `grep -Ec '^Case [0-9]+ .*: FAIL' /tmp/gsd-green.txt` is 0.
    - The stand-in is still rejected, proving the suite was not relaxed:
      `printf '#!/bin/sh\nexit 3\n' > /tmp/gsd-stub-guard.sh && chmod +x /tmp/gsd-stub-guard.sh && ESTATE_GUARD_PATH=/tmp/gsd-stub-guard.sh bash .githooks/estate-guard-selftest.sh; test $? -ne 0`
      exits 0.
    - The self-test is wired in: `grep -c 'estate-guard-selftest.sh' .githooks/pre-push` is at
      least 1.
    - The measured cost is recorded as a number in the hook:
      `grep -Ec '[0-9]+(\.[0-9]+)? ?s(ec|econds)?' .githooks/pre-push` is at least 1.
    - The escape hatch is documented rather than silent:
      `grep -ci 'skip' .githooks/pre-push` is at least 1.
    - `bash -n .githooks/pre-push` exits 0 and `.githooks/pre-push` is executable
      (`test -x .githooks/pre-push`).
    - End-to-end hook exercise against the live repository, without pushing anything:
      `printf 'refs/heads/main %s refs/heads/main %s\n' "$(git rev-parse HEAD)" "$(git rev-parse HEAD~1)" | bash .githooks/pre-push origin "$(git remote get-url origin)"; test $? -eq 0`
      exits 0.
    - FORK-02 is marked complete again:
      `grep -c '\[x\] \*\*FORK-02\*\*' .planning/REQUIREMENTS.md` is 1.
    - Check 2's exclude-versus-`.gitignore` assertion is untouched at source level, not merely
      implied by cases 5-9 passing. Asserted on CONTENT, not on a commit-relative diff range:
      a `HEAD~N` base would silently mis-resolve whenever a task needs a follow-up fix-up commit
      (plans 01-01 and 01-03 each produced 4 commits for 2 and 3 tasks respectively during the
      first execution run), and a wrong-but-computable diff can still show 0 matches and pass.
      All four of these must hold against the post-fix `.githooks/estate-guard.sh`:
      `grep -cF 'git rev-parse --git-path info/exclude' .githooks/estate-guard.sh` is 1;
      `grep -cF 'GITIGNORE_FILE="$TOPLEVEL/.gitignore"' .githooks/estate-guard.sh` is 1;
      `grep -cF 'CHECK 2 (exclusion posture): PASS - /estate/ declared in .git/info/exclude, absent from .gitignore' .githooks/estate-guard.sh` is 1;
      `grep -cF 'CHECK 2 (exclusion posture): FAIL - /estate/ must be declared in .git/info/exclude' .githooks/estate-guard.sh` is 1.
      (`-F` is load-bearing: without it BSD/macOS grep reads the `$` in `$TOPLEVEL` as an anchor
      and the pattern silently matches nothing, so the criterion would fail against a correct
      implementation.)
    - Nothing `estate/`-shaped reached this repository:
      `git log --all --diff-filter=A --name-only --pretty=format: | grep -c '^estate/'` is 0.
  </acceptance_criteria>

  <done>
    Every push re-proves the guard before trusting it, at a cost recorded as a measured number with
    the rejected alternatives named. The suite that plan 01-06 proved failing now passes, and the
    zero-check stand-in is still rejected — the guard was fixed, not the test.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local git object store → public remote | `git push` crosses here. This is THE boundary the guard exists to hold: `estate/` is proprietary production source and this fork is public on GitHub |
| commit contents → the guard's shell | Every path string and every diff line the guard reads is attacker-influenceable, because any commit chooses them. ASVS V5 input validation |
| git's pre-push arguments → the guard's scan bound | The remote name determines which commits are treated as already-published and therefore excluded from scanning |
| the guard's verdict → the developer's trust | A green verdict on an unevaluable input is worse than no guard, because it converts uncertainty into false assurance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07-01 | Information Disclosure | `check_paths_and_seq_area` endpoint-only range comparison | critical | mitigate | Task 1: per-commit walk via commit enumeration plus per-commit recursive tree diff with merge decomposition and rename detection disabled, so an add-then-remove pair inside one push is seen (CR-01). Regression-pinned by self-test case 10 |
| T-01-07-02 | Information Disclosure | check 1 anchored path match against C-quoted output | high | mitigate | Task 1: NUL-delimited enumeration end to end; no pathspec re-derived from human-readable diff output; per-file diffs use literal pathspecs (CR-02). Pinned by cases 11 and 12 |
| T-01-07-03 | Denial of control / Information Disclosure | git invocations with discarded exit status | high | mitigate | Task 1: every git invocation and the temporary-directory creation have their status checked; any failure emits a failure line and returns without the affirmative verdict — the guard fails closed (CR-03, IN-08). Pinned by case 13 |
| T-01-07-04 | Information Disclosure | new-branch scan bound excluding every configured remote | high | mitigate | Task 2: exclusion scoped to the remote forwarded from git's first pre-push argument; absent remote fails closed; the nothing-new outcome emits a visible report line instead of vanishing (CR-04). Pinned by cases 14 and 15 |
| T-01-07-05 | Spoofing (of evidence) | a future regression in the guard's checks | high | mitigate | Task 3: `pre-push` runs the self-test before trusting the guard's verdict, so a regressed guard is caught at the enforcement point rather than when a human remembers to run it (WR-13) |
| T-01-07-06 | Tampering / Elevation | diff-derived path and content strings reaching the shell | medium | mitigate | Every expansion quoted, including the diff-spec variable that is currently bare; no `eval`; literal pathspecs so no path byte is interpreted as pathspec magic; diff content is consumed only by awk, never re-interpreted as a command. Asserted by a zero-count `eval` grep |
| T-01-07-07 | Repudiation | `git push --no-verify` bypasses all client-side hooks | medium | accept | By git's own design; no server-side backstop is in scope for this phase (D-17, and already documented in the hook header and `docs/vendoring.md`). Task 3 keeps the self-test as the remaining integrity signal and documents its escape hatch beside this one so both bypasses are visible in one place |
| T-01-07-08 | Denial of Service | per-push cost of running the self-test | low | accept | Measured, not assumed: the pre-strengthening suite ran in 1.22 seconds; Task 3 re-measures and records the current number in the hook header, against a network push that already costs seconds. Escape hatch documented for the emergency case |
| T-01-07-09 | Tampering | package-manager installs | low | accept | No `npm`, `pip`, or `cargo` install task exists in this plan — nothing is added to any dependency manifest, so the package-legitimacy gate has no applicable surface |
</threat_model>

<artifacts_this_phase_produces>
| Artifact | Status | Produced by |
|---|---|---|
| `.githooks/estate-guard.sh` | modified — per-commit walk, NUL-delimited enumeration, fail-closed error handling, remote-scoped new-branch bound | Tasks 1, 2 |
| `.githooks/pre-push` | modified — forwards git's remote-name argument, runs the self-test before trusting the guard | Tasks 2, 3 |
| `.planning/REQUIREMENTS.md` | modified — FORK-02 completion mark restored with a citation to this plan's closure evidence | Task 3 |
| `.planning/phases/01-delivery-pipe-measurement-baseline/01-07-SUMMARY.md` | created — the red-to-green table and the measured per-push cost | plan close-out |
</artifacts_this_phase_produces>

<verification>
1. Full suite green: `bash .githooks/estate-guard-selftest.sh` exits 0, with at least 15 cases
   reporting a pass and zero reporting a failure.
2. Discrimination preserved: the zero-check stand-in run still exits non-zero. The guard was fixed,
   not the assertions.
3. The four Critical defects are gone from the source: no endpoint-only range enumeration, no
   swallowed git error outside a boolean existence probe, no bare every-remote exclusion, no
   unquoted diff-spec expansion.
4. The preserved behaviours hold: cases 5-9 green, `LC_ALL=C` and the sequence-area offset constant
   still present outside comments, check 2's exclude-versus-gitignore assertion unchanged.
5. The hook is exercised end to end against the live repository with a synthetic ref-update line
   and exits 0.
6. Scope discipline: `git diff --name-only` across this plan's commits touches only
   `.githooks/estate-guard.sh`, `.githooks/pre-push`, `.planning/REQUIREMENTS.md`, and this plan's
   SUMMARY. Nothing under `forest-shim/`, `docs/`, `grammar.js`, `src/`, or `test/`.
7. Nothing `estate/`-shaped exists in this repository's index or history.
</verification>

<success_criteria>
- The guard rejects an add-then-remove commit pair inside one push.
- The guard rejects an `estate/` path spelled with a non-ASCII byte or an embedded double quote.
- The guard fails closed on a range it cannot resolve, and on a new-branch push where it was not
  told which remote it is protecting.
- A new-branch push is bounded against the target remote alone, not every configured remote.
- `pre-push` re-proves the guard on every push, at a cost recorded as a measured number with the
  rejected alternatives named.
- The full self-test passes, the stand-in is still rejected, and cases 5-9 and the added-lines-only
  scoping are unchanged.
- FORK-02 is closed on evidence that the guard blocks a leak, not on evidence that tests are green.
</success_criteria>

<output>
Create `.planning/phases/01-delivery-pipe-measurement-baseline/01-07-SUMMARY.md` when done.
Record in it: the red-to-green table (one row per defect ID, the case that pins it, its state in
`01-06-RED-EVIDENCE.md`, and its state now), the measured per-push self-test cost with the
alternatives considered and rejected, and the stand-in rejection transcript.
</output>
