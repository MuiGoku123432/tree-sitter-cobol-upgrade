---
phase: 01-delivery-pipe-measurement-baseline
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-03"]
files_modified:
  - docs/vendoring.md
autonomous: true
requirements: [VEND-03]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The tree-sitter-cli version question is answered with evidence rather than assumption: the CLI that generates `parser.c` produces a parser gortex's go-tree-sitter v0.25.0 loads, proven by running cobolprobe against it (VEND-03, OQ-2)"
    - "The effect of the 0.24.5 to 0.25.3 bump on upstream's 13 corpus fixtures is measured and recorded as pass/fail, and its effect on the NIST COBOL-85 suite is recorded against the 11-entry skip list (VEND-03, D-11)"
    - "Upstream's `dependabot/npm_and_yarn/tree-sitter-cli-0.25.3` branch was checked for existing evidence FIRST, after a fresh `git fetch upstream`, and the result is recorded — a stale ref count is untrustworthy by default (VEND-03)"
    - "The 0.25.3 measurement leaves no trace: the `^0.24.5` pin is intact, the working tree is clean, and no extra worktree remains (D-10 — measure, record, revert, do not adopt)"
    - "`docs/vendoring.md` states the fork-vs-forest comparison precisely as ABI-version compatible, not table-identical: LANGUAGE_VERSION, STATE_COUNT and LARGE_STATE_COUNT match byte-for-byte while SYMBOL_COUNT and TOKEN_COUNT do not (1215/585 in this fork, 1153/523 in forest v1.9.1) — so no future reader treats the two grammars as semantically the same"
    - "`docs/vendoring.md` records that the local override is implemented as a `go.work` workspace member rather than the `go.mod` replace directive REQUIREMENTS.md VEND-02 names, and why, so the requirement text and the implementation do not silently diverge (D-05)"
    - "`docs/vendoring.md` records the client-side hook bypass as a known limitation rather than leaving it silently assumed away"
    - "`docs/vendoring.md` opens with a plain-language explanation of the four-way relationship — upstream grammar, the third-party photocopy gortex imports, gortex itself, and this fork — before any command or table"
  artifacts:
    - path: "docs/vendoring.md"
      provides: "The VEND-03 CLI pass/fail record and the VEND-04 refresh evidence, outside `.planning/` so milestone archival cannot strand it (D-08)"
      contains: "core.hooksPath"
  key_links:
    - from: "docs/vendoring.md"
      to: "forest-shim/refresh.sh"
      via: "documents the sequence and its knobs; the doc is what phases 2-4 read before running it"
      pattern: "refresh.sh"
    - from: "docs/vendoring.md"
      to: ".githooks/pre-push"
      via: "documents the one-line install and the bypass limitation"
      pattern: "core.hooksPath"
---

<objective>
Answer OQ-2 with evidence and write the fork's vendoring record.

Purpose: VEND-03 is the only open question this phase closes by measurement. gortex runs
`go-tree-sitter v0.25.0` while this fork generates with `tree-sitter-cli ^0.24.5`, and a CLI bump
would edit upstream's own build config — so the effect has to be measured before it can be dismissed.
Upstream's own dependabot branch carries zero corpus evidence (a two-file manifest bump with no
regenerated parser), and upstream CI never runs `tree-sitter test` at all, so there is nothing to
inherit. D-10 pre-decides the outcome — pin `^0.24.5`, do not adopt — but the *measurement* is still
required, and it is what makes the decision defensible rather than assumed.

Output: an isolated, reverted 0.25.3 measurement and `docs/vendoring.md`.

Transient, outside `files_modified`: a detached `git worktree` sibling directory, and a temporary
re-point of gortex's uncommitted `go.work`. Both are restored before the plan ends.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-delivery-pipe-measurement-baseline/01-CONTEXT.md
@.planning/phases/01-delivery-pipe-measurement-baseline/01-RESEARCH.md
@.planning/phases/01-delivery-pipe-measurement-baseline/01-PATTERNS.md
@.planning/phases/01-delivery-pipe-measurement-baseline/01-03-SUMMARY.md
@docs/spec/SPEC-001-nodes-for-edges.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols and files created by Phase 1. **Bold** entries are created by this plan.

**Files — the vendoring record (this plan):**
- **`docs/vendoring.md`**

**Written by the planner alongside this plan set:**
- `.planning/phases/01-delivery-pipe-measurement-baseline/COVERAGE.md` — the no-external-API
  declaration required by the seal-time API-coverage gate

**Files — other plans in this phase:**
- `forest-shim/cobol/{go.mod,binding.go,plugin.go,alloc.h,array.h,sample.scm,_keep.scm,parser.c,parser.h,scanner.c,grammar.json,smoke_test.go}` and the Go symbols `GetLanguage`, `GetQuery`,
  `Info`, `NvimFirst`, `NativeFirst`, `NvimOnly`, `NativeOnly`, `TestShimExposesForestSurface`,
  `TestShimEmbedsThisForksGrammar` (plan 01)
- `.githooks/pre-push`, `.githooks/estate-guard.sh`, `.githooks/estate-guard-selftest.sh`,
  `.githooks/commit-separability.sh` and their `COBOL_PATH_RE`, `SEQ_AREA_FIRST_BYTE`,
  `GRAMMAR_PATH_RE`, `FORKLOCAL_PATH_RE` variables (plan 02)
- `forest-shim/refresh.sh` with `TOP_DIR`, `SHIM_DIR`, `TREE_SITTER`, `FOREST_CACHE`, `REFRESH_LOG`,
  `REFRESH_SKIP_INSTALL`, `REFRESH_SKIP_GENERATE`, `REFRESH_ALLOW_DRIFT`; `forest-shim/refresh.log`
  (plan 03)
- `forest-shim/pipe-probe.sh` with `GORTEX_DIR`, `PROBE_CAPTURE_DIR`, `PROBE_ABORT_AFTER` (plan 03)
- `docs/baseline.md` (plan 05)
- `~/repos/mine/GoApps/gortex/go.work` (uncommitted, gitignored by the consumer)
</artifacts_this_phase_produces>

<flagged_assumptions>
## Edge Coverage — unresolved rows (flagged assumptions, not dropped)

| Requirement | Probe | Why it stays unresolved |
|---|---|---|
| VEND-03 | unclassified — review manually | "Is the toolchain version question answered with evidence" is a one-time measurement, not a data-shape edge with adjacency / empty / ordering / encoding cases. The probe's taxonomy has no purchase on it. The measurement's own gates — 13 corpus fixtures, the 11-entry NIST skip list, and a live ABI load through cobolprobe — are carried as explicit `must_haves.truths` instead. No further edge is claimed. |
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Measure tree-sitter-cli 0.25.3 in isolation, record, and revert</name>
  <precondition>The network is reachable (npm must fetch tree-sitter-cli 0.25.3), the `upstream` remote is configured, and `git worktree list` shows only the main worktree.</precondition>
  <files>docs/vendoring.md</files>
  <read_first>
    - `.planning/phases/01-delivery-pipe-measurement-baseline/01-CONTEXT.md` — D-10 (pin, measure in isolation, do not adopt), D-11 (the evidence set is corpus + NIST + ABI load, explicitly NOT a full recall run), and the Claude's Discretion note that the isolation mechanism is the planner's choice
    - `.planning/phases/01-delivery-pipe-measurement-baseline/01-RESEARCH.md` — section "tree-sitter-cli version reality", which records the exact dependabot diff shape and the fork-vs-forest `#define` comparison
    - `.planning/STATE.md` — the "stale-ref trap" hazard: a left-right count is only as truthful as the last `git fetch upstream`
    - `run_nist_cobol85.sh` — note it assigns `TREE_SITTER` unconditionally from `node_modules/.bin/`, so the CLI under test must be the one installed there; an env override will not reach it
    - `skip_tests.txt` — the 11-entry gate (11, not 12)
    - `forest-shim/refresh.sh` — its `REFRESH_SKIP_INSTALL` and `REFRESH_SKIP_GENERATE` knobs, used to build a shim from the spike's generated output without reinstalling the pinned CLI
    - `src/parser.c` lines 16-18 — the committed `LANGUAGE_VERSION` / `STATE_COUNT` / `LARGE_STATE_COUNT` values the spike output is compared against
  </read_first>
  <action>
**Step 0 — check upstream for existing evidence first, as VEND-03 literally requires.**
`git fetch upstream`, then `git diff --stat upstream/main upstream/dependabot/npm_and_yarn/tree-sitter-cli-0.25.3`.
Record the output verbatim. Re-fetching first is not optional: a left-right count is only as truthful
as the last fetch, and this project has already been burned by a ref that read "0 behind" while
1,304 commits behind. Also record `git log -1 --format=%cs upstream/main`. If the diff is still the
two-file manifest bump with no regenerated parser, there is no corpus evidence to inherit and the
measurement below must be performed; say so explicitly rather than implying it.

**Step 1 — isolate.** `git worktree add --detach ../ts-0253-spike HEAD`. A detached worktree creates
no branch, shares the object database, and is removed in one command. The proprietary working copy
is untracked and therefore does not appear in the worktree at all. Do every remaining step of this
measurement inside that directory; the main working tree must not be touched.

**Step 2 — baseline inside the worktree.** `npm ci`, then `node_modules/.bin/tree-sitter test` and
`sh run_nist_cobol85.sh`. Record both results. This gives the pass/fail record a same-worktree
control rather than one borrowed from a different run.

**Step 3 — bump, in the worktree only.** `npm install --no-save tree-sitter-cli@0.25.3`, so
`node_modules/.bin/tree-sitter` becomes 0.25.3 and both `run_nist_cobol85.sh` and `tree-sitter test`
exercise it without any script edit. Confirm with `node_modules/.bin/tree-sitter --version`. Then
check `git status --porcelain package.json package-lock.json` inside the worktree; if npm wrote to
either despite the flag, restore them with `git checkout --` and record that it happened.

**Step 4 — regenerate and measure.** `node_modules/.bin/tree-sitter generate`, then
`node_modules/.bin/tree-sitter test` against the 13 corpus fixtures and `sh run_nist_cobol85.sh`
against the NIST suite. Record each as pass/fail with counts. The NIST gate is: no failure beyond
the **11** entries in `skip_tests.txt` — 11, not 12; the SPEC's original 12 was verified wrong, and
inheriting it would silently permit one new regression.

**Step 5 — record the generated-output comparison.** From the worktree's freshly generated
`src/parser.c`, capture `LANGUAGE_VERSION`, `STATE_COUNT`, `LARGE_STATE_COUNT`, `SYMBOL_COUNT` and
`TOKEN_COUNT`, and compare them against the committed values in the main tree (14 / 7745 / 5095 /
1215 / 585). Whether the 0.25.3 CLI changes the ABI version is the load-bearing number.

**Step 6 — the ABI load check.** Inside the worktree, build a shim from the 0.25.3-generated output:
`REFRESH_SKIP_INSTALL=1 REFRESH_SKIP_GENERATE=1 bash forest-shim/refresh.sh` (skip the install so the
0.25.3 CLI stays in place, skip generate because step 4 already ran it). Then in
`~/repos/mine/GoApps/gortex`: copy the existing `go.work` aside, `go work init .` pointed at the
**worktree's** `forest-shim/cobol`, and run
`go test ./internal/parser/forest/cobolprobe/ -run 'TestErrorCascade|TestHypothesis' -v`. Passing
proves the 0.25.3-generated `parser.c` loads under gortex's go-tree-sitter v0.25.0. Immediately
restore the saved `go.work` and re-run the same test to confirm the main shim is back in the path.

Do **not** run `TestCobolParseRate` or `TestNeutralizedParseRate` here. D-11 excludes a full recall
run on 0.25.3 output on purpose: it is a twenty-minute measurement for a bump already decided
against.

**Step 7 — revert completely.** Restore gortex's `go.work` to the main shim.
`git worktree remove --force ../ts-0253-spike`, then `git worktree prune`. Assert `git worktree list`
shows only the main worktree, `git status --porcelain` in the main tree is empty, and `package.json`
still declares the caret range around 0.24.5. Nothing about 0.25.3 is adopted (D-10).

**Step 8 — write the record.** Create `docs/vendoring.md` with only its metadata block and a
`## tree-sitter-cli posture` section holding this measurement as a GitHub-flavoured markdown table
(one row per check: corpus fixtures, NIST, ABI load, generated-output `#define`s), plus the
dependabot-branch finding from step 0. Task 2 writes the remaining sections around it. Cite exact
paths and line numbers the way `docs/spec/SPEC-001-nodes-for-edges.md` does; never quote proprietary
source.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain)" && test "$(git worktree list | wc -l | tr -d ' ')" = "1" && grep -Fq '"tree-sitter-cli": "^0.24.5"' package.json && grep -q 'tree-sitter-cli' docs/vendoring.md && cd ~/repos/mine/GoApps/gortex && go test ./internal/parser/forest/cobolprobe/ -run 'TestErrorCascade|TestHypothesis' -v</automated>
  </verify>
  <acceptance_criteria>
    - `docs/vendoring.md` exists and contains a table row for each of: `tree-sitter test` (13 fixtures), `run_nist_cobol85.sh`, and the ABI load check, each with an explicit PASS or FAIL verdict for 0.25.3
    - `docs/vendoring.md` records the dependabot-branch check with its `git diff --stat` output and the date of `upstream/main`'s last commit
    - `docs/vendoring.md` records the five `#define` values from the 0.25.3-generated `parser.c` alongside the committed 0.24.5 values (14 / 7745 / 5095 / 1215 / 585)
    - `git worktree list` outputs exactly one line (the spike worktree is gone)
    - `git status --porcelain` in the main tree is empty
    - `package.json` still contains `"tree-sitter-cli": "^0.24.5"` and `package-lock.json` still pins `0.24.5`
    - `node_modules/.bin/tree-sitter --version` in the main tree reports 0.24.5
    - `cd ~/repos/mine/GoApps/gortex && go list -m -f '{{.Dir}}' github.com/alexaandru/go-sitter-forest/cobol` prints the MAIN fork's `forest-shim/cobol` path, not a path containing `ts-0253-spike`
    - `cd ~/repos/mine/GoApps/gortex && go test ./internal/parser/forest/cobolprobe/ -run 'TestErrorCascade|TestHypothesis' -v` exits 0
  </acceptance_criteria>
  <done>OQ-2 is answered with recorded evidence for all three clauses D-11 names, the pin is untouched, and no trace of the spike remains.</done>
</task>

<task type="auto">
  <name>Task 2: Write the vendoring record</name>
  <files>docs/vendoring.md</files>
  <read_first>
    - `docs/spec/SPEC-001-nodes-for-edges.md` — the only existing document under `docs/` and therefore the house style: single `#` title, a bold-labelled metadata block directly beneath it, numbered `##` sections, `---` rules between major sections, GitHub-flavoured tables for quantitative data, `> ⚠` blockquotes for load-bearing warnings, short paragraphs, and inline citations naming exact paths
    - `.planning/phases/01-delivery-pipe-measurement-baseline/01-PATTERNS.md` — section "`docs/vendoring.md`, `docs/baseline.md` (documentation)", which extracts those conventions explicitly
    - `.planning/phases/01-delivery-pipe-measurement-baseline/01-CONTEXT.md` — the `<specifics>` note that this document must open with the plain-language explanation of the four-way relationship, not with commands; plus D-05, D-08, D-17
    - `.planning/phases/01-delivery-pipe-measurement-baseline/01-RESEARCH.md` — "Pitfall 5" (the precise ABI wording), "Pitfall 3" (why a workspace member is not a replace directive), "Pitfall 4" (why the hook lives in a tracked directory), and Open Question 1's recommendation to fold the ABI correction into the decision's own wording rather than footnote it
    - `.planning/REQUIREMENTS.md` — VEND-02's wording, which names a `go.mod` replace directive
    - `.planning/phases/01-delivery-pipe-measurement-baseline/01-03-SUMMARY.md` — the two-run refresh evidence and the propagation-probe before/after kind lines to transcribe
    - `forest-shim/refresh.sh` — its steps and env knobs, to document accurately
    - `.githooks/pre-push` — the install command and the bypass limitation to transcribe
  </read_first>
  <action>
Complete `docs/vendoring.md` around the measurement section task 1 wrote. Follow the SPEC-001 house
style throughout: numbered `##` headings, `---` between major sections, tables for quantitative data,
`> ⚠` blockquotes for warnings, and citations naming exact paths and line numbers.

**§1 — What this is.** Open with plain language, before any command or table. Name the four-way
relationship explicitly and do not assume it is self-evident: upstream `yutaro-sakamoto/tree-sitter-cobol`
holds the grammar; `github.com/alexaandru/go-sitter-forest/cobol` is a third party's Go-importable
photocopy of it, re-scraped periodically; gortex imports that photocopy; and this repository is a
fork of upstream whose edits cannot reach gortex through that dependency without both an upstream
merge and a forest re-scrape. State the consequence in one sentence: that is the gap this shim closes
locally.

**§2 — The shim.** The directory, its file list, and the module path line that makes it a drop-in.
Record that the C and JSON files are physically copied and flattened rather than symlinked, because
cgo compiles only files sitting in the package directory and the embed directive refuses to follow
symlinks. Record the include-rewrite gotcha as a `> ⚠` callout naming both syntaxes, because that is
the failure a future maintainer will otherwise rediscover the hard way.

**§3 — Turning the pipe on and off.** The `go work init` command, `go env GOWORK` as the one-line "is
it on" check, and deleting the workspace file as the way back to forest v1.9.1. Then record the
requirement-versus-implementation divergence explicitly: REQUIREMENTS.md VEND-02 says gortex consumes
this fork "through a `go.mod` `replace` directive", and the implementation uses a `go.work` workspace
member instead. Give both reasons — gortex's existing replace directives all point at in-tree paths
while ours would be a relative path outside the repository, breaking every checkout with a different
layout including gortex CI; and a workspace member is removed from checksum-verified resolution
entirely, so there is no `go.sum` entry to keep in sync and nothing that can go stale. State plainly
that the requirement's intent (a local override) is met and only the mechanism differs.

**§4 — The refresh sequence.** What `forest-shim/refresh.sh` does step by step, its overridable
variables and their defaults, and why it is a shell script in the shim directory rather than an entry
in `package.json` (that file is upstream-owned and dependabot keeps moving it, including the very CLI
bump §5 measures). Transcribe the two-run evidence and the propagation-probe before/after node-kind
lines from the plan 03 SUMMARY — that pair is the proof that a grammar edit reaches gortex's parse
output with no upstream merge and no forest regeneration. State that dependencies are installed from
the lockfile only, and why the range-re-resolving form would silently break reproducibility.

**§5 — tree-sitter-cli posture.** Keep task 1's measurement table. Add the fork-versus-forest
comparison and state it precisely: **ABI-version compatible, not table-identical.** LANGUAGE_VERSION
14, STATE_COUNT 7745 and LARGE_STATE_COUNT 5095 match byte-for-byte between this fork's `src/parser.c`
and forest v1.9.1's cached copy, which is what go-tree-sitter checks at load time; but SYMBOL_COUNT
and TOKEN_COUNT do not (1215/585 here, 1153/523 there), and a full-file diff runs to over a million
lines because forest's copy was generated from an older revision of upstream's grammar. Fold this
correction directly into the statement of the posture rather than appending it as a footnote — a
reader must never meet the stronger "identical generated output" claim without the correction beside
it. Close with the decision: the pin stays at the 0.24.x caret range; 0.25.3 was measured and not
adopted.

**§6 — The estate-leak guard.** The three checks, the byte-column definition of the sequence-area
heuristic, why it is scoped to diff-added lines (the public NIST fixtures legitimately carry
populated sequence areas), and the one-line per-clone install `git config core.hooksPath .githooks`.
Explain why the exclusion lives in the repo-local exclude file rather than in the tracked ignore file:
it keeps the upstream diff at zero.

**§7 — Known limitations.** A `> ⚠` callout that a push with verification disabled bypasses any
client-side hook by git's own design, and that no server-side backstop is in scope for this phase.
Also record that `alloc.h` and `array.h` are carried for file-set fidelity but are referenced by
nothing in this fork's generated output today, so their presence proves nothing.

Throughout: cite file paths and line numbers. Quote no proprietary source — file names, counts and
path shapes are fine, contents are not. This file is committed to a public repository.
  </action>
  <verify>
    <automated>test -f docs/vendoring.md && grep -c '^## ' docs/vendoring.md && grep -Fq 'go.work' docs/vendoring.md && grep -Fq 'core.hooksPath' docs/vendoring.md && grep -Fq 'refresh.sh' docs/vendoring.md && bash .githooks/estate-guard-selftest.sh</automated>
  </verify>
  <acceptance_criteria>
    - `docs/vendoring.md` has at least 7 `##` headings and its first `##` heading is numbered `1.`
    - The prose above the first `##` heading, plus section 1, mention all four of: `yutaro-sakamoto`, `go-sitter-forest`, `gortex`, and this fork — the four-way relationship named explicitly before any command
    - `docs/vendoring.md` contains `go work init` and `go env GOWORK` and `core.hooksPath` and `refresh.sh`
    - `docs/vendoring.md` contains the phrase `ABI` and both number pairs `1215` / `585` and `1153` / `523`
    - `docs/vendoring.md` contains `LANGUAGE_VERSION 14` and `STATE_COUNT 7745` and `LARGE_STATE_COUNT 5095`
    - `docs/vendoring.md` explicitly names both `go.work` and the `replace` directive in the same section, recording that VEND-02's stated mechanism differs from the implemented one and why
    - `docs/vendoring.md` contains at least two `> ⚠` blockquote callouts
    - `docs/vendoring.md` contains the two-run refresh evidence and the propagation-probe before/after node-kind lines transcribed from the plan 03 SUMMARY
    - `grep -Ec '(estate/[A-Za-z0-9_]+\.(cpy|CPY|cbl|CBL))' docs/vendoring.md` is 0 — no proprietary file content or specific estate file path is quoted
    - `bash .githooks/estate-guard-selftest.sh` exits 0 after the file is written
    - `git show --stat --name-only --pretty=format: HEAD` for this task's commit lists only paths under `docs/` (separable per D-12)
  </acceptance_criteria>
  <done>A future reader — or a future phase — can turn the pipe on, refresh it, understand the ABI posture precisely, and install the guard, from one document that survives milestone archival.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → spike worktree `node_modules/` | The spike deliberately installs an unpinned newer CLI version outside the lockfile. |
| spike worktree → main working tree | A shared object database and an adjacent directory; a mistake here can leave the pin bumped or the tree dirty. |
| `docs/vendoring.md` → public GitHub | A new tracked prose file is a new place proprietary content could be quoted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Tampering | Silent adoption of tree-sitter-cli 0.25.3 | high | mitigate | The measurement happens in a detached `git worktree` removed at the end; the bump uses the no-save install form; acceptance criteria assert `package.json` still declares the caret range, `package-lock.json` still pins 0.24.5, the main tree's CLI still reports 0.24.5, and `git worktree list` has exactly one entry. |
| T-01-13 | Tampering | gortex left pointed at the spike worktree's shim after the ABI check | high | mitigate | The existing workspace file is copied aside before the re-point and restored immediately after; an acceptance criterion asserts `go list -m -f '{{.Dir}}'` resolves back to the MAIN fork's shim path, and cobolprobe is re-run against it. |
| T-01-14 | Information Disclosure | Proprietary source quoted into a public `docs/` file | high | mitigate | The action forbids quoting proprietary content and permits only file names, counts and path shapes; an acceptance criterion greps for the shape of a specific proprietary source path and requires zero matches; the pre-push guard's self-test is re-run after the file is written. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | Two installs occur, both inside the throwaway worktree: `npm ci` (lockfile-pinned) and a deliberate no-save install of `tree-sitter-cli@0.25.3`. RESEARCH.md's `## Package Legitimacy Audit` records `tree-sitter-cli` (npm, tree-sitter project, very high downloads, source `github.com/tree-sitter/tree-sitter`) as verdict OK; the 0.25.3 spike is the same package at a different version, not a new package. No `[ASSUMED]`, `[SUS]` or `[SLOP]` package is involved, so no blocking human checkpoint is required. |
</threat_model>

<verification>
- `git worktree list` has exactly one entry; `git status --porcelain` is empty; `package.json` still declares the `^0.24.5` range and `node_modules/.bin/tree-sitter --version` reports 0.24.5.
- `cd ~/repos/mine/GoApps/gortex && go list -m -f '{{.Dir}}' github.com/alexaandru/go-sitter-forest/cobol` resolves to the main fork's shim, and cobolprobe's inline-fixture tests pass.
- `docs/vendoring.md` carries the four-way explanation, the measurement table, the precise ABI wording, the `go.work`-versus-`replace` divergence note, the refresh evidence, and both `> ⚠` limitation callouts.
- `bash .githooks/estate-guard-selftest.sh` exits 0.
</verification>

<success_criteria>
- VEND-03: the tree-sitter-cli version question is answered with evidence — the CLI that generates `parser.c` yields a parser gortex's go-tree-sitter v0.25.0 loads, and the effect of the 0.24.5 to 0.25.3 bump on the 13 corpus fixtures is recorded as pass/fail, with upstream's dependabot branch checked for existing evidence first.
</success_criteria>

<output>
Create `.planning/phases/01-delivery-pipe-measurement-baseline/01-04-SUMMARY.md` when done.
</output>
