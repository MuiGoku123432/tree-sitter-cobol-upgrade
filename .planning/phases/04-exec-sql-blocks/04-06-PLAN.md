---
phase: 04-exec-sql-blocks
plan: 06
type: execute
wave: 5
depends_on: [04-05]
files_modified: [forest-shim/cobol/parser.c, forest-shim/cobol/grammar.json, forest-shim/cobol/sql.scm]
autonomous: true
requirements: [SQL-02, SQL-03, SQL-04, SQL-07]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-17: the complete Phase 4 parser and sql.scm are present in forest-shim/cobol before downstream cascade work begins."
    - "SQL-07/FORK-03: shim materialization is a fork-local commit, separate from grammar-family and gortex commits."
  artifacts:
    - {path: forest-shim/cobol/parser.c, provides: "Phase 4 parser delivered to Go"}
    - {path: forest-shim/cobol/grammar.json, provides: "Vendored SQL grammar metadata"}
    - {path: forest-shim/cobol/sql.scm, provides: "Vendored SQL extraction query"}
  key_links:
    - {from: queries/sql.scm, to: forest-shim/cobol/sql.scm, via: "forest-shim/refresh.sh wildcard copy", pattern: "queries/\\*\\.scm"}
---
<objective>
Refresh and smoke-test the forest shim before any gortex cascade task consumes the Phase 4 parser.

Purpose: resolve the delivery-order dependency and make the downstream precondition executable.
Output: refreshed parser, grammar metadata, and SQL query in forest-shim/cobol.
</objective>
<execution_context>
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/04-exec-sql-blocks/04-CONTEXT.md
@.planning/phases/04-exec-sql-blocks/04-PATTERNS.md
@.planning/phases/04-exec-sql-blocks/04-05-SUMMARY.md
@forest-shim/refresh.sh
@queries/sql.scm
</context>
<tasks>
<task type="tracer">
  <name>Task 1: Materialize the complete SQL parser and query in the shim</name>
  <files>forest-shim/cobol/parser.c, forest-shim/cobol/grammar.json, forest-shim/cobol/sql.scm</files>
  <read_first>
    forest-shim/refresh.sh:91-144 -- PATTERNS generate, parser/grammar copy, and the queries/*.scm wildcard copy
    forest-shim/refresh.sh:172-214 -- PATTERNS Go-surface drift checks and the final smoke build
    forest-shim/cobol/binding.go:25-26 -- PATTERNS the `//go:embed grammar.json *.scm` wildcard that already covers sql.scm
  </read_first>
  <action>Run forest-shim/refresh.sh without editing it. Let the established pipeline regenerate, copy parser/grammar and every query, rewrite flattened includes, enforce Go-surface drift, and smoke-build. Per D-17, require byte identity for sql.scm and SQL node presence in vendored grammar metadata. Keep all generated shim outputs in a fork-local commit separate from grammar-family history per SQL-07/FORK-03.</action>
  <verify><automated>sh forest-shim/refresh.sh &amp;&amp; cmp queries/sql.scm forest-shim/cobol/sql.scm &amp;&amp; grep -q 'exec_sql_statement' forest-shim/cobol/grammar.json &amp;&amp; grep -q 'sql_table_name' forest-shim/cobol/grammar.json</automated></verify>
  <acceptance_criteria>
    - forest-shim/refresh.sh is run unmodified and exits zero.
    - queries/sql.scm and forest-shim/cobol/sql.scm are byte-identical.
    - Vendored grammar metadata contains exec_sql_statement and sql_table_name.
    - The commit contains only the three fork-local shim outputs.
  </acceptance_criteria>
  <done>The Phase 4 parser/query is available through the shim and the existing smoke build is green.</done>
</task>
<task type="auto">
  <name>Task 2: Prove refresh is repeatable and limited to expected shim outputs</name>
  <files>forest-shim/cobol/parser.c, forest-shim/cobol/grammar.json, forest-shim/cobol/sql.scm</files>
  <read_first>
    forest-shim/refresh.sh:111-144 -- PATTERNS the wildcard query copy whose determinism this task re-runs
    forest-shim/refresh.sh:196-214 -- PATTERNS the drift checks on binding.go, plugin.go, and go.mod
  </read_first>
  <action>Run refresh a second time and verify no additional diff appears after the first materialization. Confirm binding.go, plugin.go, go.mod, refresh.sh, and scanner files are untouched. Commit only the three listed shim outputs. FORK-03 is proven at push time by the installed hook; persist no range artifact and audit no commit history here.</action>
  <verify><automated>sh forest-shim/refresh.sh &amp;&amp; git diff --exit-code -- forest-shim/cobol/parser.c forest-shim/cobol/grammar.json forest-shim/cobol/sql.scm &amp;&amp; test -z "$(git diff --name-only -- forest-shim/cobol/binding.go forest-shim/cobol/plugin.go forest-shim/go.mod forest-shim/refresh.sh src/scanner.c)"</automated></verify>
  <done>Refresh is deterministic and the fork-local delivery commit contains only expected shim artifacts.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Grammar repository -> Go shim | Generated parser/query cross into downstream delivery. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-19 | Tampering | shim refresh | high | mitigate | Scripted refresh, byte comparison, repeatability, and smoke build. |
| T-04-SC | Tampering | supply chain | low | accept | Existing lockfile and refresh pipeline only. |
</threat_model>

## Artifacts this phase produces

- Grammar nodes/rules: `exec_sql_statement`, `exec_sql_declare_section`, `exec_sql_include`, `sql_table_name`, `sql_table_alias`, `sql_include_name`, `sql_dynamic_source`, `sql_unparsed_tail`, structured CTE-definition and source-candidate rules, plus private clause helpers.
- Repository paths: `test/corpus/exec_sql.txt`, `queries/sql.scm`, `run_sql_query_capture.sh`, `run_sql_tail_census.sh`, `docs/sql-tail-census.md`, `.githooks/commit-separability.sh`.
- Generated/vendored paths: `src/parser.c`, `src/grammar.json`, `src/node-types.json`, `forest-shim/cobol/parser.c`, `forest-shim/cobol/grammar.json`, `forest-shim/cobol/sql.scm`.
- Cross-repo path under FORK-03: `/Users/e1001547-mbp-it/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go`, committed only in gortex.
<verification>Run refresh twice, byte-compare sql.scm, check SQL nodes, and prove no forbidden shim-surface changes.</verification>
<success_criteria>The refreshed shim is complete, deterministic, smoke-tested, and available before Plan 04-07.</success_criteria>
<output>Create `.planning/phases/04-exec-sql-blocks/04-06-SUMMARY.md` when done.</output>
