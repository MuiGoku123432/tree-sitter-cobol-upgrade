---
phase: 04-exec-sql-blocks
plan: 04
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified: [grammar.js, test/corpus/exec_sql.txt, queries/sql.scm, src/parser.c, src/grammar.json, src/node-types.json, run_sql_query_capture.sh]
autonomous: true
requirements: [SQL-02, SQL-03, SQL-05, SQL-07]
estimate: {tokens: 50000, raw_tokens: 50000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "D-09/D-10/D-11: declaration markers and generic INCLUDE directives parse in DATA DIVISION as siblings of unchanged data descriptions."
    - "D-12/D-17: arbitrary include members have exact direct captures and cannot become table results."
    - "SQL-07/FORK-03: grammar/query changes and fork-local gate changes are delivered in separate path-family commits."
  artifacts:
    - {path: test/corpus/exec_sql.txt, provides: "Exact DATA DIVISION SQL directive trees"}
    - {path: grammar.js, provides: "DATA DIVISION directive integration"}
    - {path: queries/sql.scm, provides: "Compiled direct include capture added after sql_include_name exists"}
    - {path: run_sql_query_capture.sh, provides: "Fork-local exact include capture and negative-table assertions"}
  key_links:
    - {from: "grammar.js record_description_list/working_storage_section", to: "exec_sql_declare_section and exec_sql_include", via: "section-content choice preserving data_description siblings", pattern: "exec_sql_(declare_section|include)"}
    - {from: queries/sql.scm, to: run_sql_query_capture.sh, via: "post-regeneration exact @include capture gate", pattern: "sql_include_name|@include"}
---
<objective>
Expand the proven SQL AST into DATA DIVISION declaration markers and generic includes without hiding ordinary COBOL data items.

Purpose: satisfy D-09 through D-12 while preserving grammar-family and fork-local commit separation.
Output: generated grammar, direct include query role, and a separately committed fork-local exact include gate.
</objective>
<execution_context>
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/04-exec-sql-blocks/04-CONTEXT.md
@.planning/phases/04-exec-sql-blocks/04-PATTERNS.md
@.planning/phases/04-exec-sql-blocks/04-01-SUMMARY.md
@grammar.js
@test/corpus/exec_sql.txt
@queries/sql.scm
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Parse declaration markers around one intact data item</name>
  <files>grammar.js, test/corpus/exec_sql.txt, src/parser.c, src/grammar.json, src/node-types.json</files>
  <read_first>
    grammar.js:872-894 -- PATTERNS working_storage_section and record_description_list content choices
    grammar.js:1221-1231 -- PATTERNS the shared record_description_list used by local storage and linkage
    test/corpus/exec_sql.txt -- the 04-01 fixture file these DATA DIVISION cases extend
  </read_first>
  <behavior>
    - BEGIN and END DECLARE SECTION each produce a separate exec_sql_declare_section marker.
    - An ordinary level item between them remains a normal data_description at the same source position.
    - Markers and following data items remain siblings with required END_EXEC boundaries.
  </behavior>
  <action>Write failing exact trees first. Per D-09, D-10, and D-11, extend working_storage_section and shared record_description_list content at grammar.js:872-894,1221-1231. Add separate marker nodes, never a wrapper around declarations. Preserve ordinary period handling and data_description nodes. Use invented neutral fixtures per D-15, regenerate src artifacts with the pinned CLI, and commit only grammar-family paths per SQL-07/FORK-03.</action>
  <verify><automated>node_modules/tree-sitter-cli/tree-sitter generate &amp;&amp; node_modules/tree-sitter-cli/tree-sitter test -e '^comment$'</automated></verify>
  <acceptance_criteria>
    - Each marker is a separate exec_sql_declare_section node with no wrapper around the declarations between them.
    - Every intervening and adjacent data item remains a data_description at its original source position.
    - Both working_storage_section and the shared record_description_list placements have exact trees.
    - The commit contains only grammar-family paths.
  </acceptance_criteria>
  <done>Both declaration markers are walkable while every intervening and adjacent data item remains intact.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add generic INCLUDE and its compiled direct query role</name>
  <files>grammar.js, test/corpus/exec_sql.txt, queries/sql.scm, src/parser.c, src/grammar.json, src/node-types.json</files>
  <read_first>
    grammar.js:2321-2352 -- PATTERNS dedicated named-node and static precedence convention for sql_include_name
    queries/sql.scm -- the 04-01 direct-capture contract this task extends with @include
  </read_first>
  <behavior>
    - INCLUDE SQLCA and an invented nonstandard member produce exec_sql_include containing sql_include_name.
    - Include placement preserves neighboring data items and procedure statements.
    - The regenerated grammar compiles the direct sql_include_name @include query pattern.
  </behavior>
  <action>Per D-12, accept generic include members rather than an allowlist. Add DATA DIVISION coverage and procedure placement where the grammar permits the directive. Assert exact sql_include_name text and position in corpus trees and preserve exclusion from table-node identities. After regeneration makes sql_include_name available, extend queries/sql.scm per D-17 with the direct `(sql_include_name) @include` pattern and compile it against the corpus. Keep SQL-05 retired, use IBM syntax authority, regenerate all src artifacts, and keep this task commit wholly in the grammar family. Do not modify run_sql_query_capture.sh in this task.</action>
  <verify><automated>node_modules/tree-sitter-cli/tree-sitter generate &amp;&amp; node_modules/tree-sitter-cli/tree-sitter test -e '^comment$' &amp;&amp; node_modules/tree-sitter-cli/tree-sitter query queries/sql.scm test/corpus/exec_sql.txt &gt;/dev/null</automated></verify>
  <done>Generic includes expose sql_include_name in valid placements, the direct include query compiles against regenerated artifacts, and neighboring COBOL remains intact.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Activate exact include assertions in the fork-local gate</name>
  <files>run_sql_query_capture.sh</files>
  <read_first>
    run_sql_query_capture.sh -- the 04-02 gate, its statement-scoped CTE subtraction, and its role-assertion extension points
    queries/sql.scm -- the @include pattern added by Task 2
  </read_first>
  <behavior>
    - Each generic include fixture emits the exact expected @include text, count, and (row, column) start/end positions.
    - Include names produce no @table capture and no physical-table result.
    - A wrong include identity, duplicate, missing capture, shifted position, or table misclassification fails the gate.
  </behavior>
  <action>After Task 2 has added the compiled sql_include_name query role, modify only run_sql_query_capture.sh in a separate fork-local commit per D-12, D-17, and SQL-07/FORK-03. Add explicit expected include capture records for SQLCA and the invented generic member, compare exact text, count, and start/end (row, column) positions as emitted by tree-sitter-cli 0.24.5 query output, and assert that neither include name appears in raw @table output or the statement's physical-table result. Preserve the existing statement-scoped CTE subtraction behavior. Regenerate the parser and compile queries/sql.scm before executing the expanded gate so assertions cannot run against stale generated artifacts.</action>
  <verify><automated>node_modules/tree-sitter-cli/tree-sitter generate &amp;&amp; node_modules/tree-sitter-cli/tree-sitter query queries/sql.scm test/corpus/exec_sql.txt &gt;/dev/null &amp;&amp; sh run_sql_query_capture.sh &amp;&amp; sh -n run_sql_query_capture.sh</automated></verify>
  <done>The separately committed fork-local gate rejects every include text/count/position mismatch and every include-to-table false positive after regeneration and query compilation.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| SQL directives -> DATA grammar | Embedded markers can consume neighboring declarations. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-06 | Tampering | DATA integration | high | mitigate | Exact sibling-marker/data-description fixtures. |
| T-04-07 | Spoofing | include role | medium | mitigate | Named include capture and negative table assertion. |
| T-04-SC | Tampering | supply chain | low | accept | No package installation. |
</threat_model>

## Artifacts this phase produces

- Grammar nodes/rules: `exec_sql_statement`, `exec_sql_declare_section`, `exec_sql_include`, `sql_table_name`, `sql_table_alias`, `sql_include_name`, `sql_dynamic_source`, `sql_unparsed_tail`, structured CTE-definition and source-candidate rules, plus private clause helpers.
- Repository paths: `test/corpus/exec_sql.txt`, `queries/sql.scm`, `run_sql_query_capture.sh`, `run_sql_tail_census.sh`, `docs/sql-tail-census.md`, `.githooks/commit-separability.sh`.
- Generated/vendored paths: `src/parser.c`, `src/grammar.json`, `src/node-types.json`, `forest-shim/cobol/parser.c`, `forest-shim/cobol/grammar.json`, `forest-shim/cobol/sql.scm`.
- Cross-repo path under FORK-03: `/Users/e1001547-mbp-it/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go`, committed only in gortex.
<verification>Generate, run all corpus tests, and run the exact query gate after each slice.</verification>
<success_criteria>DATA DIVISION SQL directives are named siblings and all ordinary data items remain queryable.</success_criteria>
<output>Create `.planning/phases/04-exec-sql-blocks/04-04-SUMMARY.md` when done.</output>
