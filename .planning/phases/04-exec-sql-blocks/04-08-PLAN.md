---
phase: 04-exec-sql-blocks
plan: 08
type: execute
wave: 7
depends_on: [04-02, 04-03, 04-06, 04-07]
files_modified: [run_sql_tail_census.sh, docs/sql-tail-census.md]
autonomous: true
requirements: [SQL-01, SQL-03, SQL-04, SQL-05, SQL-06, SQL-07]
estimate: {tokens: 46000, raw_tokens: 46000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "D-14: an estate-safe census categorizes every sql_unparsed_tail and independently reconciles expected versus captured physical tables."
    - "D-16: DCC and estate differentials, corpus/query, refresh, cascade, and NIST evidence are all green at phase close."
    - "D-16: both differential runs pass an explicit SQL/collision source-TEXT prefilter alongside the node regex, so the ACCEPT-only default selector cannot silently exclude collision-bearing files."
    - "D-16: the estate run is known to enter the estate branch of prepare_accept_paths, and its denominator is the measured SQL population rather than the recorded ACCEPT 3781/1369/2391 constants."
    - "The NIST runner exit status is preserved and zero, and its observed final line is exactly `382 tests. (Success: 371, Fail: 0, Skip: 11)`."
    - "SQL-05 remains retired; census evidence is accounting, not substitute recall."
    - "D-15: every per-invocation scratch directory and captured-output file is removed on both the success and the failure path while the original exit status is preserved, so estate-derived path and position metadata never survives a failed gate."
    - "SQL-07/FORK-03 is enforced by the installed .githooks/pre-push hook; this plan commits only fork-local paths."
  artifacts:
    - {path: run_sql_tail_census.sh, provides: "Bounded aggregate tail inventory and comparison command"}
    - {path: docs/sql-tail-census.md, provides: "Census, differential, NIST, refresh, and cascade closeout evidence"}
  key_links:
    - {from: run_sql_tail_census.sh, to: docs/sql-tail-census.md, via: "deterministic aggregate output for OCESQL and estate", pattern: "sql_unparsed_tail"}
    - {from: run_nist_cobol85.sh, to: docs/sql-tail-census.md, via: "captured output, preserved status, and exact final summary line", pattern: "Fail: 0, Skip: 11"}
---
<objective>
Generate the D-14 census and differential evidence and execute every closing regression gate for Phase 4.

Purpose: complete behavioral validation of the SQL grammar, query, shim, and cascade work.
Output: aggregate census runner and generated evidence report.

FORK-02/FORK-03 are enforced by the already-installed `.githooks/pre-push` hook
(`git config core.hooksPath` is `.githooks`), which runs `estate-guard-selftest.sh`,
`estate-guard.sh --remote`, and `commit-separability.sh --range` on every push, exactly as
Phases 1 through 3 relied on. This plan performs no commit-history audit, persists no
range artifact, and invokes no guard manually. The per-task discipline is that every
commit here touches only fork-local paths (`run_sql_tail_census.sh`, `docs/`).
</objective>
<execution_context>
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/04-exec-sql-blocks/04-CONTEXT.md
@.planning/phases/04-exec-sql-blocks/04-PATTERNS.md
@.planning/phases/04-exec-sql-blocks/04-03-SUMMARY.md
@.planning/phases/04-exec-sql-blocks/04-06-SUMMARY.md
@.planning/phases/04-exec-sql-blocks/04-07-SUMMARY.md
@run_accept_differential.sh
@run_nist_cobol85.sh
@skip_tests.txt
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Generate an aggregate sql_unparsed_tail census for one population</name>
  <files>run_sql_tail_census.sh, docs/sql-tail-census.md</files>
  <read_first>
    run_cics_query_capture.sh:27-43,105-133 -- PATTERNS configuration block, accumulated status, and per-capture counting; the analog for run_sql_tail_census.sh status accumulation
    run_accept_differential.sh:629-674 -- PATTERNS owned-scratch corpus walking and fail-closed empty-denominator refusal; the analog for owned mktemp scratch and bounded traversal
    run_accept_differential.sh:710-786 -- PATTERNS per-file parser deadline, progress reporting, and aggregate-only output
    queries/sql.scm -- the final capture contract the census consumes
  </read_first>
  <behavior>
    - The runner inventories statements, tails, independently expected table anchors, and final post-D-18 physical results without persisting source text, identifiers, or paths.
    - Each structural category records statement and tail counts plus independently expected and captured physical-table counts.
    - A synthetic missing source candidate causes reconciliation failure.
  </behavior>
  <action>Create run_sql_tail_census.sh using the current parser and queries/sql.scm. Accept a corpus root and a population label, process COBOL files in a per-invocation `mktemp -d` scratch directory that the script owns and removes on exit, and retain the existing per-file parser deadline. Per D-14 and D-15, independently scan bounded EXEC SQL through END-EXEC statements for D-04 anchors without consulting AST or query output; retain CTE identifiers only in memory for same-statement exclusion and persist only aggregates. Separately process parser and query results using D-18's unique smallest enclosing @statement assignment by lexicographic (row, column) comparison and same-statement subtraction, then compare category totals. Provide a selftest covering both success and a deliberate missing-capture failure. Render tracked OCESQL results and the SQL-05 retirement into docs/sql-tail-census.md. Commit only the fork-local script and document.</action>
  <verify><automated>sh run_sql_tail_census.sh --selftest &amp;&amp; OUT_DIR="$(mktemp -d)" &amp;&amp; { sh run_sql_tail_census.sh --population OCESQL --corpus test/ocesql/src --output "$OUT_DIR/ocesql-tail.tsv" &amp;&amp; sh run_sql_tail_census.sh --render "$OUT_DIR/ocesql-tail.tsv" docs/sql-tail-census.md; ST=$?; rm -rf "$OUT_DIR"; test "$ST" -eq 0; }</automated></verify>
  <acceptance_criteria>
    - The selftest exits nonzero on a synthetic missing source candidate and zero otherwise.
    - Every structural category records statement, tail, independently expected, and captured physical-table counts.
    - The script follows run_cics_query_capture.sh accumulated-status style and run_accept_differential.sh owned-scratch and per-file-deadline patterns, with no set -e.
    - No source text, identifier, or path is persisted; the scratch directory is removed on exit.
    - The per-invocation scratch directory is removed on every exit path, success or failure, and the original exit status is preserved.
    - The commit contains only run_sql_tail_census.sh and docs/sql-tail-census.md.
  </acceptance_criteria>
  <done>OCESQL tails have executable aggregate category, count, and capture-impact evidence, and missing captures fail closed.</done>
</task>
<task type="auto">
  <name>Task 2: Add estate aggregates and run both collision differentials</name>
  <precondition>The DCC corpus directory `$HOME/repos/mine/cobolCode/cam-corpus-dcc/DCC` and the repository-local `estate/` directory both exist; the task halts naming the missing one otherwise.</precondition>
  <files>docs/sql-tail-census.md</files>
  <read_first>
    run_accept_differential.sh:556-627 -- PATTERNS the estate branch of prepare_accept_paths, its trigger conditions, and the ACCEPT denominator gate 04-03 makes conditional
    run_accept_differential.sh:629-674 -- PATTERNS the generic branch and its default ACCEPT source-text prefilter
    run_accept_differential.sh:788-936 -- PATTERNS the run argument order and dispatch after 04-03
    .planning/phases/04-exec-sql-blocks/04-03-SUMMARY.md -- the delivered four-argument run interface and prefilter semantics
  </read_first>
  <action>Define the two corpus roots explicitly rather than relying on inherited environment variables: DCC is the CONTEXT canonical reference `$HOME/repos/mine/cobolCode/cam-corpus-dcc/DCC` and the estate copy is the repository-local `estate` directory. Validate each with a directory test and halt with a message naming the missing root before running anything. Derive the differential baseline deterministically as the first parent of the commit that added test/corpus/exec_sql.txt, which is the last pre-Phase-4 grammar state, and verify it resolves. Run the census against estate into a per-invocation `mktemp -d` scratch and merge only aggregate rows into the report per D-15. Run 04-03's isolated differential against DCC and estate using its exact `run &lt;BASELINE_REF&gt; &lt;CORPUS_DIR&gt; [NODE_TYPE_REGEX] [TEXT_PREFILTER_RE|--no-prefilter]` interface. Pass BOTH the node regex `exec_sql_statement|select_statement|delete_statement|merge_statement|alter_statement|open_statement|close_statement` AND a matching source-TEXT prefilter covering EXEC SQL plus every collision keyword in that node set; the fourth argument is mandatory here because the differential's default text prefilter is ACCEPT-only and would drop roughly seventy percent of the collision-bearing population measured in DCC, reporting green on a corpus that was never examined. Name the estate branch explicitly in the recorded evidence: `prepare_accept_paths` takes a distinct path when the corpus root is `$TOP_DIR/estate` and `estate/endevor` exists, and both hold for the estate run, so that run enters the estate selector; because a custom selection is active, 04-03 suppresses the recorded 3781/1369/2391 ACCEPT denominator gate and instead reports the measured SQL-population denominator. Record the measured selected-file and statement totals for each population as observed; do not assert an invented expected constant and do not reuse the ACCEPT constants. Use a distinct fresh `mktemp -d` DIFF_TMP outside the repository for each call so reruns are repeatable, and remove each caller-owned scratch directory afterwards. Require zero clean loss or type changes and separately report intended trailing-error conversions. Record aggregate outcomes and exact commands only.</action>
  <verify><automated>DCC_CORPUS="$HOME/repos/mine/cobolCode/cam-corpus-dcc/DCC" &amp;&amp; ESTATE_CORPUS="estate" &amp;&amp; { test -d "$DCC_CORPUS" || { echo "halt: DCC corpus not found at $DCC_CORPUS"; false; }; } &amp;&amp; { test -d "$ESTATE_CORPUS" || { echo "halt: estate corpus not found at $ESTATE_CORPUS"; false; }; } &amp;&amp; SQL_BASELINE_REF="$(git log --diff-filter=A --format=%H -- test/corpus/exec_sql.txt | tail -1)^" &amp;&amp; git rev-parse --verify "${SQL_BASELINE_REF}^{commit}" &amp;&amp; SQL_NODE_REGEX='exec_sql_statement|select_statement|delete_statement|merge_statement|alter_statement|open_statement|close_statement' &amp;&amp; SQL_TEXT_PREFILTER='(^|[^A-Za-z0-9-])(EXEC[[:space:]]+SQL|SELECT|DELETE|MERGE|ALTER|OPEN|CLOSE)([^A-Za-z0-9-]|$)' &amp;&amp; OUT_DIR="$(mktemp -d)" &amp;&amp; DCC_TMP="$(mktemp -d)" &amp;&amp; ESTATE_TMP="$(mktemp -d)" &amp;&amp; { sh run_sql_tail_census.sh --population OCESQL --corpus test/ocesql/src --output "$OUT_DIR/ocesql-tail.tsv" &amp;&amp; sh run_sql_tail_census.sh --population ESTATE --corpus "$ESTATE_CORPUS" --output "$OUT_DIR/estate-tail.tsv" &amp;&amp; sh run_sql_tail_census.sh --render "$OUT_DIR/ocesql-tail.tsv" "$OUT_DIR/estate-tail.tsv" docs/sql-tail-census.md &amp;&amp; DIFF_TMP="$DCC_TMP" sh run_accept_differential.sh run "$SQL_BASELINE_REF" "$DCC_CORPUS" "$SQL_NODE_REGEX" "$SQL_TEXT_PREFILTER" &amp;&amp; DIFF_TMP="$ESTATE_TMP" sh run_accept_differential.sh run "$SQL_BASELINE_REF" "$ESTATE_CORPUS" "$SQL_NODE_REGEX" "$SQL_TEXT_PREFILTER"; ST=$?; rm -rf "$OUT_DIR" "$DCC_TMP" "$ESTATE_TMP"; test "$ST" -eq 0; }</automated></verify>
  <acceptance_criteria>
    - Both differential calls pass four arguments; neither relies on the ACCEPT default text prefilter.
    - The text prefilter matches EXEC SQL plus every collision keyword present in the node regex.
    - The recorded evidence names the estate branch of prepare_accept_paths and states that its ACCEPT denominator gate was suppressed because a custom selection was active.
    - Measured selected-file and statement denominators are recorded as observed for both populations, with no invented expected constant.
    - Every scratch directory holding estate-derived path or position metadata is removed on every exit path, success or failure, and the original exit status is preserved.
    - Zero clean reclassification, zero type changes, and no estate content in the report.
  </acceptance_criteria>
  <done>Both populations have regenerated census and differential evidence from validated corpus paths, an explicit SQL/collision text prefilter, and fresh scratch directories, with zero clean reclassification and no estate disclosure.</done>
</task>
<task type="auto">
  <name>Task 3: Capture closing regression evidence for Phase 4</name>
  <files>docs/sql-tail-census.md</files>
  <read_first>
    run_nist_cobol85.sh:15-41 -- PATTERNS runner invocation and summary-line emission
    skip_tests.txt -- the locked 11-name baseline this task asserts unchanged
    forest-shim/refresh.sh:91-144 -- PATTERNS generation and shim copy sequence the refresh gate exercises
  </read_first>
  <action>Execute the D-16 closing gates: generation, the full non-comment corpus, the exact SQL query capture gate, census report verification, shim refresh and byte comparison, and the gortex cascade test. For NIST, redirect stdout and stderr to a per-invocation `mktemp` file while preserving the runner exit status in a separate shell variable; require status zero and require the final nonblank line to equal the exact observed result recorded for this phase, namely 382 tests with 371 successes, zero failures, and 11 skips. Separately normalize skip_tests.txt by excluding blank and whitespace-only lines, then compare the ordered normalized nonblank values to exactly NC205A, SM101A, SM103A, SM105A, SM107A, SM201A, SM203A, SM205A, SM206A, SM208A, SM401M; reject every nonblank comment, duplicate, replacement, or extra nonblank entry. Require no diff in skip_tests.txt against the same pre-Phase-4 baseline derived in Task 2; the existing baseline blank line is permitted only because normalization excludes it. Append the commands, statuses, the final NIST summary line, and the results to docs/sql-tail-census.md. Commit only the fork-local document.</action>
  <verify><automated>SQL_BASELINE_REF="$(git log --diff-filter=A --format=%H -- test/corpus/exec_sql.txt | tail -1)^" &amp;&amp; git rev-parse --verify "${SQL_BASELINE_REF}^{commit}" &amp;&amp; NIST_OUT="$(mktemp)" &amp;&amp; { node_modules/tree-sitter-cli/tree-sitter generate &amp;&amp; node_modules/tree-sitter-cli/tree-sitter test -e '^comment$' &amp;&amp; sh run_sql_query_capture.sh &amp;&amp; sh run_sql_tail_census.sh --verify-report docs/sql-tail-census.md &amp;&amp; sh forest-shim/refresh.sh &amp;&amp; cmp queries/sql.scm forest-shim/cobol/sql.scm &amp;&amp; (cd /Users/e1001547-mbp-it/repos/mine/GoApps/gortex &amp;&amp; go test ./internal/parser/forest/cobolprobe -run TestErrorCascade -v -count=2) &amp;&amp; ( NIST_STATUS=0; sh run_nist_cobol85.sh &gt;"$NIST_OUT" 2&gt;&amp;1 || NIST_STATUS=$?; test "$NIST_STATUS" -eq 0 ) &amp;&amp; test "$(awk 'NF{line=$0} END{print line}' "$NIST_OUT")" = '382 tests. (Success: 371, Fail: 0, Skip: 11)' &amp;&amp; python3 -c 'expected=("NC205A","SM101A","SM103A","SM105A","SM107A","SM201A","SM203A","SM205A","SM206A","SM208A","SM401M"); rows=[line.strip() for line in open("skip_tests.txt",encoding="utf-8").read().splitlines() if line.strip()]; assert all(not row.startswith("#") for row in rows) and len(rows)==11 and len(set(rows))==11 and tuple(rows)==expected' &amp;&amp; git diff --exit-code "${SQL_BASELINE_REF}..HEAD" -- skip_tests.txt; ST=$?; rm -f "$NIST_OUT"; test "$ST" -eq 0; }</automated></verify>
  <acceptance_criteria>
    - The NIST runner exit status is captured separately from its output and is zero.
    - The final nonblank NIST line equals `382 tests. (Success: 371, Fail: 0, Skip: 11)`.
    - skip_tests.txt normalizes to exactly the ordered 11-name allowlist and is unchanged against the pre-Phase-4 baseline.
    - Generation, corpus, SQL query gate, census report verification, refresh byte comparison, and the gortex cascade test all exit zero.
    - The captured NIST output file is removed on every exit path, success or failure, and the original exit status is preserved.
    - The commit contains only docs/sql-tail-census.md.
  </acceptance_criteria>
  <done>Census, differential, corpus, query, refresh, cascade, and NIST evidence are green, and the observed NIST final line matches the exact recorded result with a preserved zero exit status.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Estate -> aggregate report | Proprietary inputs must yield only non-identifying totals and categories. |
| Test runner -> evidence | Exit status and final summary line must both survive capture. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-13 | Information Disclosure | census | high | mitigate | Aggregate-only renderer, owned mktemp scratch, and the installed pre-push estate guard. |
| T-04-14 | Tampering | NIST evidence | high | mitigate | Preserve exit status, assert the exact final line, exact allowlist, and unchanged skip file. |
| T-04-15 | Tampering | rerun determinism | medium | mitigate | Fresh mktemp scratch and DIFF_TMP per invocation, with cleanup that runs unconditionally on both the success and the failure path -- the gate chains are wrapped in a brace group whose exit status is captured before `rm`, then restored -- matching the `trap 'rm -rf "$SCRATCH"' EXIT INT TERM` discipline at run_accept_differential_selftest.sh:31, so no estate-derived path or position metadata survives a failed run. |
| T-04-17 | Tampering | differential corpus selection | high | mitigate | Explicit SQL/collision text prefilter plus named estate branch with a measured rather than constant denominator. |
| T-04-SC | Tampering | supply chain | low | accept | No dependency installation. |
</threat_model>

## Artifacts this phase produces

- Grammar nodes/rules: `exec_sql_statement`, `exec_sql_declare_section`, `exec_sql_include`, `sql_table_name`, `sql_table_alias`, `sql_include_name`, `sql_dynamic_source`, `sql_unparsed_tail`, structured CTE-definition and source-candidate rules, plus private clause helpers.
- Repository paths: `test/corpus/exec_sql.txt`, `queries/sql.scm`, `run_sql_query_capture.sh`, `run_sql_tail_census.sh`, `docs/sql-tail-census.md`, `.githooks/commit-separability.sh`.
- Generated/vendored paths: `src/parser.c`, `src/grammar.json`, `src/node-types.json`, `forest-shim/cobol/parser.c`, `forest-shim/cobol/grammar.json`, `forest-shim/cobol/sql.scm`.
- Cross-repo path under FORK-03: `/Users/e1001547-mbp-it/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go`, committed only in gortex.
<verification>All three task commands must run green; FORK-02 and FORK-03 are proven at push time by the installed hook, as in Phases 1 through 3.</verification>
<success_criteria>All Phase 4 behavioral evidence is complete, reproducible from validated corpus paths, and recorded.</success_criteria>
<output>Create `.planning/phases/04-exec-sql-blocks/04-08-SUMMARY.md` when done.</output>
