---
phase: 04-exec-sql-blocks
plan: 07
type: execute
wave: 6
depends_on: [04-06]
files_modified: [/Users/e1001547-mbp-it/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go]
autonomous: true
requirements: [SQL-04, SQL-05, SQL-07]
estimate: {tokens: 24000, raw_tokens: 24000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-16/SQL-04: procedure and DATA DIVISION SQL cases each preserve 20/20 paragraphs and data items against the immutable clean control."
    - "The gortex change is committed only in gortex, after the Phase 4 shim exists, and that commit touches only cascade_test.go."
  artifacts:
    - {path: /Users/e1001547-mbp-it/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go, provides: "Blocking SQL cascade parity gates"}
  key_links:
    - {from: forest-shim/cobol/parser.c, to: gortex TestErrorCascade, via: "existing go.work-resolved cobolforest.GetLanguage()", pattern: "cobolforest.GetLanguage"}
---
<objective>
Turn SQL recovery into a blocking downstream contract in the gortex repository.

Purpose: prove actual Go delivery preserves COBOL paragraphs and data items after EXEC SQL.
Output: fail-first SQL parity assertions in gortex TestErrorCascade.

FORK-02/FORK-03 are enforced by the installed `.githooks/pre-push` hook in this
repository. This plan writes no file in this repository: the only change is the
gortex-repo commit, which is verified directly with a single scoped check that the
commit touching `cascade_test.go` changed only that path. No range evidence is
persisted and no commit history is audited here.
</objective>
<execution_context>
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/04-exec-sql-blocks/04-CONTEXT.md
@.planning/phases/04-exec-sql-blocks/04-PATTERNS.md
@.planning/phases/04-exec-sql-blocks/04-06-SUMMARY.md
@/Users/e1001547-mbp-it/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Observe procedure and DATA DIVISION SQL parity against the clean control</name>
  <precondition>Plan 04-06 is green and gortex go.work resolves cobolforest to this repository's refreshed shim.</precondition>
  <files>/Users/e1001547-mbp-it/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go</files>
  <read_first>
    cascade_test.go:13-45 -- PATTERNS parser setup and recursive data-item/paragraph counters
    cascade_test.go:51-70,150-163 -- PATTERNS the immutable 20/20 clean control and injection shape
    cascade_test.go:166-203 -- PATTERNS independent per-shape parity assertions
  </read_first>
  <behavior>
    - A procedure-division EXEC SQL case reports 20/20 paragraph and data-item parity with the immutable clean control.
    - A DATA DIVISION EXEC SQL case reports the same parity.
    - The observation is reproducible across two consecutive runs.
  </behavior>
  <action>Add one representative procedure-division and one representative DATA DIVISION SQL fixture to TestErrorCascade in the existing CICS/IDMS style, reusing the immutable clean control already present in the file. Per D-16 and SQL-04, observe paragraph and data-item counts for each shape rather than asserting them in this task. Use invented neutral COBOL per D-15. Run the gate with `-count=2` so the result cannot come from a cached test run. Make no change in this repository; commit only cascade_test.go in gortex.</action>
  <verify><automated>(cd /Users/e1001547-mbp-it/repos/mine/GoApps/gortex &amp;&amp; go test ./internal/parser/forest/cobolprobe -run TestErrorCascade -v -count=2)</automated></verify>
  <acceptance_criteria>
    - Both SQL shapes reuse the existing immutable 20/20 clean control rather than a new control.
    - Counts are observed and reported in this task, not asserted.
    - Two consecutive `-count=2` runs report identical counts.
    - No file in this repository is modified.
  </acceptance_criteria>
  <done>Both SQL placements report deterministic clean-control parity through the refreshed shim.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Prove fail-first and enforce four independent SQL parity assertions</name>
  <precondition>Task 1 observations report deterministic 20/20 parity with the current shim.</precondition>
  <files>/Users/e1001547-mbp-it/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go</files>
  <read_first>
    cascade_test.go:166-203 -- PATTERNS the existing CICS assertion style the four SQL assertions mirror
    .planning/phases/04-exec-sql-blocks/04-06-SUMMARY.md -- the shim state that must be restored for the GREEN proof
  </read_first>
  <behavior>
    - The pre-Phase-4 shim fails at least one new SQL assertion.
    - The current shim passes paragraph and data-item parity for each SQL shape twice.
    - The gortex commit that introduced the SQL assertions touches only cascade_test.go.
  </behavior>
  <action>Add four independent assertions -- paragraph parity and data-item parity for each of the two SQL shapes -- matching the existing CICS assertion style. Prove RED by restoring the pre-Phase-4 shim state in an isolated worktree or a reversible restoration, recording the nonzero SQL assertion, then restore 04-06's shim and prove GREEN twice. Do not use tree-sitter CLI cache state as evidence. Commit only cascade_test.go in gortex. Verify separability with the single scoped check below: resolve the most recent gortex commit touching cascade_test.go and assert its exact changed-path set is only that file. Do not persist ranges in any JSON artifact and do not audit any other commit.</action>
  <verify><automated>GORTEX=/Users/e1001547-mbp-it/repos/mine/GoApps/gortex &amp;&amp; CASCADE=internal/parser/forest/cobolprobe/cascade_test.go &amp;&amp; SQL_COMMIT="$(git -C "$GORTEX" log -n 1 --format=%H -- "$CASCADE")" &amp;&amp; test -n "$SQL_COMMIT" &amp;&amp; test "$(git -C "$GORTEX" diff-tree --no-commit-id --name-only -r "$SQL_COMMIT" | LC_ALL=C sort -u | grep -v '^$')" = "$CASCADE" &amp;&amp; (cd "$GORTEX" &amp;&amp; go test ./internal/parser/forest/cobolprobe -run TestErrorCascade -v -count=2)</automated></verify>
  <done>SQL-04 has RED/GREEN evidence, four independent parity assertions, and a gortex commit proven to touch only cascade_test.go.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Grammar repo -> gortex repo | Delivery state is shared while commits remain separate. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-20 | Tampering | TestErrorCascade | high | mitigate | Immutable control plus pre-parser RED and current-parser GREEN proof at -count=2. |
| T-04-21 | Tampering | cross-repo commit scope | medium | mitigate | Scoped single-commit path check on cascade_test.go in the gortex worktree. |
| T-04-SC | Tampering | supply chain | low | accept | Existing Go module and local shim only. |
</threat_model>

## Artifacts this phase produces

- Grammar nodes/rules: `exec_sql_statement`, `exec_sql_declare_section`, `exec_sql_include`, `sql_table_name`, `sql_table_alias`, `sql_include_name`, `sql_dynamic_source`, `sql_unparsed_tail`, structured CTE-definition and source-candidate rules, plus private clause helpers.
- Repository paths: `test/corpus/exec_sql.txt`, `queries/sql.scm`, `run_sql_query_capture.sh`, `run_sql_tail_census.sh`, `docs/sql-tail-census.md`, `.githooks/commit-separability.sh`.
- Generated/vendored paths: `src/parser.c`, `src/grammar.json`, `src/node-types.json`, `forest-shim/cobol/parser.c`, `forest-shim/cobol/grammar.json`, `forest-shim/cobol/sql.scm`.
- Cross-repo path under FORK-03: `/Users/e1001547-mbp-it/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go`, committed only in gortex.
<verification>Observe parity first, then run the exact cross-repo parity and scoped path checks.</verification>
<success_criteria>Both SQL placements preserve 20/20 structures and the gortex change is provably one-path.</success_criteria>
<output>Create `.planning/phases/04-exec-sql-blocks/04-07-SUMMARY.md` when done.</output>
