---
phase: 04-exec-sql-blocks
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [run_sql_query_capture.sh, .githooks/commit-separability.sh]
autonomous: true
requirements: [SQL-03, SQL-05, SQL-07]
estimate: {tokens: 38000, raw_tokens: 38000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "D-18: every role is assigned to the unique smallest enclosing @statement (row, column) range before same-statement CTE subtraction produces exact physical-table results."
    - "D-17: captures present in the 04-01 query are checked for exact text, count, position, multiplicity, and source order, including empty physical sets; later plans extend the same gate for newly introduced roles."
    - "SQL-07/FORK-03: each task commit contains only fork-local script paths; separability is enforced by the installed .githooks/pre-push hook."
    - "SQL-07/FORK-03: the hook's fork-local path family is widened additively -- every path the existing `^run_accept_differential` alternative matches, including the tracked `run_accept_differential_helper.c`, stays fork-local -- while newly covering every root-level run_*.sh gate this phase creates, so a commit mixing queries/sql.scm with run_sql_query_capture.sh or run_sql_tail_census.sh fails rather than passing."
  artifacts:
    - {path: run_sql_query_capture.sh, provides: "Executable exact SQL capture and CTE subtraction gate"}
    - {path: .githooks/commit-separability.sh, provides: "Fork-local path family widened additively to cover every root-level run_* gate without dropping any currently matched path"}
  key_links:
    - {from: queries/sql.scm, to: run_sql_query_capture.sh, via: "role containment in the unique smallest @statement (row, column) range", pattern: "statement|cte_definition|source_candidate"}
    - {from: .githooks/commit-separability.sh, to: run_sql_query_capture.sh, via: "FORKLOCAL_PATH_RE matching root-level run_*.sh", pattern: "FORKLOCAL_PATH_RE"}
---
<objective>
Complete the tracer end to end with a fork-local exact query gate in a commit separate from the grammar family.

Purpose: mechanically prove D-18 physical-table identity and all D-17 raw role captures.
Output: reusable run_sql_query_capture.sh, plus the hook widening that makes the fork-local family actually cover it.

FORK-02/FORK-03 are enforced by the installed `.githooks/pre-push` hook. The
per-task discipline is that every commit in this plan touches only fork-local
paths -- `run_sql_query_capture.sh` in Tasks 1 and 2, `.githooks/commit-separability.sh`
in Task 3 -- and no grammar-family path may be mixed in. Task 3 exists because
`FORKLOCAL_PATH_RE` at `.githooks/commit-separability.sh:18` currently matches only
`^run_accept_differential`, so a commit mixing `queries/sql.scm` with
`run_sql_query_capture.sh` is reported PASS today.
</objective>
<execution_context>
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/04-exec-sql-blocks/04-CONTEXT.md
@.planning/phases/04-exec-sql-blocks/04-PATTERNS.md
@.planning/phases/04-exec-sql-blocks/04-01-SUMMARY.md
@queries/sql.scm
@run_cics_query_capture.sh
@.githooks/commit-separability.sh
@.githooks/estate-guard-selftest.sh
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Compute one exact physical-table result from raw SQL captures</name>
  <files>run_sql_query_capture.sh</files>
  <read_first>
    run_cics_query_capture.sh:27-43 -- PATTERNS configuration block, scratch dir, EXPECTED_CAPTURES, report()
    run_cics_query_capture.sh:55-73,105-133 -- PATTERNS precondition checks, query invocation, and capture counting
    queries/sql.scm -- the committed 04-01 capture contract this gate consumes
  </read_first>
  <behavior>
    - CTE-only outer source produces an empty physical set while its nested base source remains physical.
    - Outer CTE plus physical JOIN excludes only the same-statement CTE name.
    - Adjacent statements reusing identical CTE names do not leak scope; nested roles bind to their unique smallest enclosing statement.
    - No container, multiple equal smallest containers, a role outside its assigned statement, or overlapping sibling statement ranges fails closed.
  </behavior>
  <action>Create run_sql_query_capture.sh from run_cics_query_capture.sh:27-43,55-73,105-133. Preserve explicit status accumulation rather than shell fail-fast flags. Invoke the pinned CLI and queries/sql.scm. tree-sitter-cli 0.24.5 emits each capture as `start: (row, column), end: (row, column)` and emits no byte offsets, so parse each @statement and role capture's start and end (row, column) pair and compare positions lexicographically -- position A is at or before position B when A.row is less than B.row, or A.row equals B.row and A.column is at most B.column. Treat containment as statement.start at or before role.start and role.end at or before statement.end under that ordering. Assign the role to the containing statement whose (start, end) span is narrowest, comparing spans by row difference first and then by column difference on a single-row span. Require exactly one smallest container. Fail on no container, multiple equal-narrowest containers, any role position outside its assigned range, or overlapping sibling @statement ranges. Then normalize identity exactly as emitted and subtract only CTE-definition names assigned to that same statement per D-18. Assert exact physical sets for all three locked cases, adjacent statements reusing a CTE name, nested subqueries, boundary-touching role positions, and an empty-source utility statement. Use `.sql-query-scratch` via `.git/info/exclude`; never emit estate content. Commit only run_sql_query_capture.sh.</action>
  <verify><automated>sh run_sql_query_capture.sh</automated></verify>
  <acceptance_criteria>
    - All three locked CTE cases produce their exact expected physical-table sets, including the empty set.
    - Adjacent statements reusing one CTE name do not share scope.
    - No container, multiple equal-narrowest containers, an out-of-range role, and overlapping sibling statement ranges each exit nonzero.
    - The commit contains only run_sql_query_capture.sh.
  </acceptance_criteria>
  <done>The tracer's raw capture stream deterministically yields the exact statement-scoped physical-table contract from (row, column) positions.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Enforce exact capture positions, multiplicity, order, and negative roles</name>
  <files>run_sql_query_capture.sh</files>
  <read_first>
    run_sql_query_capture.sh -- the complete Task 1 gate this task extends
    run_cics_query_capture.sh:105-133 -- PATTERNS per-capture counting and accumulated failure reporting
  </read_first>
  <behavior>
    - Qualified names retain exact complete (row, column) spans and aliases never overlap them or their assigned statement boundary.
    - Duplicate occurrences and multiple sources retain source order.
    - CTE names, derived aliases, and host variables never enter the physical table result; the static D-04 physical-table records are added and rerun in 04-05 Task 3 and the dynamic-source exclusion in 04-05 Task 4, after those nodes exist.
  </behavior>
  <action>Extend the D-17 gate to assert @statement and role capture text, exact start/end (row, column) pairs, counts, multiplicity, source order, and the exact containment failure cases from Task 1. Do not assert byte offsets; the 0.24.5 query runner does not emit them. Include explicit adjacent same-name, nested-subquery, and start/end boundary tests before asserting table, alias, CTE definition, and source candidate results. Assert negative physical results for CTE references, derived aliases, and host variables. Design the role assertions so 04-04 Task 3 can add include expectations, 04-05 Task 3 can add exact static physical-table records for every D-04 form including nested-subquery and MERGE USING sources, and 04-05 Task 4 can add dynamic-source expectations, all without changing the statement-scoped filtering contract. Preserve SQL-05 as retired without a recall command. Commit only run_sql_query_capture.sh so no grammar-family path is mixed into this fork-local task.</action>
  <verify><automated>sh run_sql_query_capture.sh &amp;&amp; sh -n run_sql_query_capture.sh</automated></verify>
  <done>The gate proves every capture role available after 04-01 and fails on wrong identity, position, count, or ordering; staged include, static physical-table, and dynamic-source checks have explicit extension points in 04-04 Task 3, 04-05 Task 3, and 04-05 Task 4.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Widen the hook's fork-local path family to cover every root-level run_* gate</name>
  <read_first>
    .githooks/commit-separability.sh:16-18 -- PATTERNS GRAMMAR_PATH_RE and FORKLOCAL_PATH_RE; the fork-local family matches only `^run_accept_differential`, which today also covers the tracked root-level `run_accept_differential_helper.c`
    .githooks/commit-separability.sh:64-85 -- PATTERNS per-commit intersection check and explicit PASS/FAIL reporting
    .githooks/estate-guard-selftest.sh -- PATTERNS synthetic fail-first selftest style for a hook script
  </read_first>
  <files>.githooks/commit-separability.sh</files>
  <behavior>
    - A synthetic commit mixing `queries/sql.scm` with `run_sql_query_capture.sh` is reported FAIL, not PASS.
    - The same holds for `run_sql_tail_census.sh`, which Plan 04-08 creates.
    - `run_accept_differential.sh` and `run_accept_differential_selftest.sh` keep their existing fork-local classification.
    - The tracked root-level `run_accept_differential_helper.c` keeps its existing fork-local classification, so a synthetic commit mixing `grammar.js` with `run_accept_differential_helper.c` still reports FAIL after the change.
    - The tracked digit-bearing `run_nist_cobol85.sh` is fork-local.
    - No grammar-family path is reclassified.
  </behavior>
  <action>Commit this as a fork-local `.githooks/` change in its own commit, separate from the run_sql_query_capture.sh commits in Tasks 1 and 2. Widen the `^run_accept_differential` alternative in FORKLOCAL_PATH_RE at .githooks/commit-separability.sh:18 additively rather than replacing it: the alternative becomes `^run_accept_differential|^run_[a-z0-9_]*\.(sh|c)$`. Widening additively is mandatory because the tracked root-level `run_accept_differential_helper.c` matches the current alternative and would be silently dropped by an `.sh`-only pattern, and because `run_nist_cobol85.sh` contains digits that a digit-free character class excludes. The chosen pattern must match all seven tracked or planned paths `run_accept_differential.sh`, `run_accept_differential_selftest.sh`, `run_accept_differential_helper.c`, `run_nist_cobol85.sh`, `run_cics_query_capture.sh`, `run_sql_query_capture.sh`, and `run_sql_tail_census.sh`. Leave GRAMMAR_PATH_RE, every other alternative, and the hook's reporting format byte-identical. Prove the change fail-first: before editing, build a synthetic commit set in a throwaway `mktemp -d` git repository containing one commit touching both `queries/sql.scm` and `run_sql_query_capture.sh`, run the hook against that range, and record PASS; after editing, re-run and require FAIL. Also require that a commit mixing `grammar.js` with `run_accept_differential_helper.c` reports FAIL both before and after the change, proving the widening did not narrow the existing family, and that a run_accept_differential-only commit and a grammar-only commit still report PASS. Follow the accumulated-status shell style already in the hook; do not introduce set -e.</action>
  <verify><automated>sh -n .githooks/commit-separability.sh &amp;&amp; HOOK="$(pwd)/.githooks/commit-separability.sh" &amp;&amp; T="$(mktemp -d)" &amp;&amp; { git init -q "$T" &amp;&amp; git -C "$T" config user.email t@example.invalid &amp;&amp; git -C "$T" config user.name t &amp;&amp; : &gt; "$T/README" &amp;&amp; git -C "$T" add README &amp;&amp; git -C "$T" commit -qm base &amp;&amp; BASE="$(git -C "$T" rev-parse HEAD)" &amp;&amp; mkdir -p "$T/queries" &amp;&amp; : &gt; "$T/queries/sql.scm" &amp;&amp; : &gt; "$T/run_sql_query_capture.sh" &amp;&amp; git -C "$T" add -A &amp;&amp; git -C "$T" commit -qm mixed &amp;&amp; MIXED="$(git -C "$T" rev-parse HEAD)" &amp;&amp; : &gt; "$T/run_accept_differential.sh" &amp;&amp; git -C "$T" add -A &amp;&amp; git -C "$T" commit -qm forklocal &amp;&amp; FORKLOCAL="$(git -C "$T" rev-parse HEAD)" &amp;&amp; : &gt; "$T/grammar.js" &amp;&amp; : &gt; "$T/run_accept_differential_helper.c" &amp;&amp; git -C "$T" add -A &amp;&amp; git -C "$T" commit -qm helpermix &amp;&amp; HELPERMIX="$(git -C "$T" rev-parse HEAD)" &amp;&amp; ! ( cd "$T" &amp;&amp; sh "$HOOK" --range "${BASE}..${MIXED}" ) &amp;&amp; ( cd "$T" &amp;&amp; sh "$HOOK" --range "${MIXED}..${FORKLOCAL}" ) &amp;&amp; ! ( cd "$T" &amp;&amp; sh "$HOOK" --range "${FORKLOCAL}..${HELPERMIX}" ); ST=$?; rm -rf "$T"; test "$ST" -eq 0; }</automated></verify>
  <acceptance_criteria>
    - A synthetic commit mixing a `queries/` path with `run_sql_query_capture.sh` exits nonzero and names both hit paths.
    - The same synthetic commit was proven to exit zero before the regex change.
    - A synthetic commit mixing `grammar.js` with the tracked root-level `run_accept_differential_helper.c` exits nonzero AFTER the change, proving the widening did not narrow the pre-existing `^run_accept_differential` coverage; it was recorded as already exiting nonzero before the change.
    - `run_accept_differential.sh`, `run_accept_differential_selftest.sh`, `run_accept_differential_helper.c`, `run_nist_cobol85.sh`, `run_cics_query_capture.sh`, `run_sql_query_capture.sh`, and `run_sql_tail_census.sh` all match the fork-local family after the change.
    - `GRAMMAR_PATH_RE` and the hook's PASS/FAIL reporting format are byte-identical.
    - The synthetic scratch repository is removed on every path, success or failure.
    - The commit contains only `.githooks/commit-separability.sh`.
  </acceptance_criteria>
  <done>The installed hook actually enforces the grammar-versus-fork-local split that Plans 04-02, 04-04, 04-05, and 04-08 assert in their must_haves.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Query output -> physical identity | Raw captures require statement-local filtering. |
| Fixture -> scratch logs | Parsed text must remain in ignored local scratch. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-04 | Spoofing | CTE subtraction | high | mitigate | Three scoped cases plus cross-statement non-leak assertion over (row, column) containment. |
| T-04-05 | Information Disclosure | scratch logs | high | mitigate | Owned ignored scratch, invented fixtures, and the installed pre-push estate guard. |
| T-04-16 | Tampering | commit-separability coverage | high | mitigate | Widen FORKLOCAL_PATH_RE additively to every root-level run_* gate, prove the previously-passing mixed synthetic commit now fails, and prove the grammar.js + run_accept_differential_helper.c commit still fails so no currently matched path is dropped. |
| T-04-SC | Tampering | supply chain | low | accept | No package installation. |
</threat_model>

## Artifacts this phase produces

- Grammar nodes/rules: `exec_sql_statement`, `exec_sql_declare_section`, `exec_sql_include`, `sql_table_name`, `sql_table_alias`, `sql_include_name`, `sql_dynamic_source`, `sql_unparsed_tail`, structured CTE-definition and source-candidate rules, plus private clause helpers.
- Repository paths: `test/corpus/exec_sql.txt`, `queries/sql.scm`, `run_sql_query_capture.sh`, `run_sql_tail_census.sh`, `docs/sql-tail-census.md`, `.githooks/commit-separability.sh`.
- Generated/vendored paths: `src/parser.c`, `src/grammar.json`, `src/node-types.json`, `forest-shim/cobol/parser.c`, `forest-shim/cobol/grammar.json`, `forest-shim/cobol/sql.scm`.
- Cross-repo path under FORK-03: `/Users/e1001547-mbp-it/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go`, committed only in gortex.
<verification>Run the script and shell syntax check; both must exit zero.</verification>
<success_criteria>Exact physical-table results are executable and the fork-local gate remains commit-separable.</success_criteria>
<output>Create `.planning/phases/04-exec-sql-blocks/04-02-SUMMARY.md` when done.</output>
