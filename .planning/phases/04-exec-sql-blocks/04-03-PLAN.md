---
phase: 04-exec-sql-blocks
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - run_accept_differential.sh
  - run_accept_differential_selftest.sh
autonomous: true
requirements: [SQL-05, SQL-06, SQL-07]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-16: run mode honors a caller-supplied SQL node regex through path selection, both isolated helper snapshots, helper invocation, and reporting."
    - "D-16: corpus path selection is driven by a caller-supplied source-TEXT prefilter (or an explicit full-walk mode) that is a separate argument from the node-type regex, so a collision-bearing file without the default keyword is never silently dropped."
    - "D-16: the estate branch of prepare_accept_paths applies its recorded ACCEPT denominator gate only when no custom selection is requested; under a custom selection it records the measured population instead of asserting an invented constant."
    - "The omitted regex argument retains the existing ACCEPT default and all existing synthetic comparison cases stay green."
    - "The differential remains parser-cache-safe by compiling separate baseline/current helpers and never relies on the tree-sitter CLI cache for comparison."
    - "SQL-05 remains retired; parameterization enables collision evidence, not a substitute recall measurement."
  artifacts:
    - path: "run_accept_differential.sh"
      provides: "Reusable run-mode node-regex parameterization for Phase 4 collision checks"
    - path: "run_accept_differential_selftest.sh"
      provides: "Fail-first proof of custom-regex propagation plus backward-compatible ACCEPT defaults"
  key_links:
    - from: "cmd_run caller regex"
      to: "compiled inventory helper"
      via: "run_helper_snapshot argument threaded into selection, invocation, and report"
      pattern: "NODE_TYPE_REGEX"
  prohibitions:
    - statement: "A custom SQL differential run must not silently fall back to the ACCEPT-only node filter."
      status: unverified
      flagged: true
    - statement: "A custom SQL differential run must not silently fall back to the ACCEPT-only source-text prefilter or to the ACCEPT-selected estate branch."
      status: unverified
      flagged: true
    - statement: "Differential inventories and corpus content must not be written under tracked repository paths."
      status: unverified
      flagged: true
---

<objective>
Close the reusable differential validation gap so Phase 4 can detect SQL and COBOL keyword reclassification with trustworthy baseline/current evidence.

Purpose: prevent a green-but-unmeasured SQL differential caused by the current ACCEPT-only run path.
Output: backward-compatible differential parameterization and its fast synthetic proof.
</objective>

<execution_context>
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-exec-sql-blocks/04-CONTEXT.md
@.planning/phases/04-exec-sql-blocks/04-RESEARCH.md
@.planning/phases/04-exec-sql-blocks/04-PATTERNS.md
@.planning/phases/04-exec-sql-blocks/04-VALIDATION.md
@.planning/phases/03-exec-cics-blocks/03-04-SUMMARY.md
@run_accept_differential.sh
@run_accept_differential_selftest.sh
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove a caller-selected SQL regex reaches one isolated baseline/current comparison</name>
  <read_first>
    run_accept_differential.sh:15-45,117-123,171-176 -- PATTERNS already-correct snapshot parameterization
    run_accept_differential.sh:710-743,779-906 -- PATTERNS exact hard-coded run-mode gap and parser-cache-safe orchestration
    run_accept_differential_selftest.sh:23-33,68-97 -- PATTERNS exact scratch and aggregate-result harness
  </read_first>
  <files>run_accept_differential.sh, run_accept_differential_selftest.sh</files>
  <behavior>
    - RED: a synthetic run requesting exec_sql_statement plus a collision-sensitive COBOL node fails because the helper currently receives the ACCEPT regex.
    - GREEN: the selected regex reaches path preselection, baseline helper, current helper, and status output verbatim.
    - GREEN: a synthetic changed SQL record is detected and named, proving the custom path can fail rather than only report success.
  </behavior>
  <action>
    Add the failing synthetic case first using the existing mktemp harness and no repository corpus. Extend cmd_run with an optional node regex after its current arguments, defaulting to DEFAULT_NODE_TYPE_REGEX for backward compatibility. State the caller convention explicitly in the usage text and in a comment above cmd_run: the caller supplies a BARE alternation with no anchors and no surrounding group, exactly as cmd_snapshot already embeds its third argument inside NODE_LINE_RE; cmd_run is responsible for wrapping that bare value as `^(<regex>)$` when it builds the helper's final argument, which is how the current hard-coded `^(accept_statement|idms_accept_statement)$` at run_accept_differential.sh:742 is formed. Thread the bare value through selected-path discovery, both run_helper_snapshot calls, the wrapped helper binary argument, and every report line; the report line at run_accept_differential.sh:779 must print the caller's value rather than DEFAULT_NODE_TYPE_REGEX. Preserve the compiled-helper sequence at PATTERNS run_accept_differential.sh:824-881 so the tree-sitter grammar-name cache cannot contaminate evidence. Keep every fallible command explicitly status-checked in the existing accumulated-result style. Do not create a SQL-specific walker.
  </action>
  <verify>
    <automated>sh run_accept_differential_selftest.sh</automated>
  </verify>
  <acceptance_criteria>
    - The self-test proves a custom regex is visible in the isolated helper output/report.
    - The self-test asserts the ANCHORED form `^(<caller regex>)$` appears in the reported helper invocation line, proving cmd_run wraps a bare caller alternation exactly once.
    - A deliberate custom-regex reclassification exits nonzero and names the offending synthetic record.
    - Existing compare cases remain unchanged and green.
  </acceptance_criteria>
  <done>One complete isolated comparison uses the caller's SQL-relevant node set and demonstrably catches a regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parameterize corpus path selection so a custom node set is not filtered by the ACCEPT source text</name>
  <read_first>
    run_accept_differential.sh:556-627 -- PATTERNS the estate branch: it triggers when the corpus root is $TOP_DIR/estate and estate/endevor exists, selects with `^\s*ACCEPT`, and hard-fails unless the population is exactly 3781/1369/2391
    run_accept_differential.sh:629-674 -- PATTERNS the generic branch: `find` enumeration followed by the hard-coded source-TEXT prefilter `rg -i '(^|[^A-Za-z0-9-])ACCEPT([^A-Za-z0-9-]|$)'`
    run_accept_differential.sh:839-846 -- PATTERNS the single prepare_accept_paths call site inside cmd_run
    run_accept_differential_selftest.sh:23-33,68-97 -- PATTERNS scratch isolation and aggregate-result harness
  </read_first>
  <files>run_accept_differential.sh, run_accept_differential_selftest.sh</files>
  <behavior>
    - RED: a synthetic corpus file that contains a collision keyword but no ACCEPT token is dropped from selection, so a reclassification inside it goes undetected; the new case fails before the fix and passes after it.
    - The source-TEXT prefilter is a caller-supplied argument DISTINCT from the node-type regex, and the literal value `--no-prefilter` selects every discovered source file with no text filter at all.
    - Omitting the prefilter argument reproduces the current ACCEPT text prefilter byte for byte.
    - Under a custom selection the estate branch does not apply the 2391-statement ACCEPT denominator gate; it reports the measured selected-file and statement population instead.
    - Zero selected files still fails closed in every mode.
  </behavior>
  <action>
    Add the failing synthetic case first, and make it exercise the real prepare_accept_paths selection function rather than a stub: build a throwaway corpus under the existing mktemp scratch containing one file with a collision keyword and no ACCEPT token, run the real selection path, and assert that file appears in the selected-paths output. Then extend cmd_run with a fourth optional argument, the source-TEXT prefilter ERE, and pass it into prepare_accept_paths as a third parameter. When that parameter is empty, use the existing `(^|[^A-Za-z0-9-])ACCEPT([^A-Za-z0-9-]|$)` value so the default path is unchanged; when it is the literal `--no-prefilter`, skip the rg step entirely and select every file the existing find enumeration discovered, keeping the same extension filter at run_accept_differential.sh:657-664. Name the estate branch explicitly: prepare_accept_paths takes a separate path when the corpus root equals $TOP_DIR/estate and estate/endevor exists, and both conditions hold for this repository, so any estate run reaches it. Make that branch's per-line selector and its recorded-denominator assertion conditional on whether a custom selection is active -- active meaning the caller supplied either a node regex or a text prefilter. With no custom selection, keep the `^\s*ACCEPT` selector and the exact 3781/1369/2391 hard failure unchanged. With a custom selection, drive the per-line match from the caller's prefilter (or match every code line under `--no-prefilter`), keep the selection-independent structural assertions on declared members and compilable programs, drop the statement-count constant, and report the measured selected_files and statement totals as the recorded denominator for that run. Keep the zero-selection refusal in every mode. Keep every fallible command explicitly status-checked; do not introduce set -e.
  </action>
  <verify>
    <automated>sh run_accept_differential_selftest.sh</automated>
  </verify>
  <acceptance_criteria>
    - A collision-bearing, ACCEPT-free synthetic file is selected under a custom text prefilter and under `--no-prefilter`, and the new case fails if it is dropped.
    - The new case calls the real selection function; no stub or reimplemented selector appears in the self-test.
    - The default invocation still selects exactly the files the current ACCEPT prefilter selects.
    - The estate branch is named in both code comment and self-test report text, and its ACCEPT denominator gate is proven to apply only when no custom selection is requested.
  </acceptance_criteria>
  <done>Path selection is driven by an explicit caller-supplied text prefilter that cannot be confused with the node-type regex, and the estate denominator gate is conditional rather than unconditional.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve ACCEPT defaults and reject missing or malformed run inputs</name>
  <read_first>
    run_accept_differential.sh:117-255 -- default handling, snapshot validation, and usage text
    run_accept_differential.sh:788-936 -- run dispatch and argument order after Tasks 1 and 2
    run_accept_differential_selftest.sh -- complete current case contract after Tasks 1 and 2
  </read_first>
  <files>run_accept_differential.sh, run_accept_differential_selftest.sh</files>
  <behavior>
    - Omitting both optional arguments uses accept_statement|idms_accept_statement and the ACCEPT text prefilter exactly as before.
    - An explicit empty/malformed invocation fails loudly rather than generating two empty inventories that compare green.
    - Scratch inventories remain outside the repository and cleanup retains ownership checks.
  </behavior>
  <action>
    Extend the self-test with an omitted-argument case that asserts the original ACCEPT default node regex and the original ACCEPT text prefilter both appear in reporting, and with invalid-input cases that prove fail-closed behavior for an empty node regex, an empty prefilter string that is not the `--no-prefilter` literal, and a prefilter that rg rejects. Update usage/help text to describe the full `run &lt;BASELINE_REF&gt; &lt;CORPUS_DIR&gt; [NODE_TYPE_REGEX] [TEXT_PREFILTER_RE|--no-prefilter]` shape, the bare-alternation caller convention from Task 1, and the Phase 4 call shape without baking a corpus path into the script. Preserve the existing six synthetic compare cases and repository-root output refusal. Per D-16 and SQL-07, report counts/record identities only; never emit source lines from corpus inputs.
  </action>
  <verify>
    <automated>sh run_accept_differential_selftest.sh &amp;&amp; sh -n run_accept_differential.sh &amp;&amp; sh -n run_accept_differential_selftest.sh</automated>
  </verify>
  <acceptance_criteria>
    - All old and new synthetic cases pass.
    - Default and custom node-regex paths and default and custom text-prefilter paths are each explicitly asserted.
    - Missing/malformed inputs fail nonzero without creating tracked output.
    - Both scripts are syntax-checked by separate `sh -n` invocations, because a single `sh -n a.sh b.sh` only checks the first file.
  </acceptance_criteria>
  <done>The reusable differential supports Phase 4 while retaining its Phase 2 contract and isolation guarantees.</done>
</task>

</tasks>

## Artifacts this phase produces

- Grammar nodes/rules: `exec_sql_statement`, `exec_sql_declare_section`, `exec_sql_include`, `sql_table_name`, `sql_table_alias`, `sql_include_name`, `sql_dynamic_source`, `sql_unparsed_tail`, structured CTE-definition and source-candidate rules, plus private clause helpers.
- Repository paths: `test/corpus/exec_sql.txt`, `queries/sql.scm`, `run_sql_query_capture.sh`, `run_sql_tail_census.sh`, `docs/sql-tail-census.md`, `.githooks/commit-separability.sh`.
- Generated/vendored paths: `src/parser.c`, `src/grammar.json`, `src/node-types.json`, `forest-shim/cobol/parser.c`, `forest-shim/cobol/grammar.json`, `forest-shim/cobol/sql.scm`.
- Cross-repo path under FORK-03: `/Users/e1001547-mbp-it/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go`, committed only in gortex.

<assumption_delta_decision>
no-change -- this is validation plumbing, not an identity-model transition.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arguments -> differential helper | Caller controls corpus path, output path, baseline ref, and node regex. |
| Corpus -> inventory output | Proprietary source is parsed into metadata that must remain outside the public repository. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-09 | Tampering | run-mode regex propagation | high | mitigate | Synthetic fail-first case proves the selected regex reaches both helpers and detects a changed record. |
| T-04-12 | Tampering | corpus path selection | high | mitigate | Caller-supplied text prefilter separate from the node regex, explicit full-walk mode, named estate branch with conditional denominator gate, and a fail-first case for a collision-bearing ACCEPT-free file. |
| T-04-10 | Information Disclosure | differential output | high | mitigate | Existing repository-root refusal, mktemp scratch, count/metadata-only reporting, and FORK-02 gate. |
| T-04-11 | Denial of Service | corpus traversal | medium | mitigate | Preserve per-file parser deadline, GNU timeout, selected-path prefilter, and owned-scratch cleanup. |
| T-04-SC | Tampering | package supply chain | low | accept | Shell/C tooling is already present; no install occurs. |
</threat_model>

<verification>
The synthetic harness must prove both fail-first custom filtering and unchanged default filtering in under 60 seconds.
</verification>

<success_criteria>
Phase 4 can invoke the established isolated differential with SQL-relevant node types and trust that those exact types were measured.
</success_criteria>

<output>
Create `.planning/phases/04-exec-sql-blocks/04-03-SUMMARY.md` when done.
</output>
