---
phase: 04-exec-sql-blocks
plan: 05
type: execute
wave: 4
depends_on: [04-02, 04-04]
files_modified: [grammar.js, test/corpus/exec_sql.txt, queries/sql.scm, src/parser.c, src/grammar.json, src/node-types.json, run_sql_query_capture.sh]
autonomous: true
requirements: [SQL-02, SQL-03, SQL-05, SQL-07]
estimate: {tokens: 84000, raw_tokens: 84000, tasks: 4, confidence: low}
must_haves:
  truths:
    - "D-04/D-05/D-06/D-07: every mandated static DML/DDL source and target, including nested sources, yields all physical names with aliases separate and false identities excluded."
    - "D-04/D-17: every static D-04 form has an explicit expected physical-table record -- text, count, source order, and start/end (row, column) -- in run_sql_query_capture.sh, so the grammar task cannot self-certify against stale assertions."
    - "D-08: PREPARE exposes its source as sql_dynamic_source and never as a table; unsupported forms remain visible in D-13 tails."
    - "D-03/D-17: the query contract remains direct and limited to locked public roles."
    - "SQL-07/FORK-03: grammar/query changes and fork-local gate changes are delivered in separate path-family commits; the two fork-local gate extensions are themselves separate commits."
  artifacts:
    - {path: grammar.js, provides: "Complete procedure SQL table-bearing and dynamic grammar"}
    - {path: test/corpus/exec_sql.txt, provides: "Exact DML, DDL, nested, dynamic, and fallback trees"}
    - {path: queries/sql.scm, provides: "Final compiled direct-capture contract including sql_dynamic_source"}
    - {path: run_sql_query_capture.sh, provides: "Fork-local exact static physical-table records plus exact dynamic-source capture and negative-table assertions"}
  key_links:
    - {from: sql_unparsed_tail, to: "modeled FROM/JOIN/INTO/UPDATE/USING/TABLE clauses", via: "interleaved bounded elements with modeled anchors taking precedence", pattern: "sql_unparsed_tail"}
    - {from: test/corpus/exec_sql.txt, to: run_sql_query_capture.sh, via: "post-regeneration exact static physical-table record gate", pattern: "sql_table_name|@table"}
    - {from: queries/sql.scm, to: run_sql_query_capture.sh, via: "post-regeneration exact @dynamic_source capture gate", pattern: "sql_dynamic_source|@dynamic_source"}
---
<objective>
Expand procedure SQL from the tracer to every required static table-bearing form and the bounded dynamic-source contract.

Purpose: complete physical DB2 table extraction without taking on deferred full SQL semantics.
Output: complete procedure grammar, generated artifacts, direct query roles, and two separately committed fork-local gate extensions -- exact static physical-table records and the exact dynamic-source contract.
</objective>
<execution_context>
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/04-exec-sql-blocks/04-CONTEXT.md
@.planning/phases/04-exec-sql-blocks/04-RESEARCH.md
@.planning/phases/04-exec-sql-blocks/04-PATTERNS.md
@.planning/phases/04-exec-sql-blocks/04-04-SUMMARY.md
@grammar.js
@test/corpus/exec_sql.txt
@queries/sql.scm
@run_sql_query_capture.sh
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Extract every static DML and DDL physical table</name>
  <reversibility rating="costly">D-05 and D-06 fix full-name and separate-alias spans as the durable query contract.</reversibility>
  <files>grammar.js, test/corpus/exec_sql.txt, src/parser.c, src/grammar.json, src/node-types.json</files>
  <read_first>
    grammar.js:2246-2423 -- PATTERNS interleaved modeled clauses and bounded tail chunks that keep later anchors reachable
    grammar.js:2321-2352 -- PATTERNS named-node plus static precedence for table and alias roles
    test/corpus/exec_sql.txt -- the existing SELECT, CTE, DATA DIVISION, and include fixtures this task extends
  </read_first>
  <behavior>
    - SELECT FROM/JOIN, INSERT INTO, UPDATE, DELETE FROM, MERGE INTO/USING, and CREATE/ALTER/DROP TABLE expose every static target/source.
    - Nested SELECT and derived-table bodies expose base physical tables while D-18 filtering removes same-statement CTE references.
    - Multiple and duplicate references retain source order; qualified objects exclude aliases.
  </behavior>
  <action>Write one failing exact-tree case for each D-04 form and nested/multiple sources. Build interleaved modeled clauses and bounded tail chunks so unsupported projection/predicate text cannot consume later table anchors. Keep one exec_sql_statement per D-02. Apply D-05 whole qualified names, D-06 sibling aliases, and D-07 exclusion roles for CTE/derived aliases; preserve nested bases and D-18 gate inputs. Extend direct D-17 patterns only for locked roles. Regenerate generated artifacts and commit only grammar-family paths under SQL-07/FORK-03.</action>
  <verify><automated>node_modules/tree-sitter-cli/tree-sitter generate &amp;&amp; node_modules/tree-sitter-cli/tree-sitter test -e '^comment$' &amp;&amp; sh run_sql_query_capture.sh</automated></verify>
  <acceptance_criteria>
    - Every D-04 form has at least one exact expected tree in test/corpus/exec_sql.txt.
    - Qualified names keep whole spans, aliases are siblings, and derived/CTE aliases are excluded, as shown by the exact trees.
    - Unsupported projection or predicate text never consumes a later FROM/JOIN anchor.
    - Multiple and duplicate references retain source order in the exact trees.
    - The pre-existing run_sql_query_capture.sh gate still exits zero, proving no regression of the 04-01/04-04 assertions; exact physical-table output for the new D-04 forms is asserted in Task 3, not here.
    - The commit contains only grammar-family paths.
  </acceptance_criteria>
  <done>Every D-04 form has exact trees, including nested and multiple sources, and the existing gate is unbroken.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Preserve PREPARE source, bounded tails, and the compiled dynamic query role</name>
  <files>grammar.js, test/corpus/exec_sql.txt, queries/sql.scm, src/parser.c, src/grammar.json, src/node-types.json</files>
  <read_first>
    grammar.js:2398-2423 -- PATTERNS bounded operand tokens and the named unparsed-tail rule
    queries/sql.scm -- the direct-capture contract this task completes with @dynamic_source
  </read_first>
  <behavior>
    - PREPARE statement-name FROM :host-variable emits exactly one sql_dynamic_source and no table node.
    - Unsupported utility/clause content remains in named sql_unparsed_tail while later modeled table anchors still fire.
    - END_EXEC and following COBOL never enter a dynamic-source or tail span.
    - The regenerated grammar compiles the direct sql_dynamic_source @dynamic_source query pattern.
  </behavior>
  <action>Per D-08, guarantee PREPARE's clearly delimited source operand and leave other dynamic forms in D-13's named tail unless executable delimiter evidence supports the same public node. Per D-03, keep columns, predicates, expressions, and host variables out of new semantic nodes. Add exact dynamic text and position corpus trees and a case where unsupported content precedes a required table anchor. After regeneration makes sql_dynamic_source available, extend queries/sql.scm per D-17 with the direct `(sql_dynamic_source) @dynamic_source` pattern, confirm all locked direct patterns remain, and compile the query against the corpus. Keep SQL-05 retired, regenerate src artifacts, and keep this task commit entirely grammar-family. Do not modify run_sql_query_capture.sh in this task.</action>
  <verify><automated>node_modules/tree-sitter-cli/tree-sitter generate &amp;&amp; node_modules/tree-sitter-cli/tree-sitter test -e '^comment$' &amp;&amp; node_modules/tree-sitter-cli/tree-sitter query queries/sql.scm test/corpus/exec_sql.txt &gt;/dev/null</automated></verify>
  <done>Dynamic and unsupported SQL stay visible and bounded, and the complete direct query contract compiles against regenerated artifacts.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Activate exact static physical-table assertions in the fork-local gate</name>
  <files>run_sql_query_capture.sh</files>
  <read_first>
    run_sql_query_capture.sh -- the gate after 04-02 and 04-04, its role-assertion extension points, and its statement-scoped CTE subtraction
    run_sql_query_capture.sh -- the 04-04 Task 3 include-assertion block, whose expected-record shape this task mirrors for static table roles
    test/corpus/exec_sql.txt -- the exact D-04 trees Task 1 committed, which fix the expected text and positions
  </read_first>
  <behavior>
    - INSERT INTO, UPDATE, DELETE FROM, MERGE INTO with its USING source, and CREATE/ALTER/DROP TABLE each emit their expected physical-table records with exact text, exact count, exact source order, and exact start/end (row, column) positions.
    - Nested SELECT and derived-table subquery sources contribute their base physical tables, in source order, at their own exact positions.
    - MERGE USING sources appear as physical tables distinct from the MERGE INTO target and in the correct relative source order.
    - Qualified names retain whole spans and their sibling aliases never appear in the physical-table result.
    - A missing, extra, duplicated, reordered, retextualized, or position-shifted physical-table record fails the gate.
  </behavior>
  <action>After Task 1 has added the static D-04 nodes and Task 2 has regenerated the parser and completed queries/sql.scm, modify only run_sql_query_capture.sh in a separate fork-local commit per D-04, D-05, D-06, D-07, D-17, D-18, and SQL-07/FORK-03. Mirror the expected-record shape 04-04 Task 3 established for include roles: add an explicit expected physical-table record set for every D-04 form introduced by Task 1 -- INSERT INTO, UPDATE, DELETE FROM, MERGE INTO target, MERGE USING source, CREATE TABLE, ALTER TABLE, and DROP TABLE -- plus nested-subquery and derived-table base sources. For each record compare exact text, exact total count per statement, source order, and exact start/end (row, column) pairs as emitted by tree-sitter-cli 0.24.5 query output; do not assert byte offsets. Assert the sibling aliases of qualified names are absent from the physical-table result. Preserve every 04-02 containment and CTE-subtraction assertion and every 04-04 include assertion unchanged. Do not add dynamic-source assertions here; those remain Task 4's separate fork-local commit. Regenerate the parser and compile queries/sql.scm before executing the expanded gate so assertions cannot run against stale generated artifacts.</action>
  <verify><automated>node_modules/tree-sitter-cli/tree-sitter generate &amp;&amp; node_modules/tree-sitter-cli/tree-sitter query queries/sql.scm test/corpus/exec_sql.txt &gt;/dev/null &amp;&amp; sh run_sql_query_capture.sh &amp;&amp; sh -n run_sql_query_capture.sh</automated></verify>
  <acceptance_criteria>
    - Every D-04 form introduced by Task 1 has at least one explicit expected physical-table record asserting text, count, source order, and start/end (row, column).
    - Nested subquery sources and MERGE USING sources each have their own expected records.
    - Aliases of qualified names are asserted absent from the physical-table result.
    - All 04-02 containment/CTE and 04-04 include assertions still run and still pass.
    - The commit contains only run_sql_query_capture.sh.
  </acceptance_criteria>
  <done>The separately committed fork-local gate proves exact physical-table identity, multiplicity, order, and position for every static D-04 form, so Task 1's grammar work is mechanically checked rather than self-certified.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 4: Activate exact dynamic-source assertions in the fork-local gate</name>
  <files>run_sql_query_capture.sh</files>
  <read_first>
    run_sql_query_capture.sh -- the gate after 04-02, 04-04, and Task 3, including include assertions, static physical-table records, and CTE subtraction
    queries/sql.scm -- the @dynamic_source pattern added by Task 2
  </read_first>
  <behavior>
    - The PREPARE fixture emits exactly one @dynamic_source with the expected host-variable text and start/end (row, column) positions.
    - The dynamic source produces no @table capture and no physical-table result.
    - A wrong dynamic identity, duplicate, missing capture, shifted position, or table misclassification fails the gate.
  </behavior>
  <action>After Task 2 has added the compiled sql_dynamic_source query role and Task 3 has landed the static physical-table records, modify only run_sql_query_capture.sh in a separate fork-local commit per D-08, D-17, and SQL-07/FORK-03. Add an explicit expected dynamic-source capture record for PREPARE, compare exact text, count, and start/end (row, column) positions as emitted by tree-sitter-cli 0.24.5 query output, and assert that the source appears in neither raw @table output nor the statement's physical-table result. Preserve all include assertions from 04-04, all static physical-table records from Task 3, and the existing statement-scoped CTE subtraction. Regenerate the parser and compile queries/sql.scm before executing the expanded gate so assertions cannot run against stale generated artifacts.</action>
  <verify><automated>node_modules/tree-sitter-cli/tree-sitter generate &amp;&amp; node_modules/tree-sitter-cli/tree-sitter query queries/sql.scm test/corpus/exec_sql.txt &gt;/dev/null &amp;&amp; sh run_sql_query_capture.sh &amp;&amp; sh -n run_sql_query_capture.sh</automated></verify>
  <done>The separately committed fork-local gate rejects every dynamic-source text/count/position mismatch and every dynamic-to-table false positive after regeneration and query compilation.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| SQL tokens -> modeled clauses | Untrusted text can masquerade as aliases/tables or consume anchors. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-08 | Spoofing | table classification | high | mitigate | Exact positive/negative query assertions for every static and dynamic form, landed as explicit expected physical-table records in the fork-local gate (Task 3) rather than asserted by the grammar task itself. |
| T-04-18 | Denial of Service | nested rules | medium | mitigate | Bounded recursion, static precedence, and corpus runtime. |
| T-04-SC | Tampering | supply chain | low | accept | No package installation. |
</threat_model>

## Artifacts this phase produces

- Grammar nodes/rules: `exec_sql_statement`, `exec_sql_declare_section`, `exec_sql_include`, `sql_table_name`, `sql_table_alias`, `sql_include_name`, `sql_dynamic_source`, `sql_unparsed_tail`, structured CTE-definition and source-candidate rules, plus private clause helpers.
- Repository paths: `test/corpus/exec_sql.txt`, `queries/sql.scm`, `run_sql_query_capture.sh`, `run_sql_tail_census.sh`, `docs/sql-tail-census.md`, `.githooks/commit-separability.sh`.
- Generated/vendored paths: `src/parser.c`, `src/grammar.json`, `src/node-types.json`, `forest-shim/cobol/parser.c`, `forest-shim/cobol/grammar.json`, `forest-shim/cobol/sql.scm`.
- Cross-repo path under FORK-03: `/Users/e1001547-mbp-it/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go`, committed only in gortex.
<verification>Run generation, the complete non-comment corpus, and the exact SQL query gate after each task.</verification>
<success_criteria>All required static physical tables are queryable while dynamic and unsupported content obey the bounded contract.</success_criteria>
<output>Create `.planning/phases/04-exec-sql-blocks/04-05-SUMMARY.md` when done.</output>
