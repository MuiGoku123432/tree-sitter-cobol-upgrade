---
phase: 04-exec-sql-blocks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [grammar.js, test/corpus/exec_sql.txt, queries/sql.scm, src/parser.c, src/grammar.json, src/node-types.json]
autonomous: true
requirements: [SQL-01, SQL-02, SQL-03, SQL-05, SQL-07]
estimate: {tokens: 46000, raw_tokens: 46000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-01/D-02/D-13: bounded EXEC SQL SELECT parsing is the durable contract, with required END-EXEC outside a named tail; SQL-05 remains retired."
    - "D-04/D-05/D-06/D-18: raw query output carries an explicit @statement range plus role captures, enabling unambiguous smallest-enclosing-statement assignment."
    - "D-15/SQL-07: fixtures are invented, and every task commit in this plan contains only grammar-family paths, enforced by the installed .githooks/pre-push commit-separability guard."
  artifacts:
    - {path: test/corpus/exec_sql.txt, provides: "Exact-tree tracer and CTE/terminator cases"}
    - {path: queries/sql.scm, provides: "Compiled direct captures for SQL roles present after 04-01; later plans extend it when their nodes exist"}
    - {path: grammar.js, provides: "Bounded procedure EXEC SQL grammar"}
  key_links:
    - {from: grammar.js, to: queries/sql.scm, via: "(exec_sql_statement) @statement plus table, alias, CTE-definition, and source-candidate roles", pattern: "@statement|cte_definition|source_candidate"}
---
<objective>
Prove the grammar-family half of one production EXEC SQL path from exact COBOL fixture through generated AST to raw SQL query captures while recording D-01 before implementation.

Purpose: establish the bounded public AST plus D-18 evidence roles.
Output: procedure SELECT grammar, generated parser, exact corpus, and published query.

FORK-02 and FORK-03 are enforced by the already-installed `.githooks/pre-push` hook
(`git config core.hooksPath` is `.githooks`), which runs `estate-guard-selftest.sh`,
`estate-guard.sh`, and `commit-separability.sh` on every push. This plan adds no
range-evidence artifact and no history audit; the per-task discipline below is that
each commit touches exactly one path family -- grammar family (`grammar.js`, `src/`,
`test/corpus/`, `queries/`) or fork-local (`forest-shim/`, `docs/`, `.planning/`,
`.githooks/`, `run_*.sh`, `.github/workflows/fork-checks.yml`).
</objective>
<execution_context>
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-exec-sql-blocks/04-CONTEXT.md
@.planning/phases/04-exec-sql-blocks/04-RESEARCH.md
@.planning/phases/04-exec-sql-blocks/04-PATTERNS.md
@grammar.js
@queries/cics.scm
@test/corpus/exec_cics.txt
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Parse one SELECT and preserve its CTE and terminator boundaries</name>
  <reversibility rating="costly">D-01, D-05, D-06, and D-18 establish public AST identities.</reversibility>
  <read_first>
    grammar.js:1366-1409 -- PATTERNS _statement registration list where exec_sql_statement becomes a peer
    grammar.js:2246-2423 -- PATTERNS exec_cics_statement required terminator, bounded operand tokens, and named unparsed tail
    grammar.js:2321-2352 -- PATTERNS dedicated named-node plus static precedence convention for extracted identifiers
    test/corpus/exec_cics.txt:1-75 -- PATTERNS fixture structure, exact-tree assertion, and keyword-collision cases
    test/corpus/exec_cics.txt:224-273 -- PATTERNS tail-boundary case proving the terminator stays outside the tail
  </read_first>
  <files>grammar.js, test/corpus/exec_sql.txt, src/parser.c, src/grammar.json, src/node-types.json</files>
  <behavior>
    - A procedure SELECT parses as one exec_sql_statement with kind SELECT and required END_EXEC outside sql_unparsed_tail.
    - APP.CUSTOMER is one sql_table_name span and its alias is a separate sql_table_alias.
    - CTE definitions and source candidates are distinct named nodes; nested physical sources remain visible.
    - Adjacent statements may reuse one CTE name without sharing scope, and role spans at statement boundaries remain distinguishable.
    - Unsupported content, lowercase, multiline input, and a following paragraph remain bounded without ERROR or MISSING nodes.
  </behavior>
  <action>Record SQL-01 and D-01 in the corpus header before grammar work and record SQL-05 as retired with no replacement metric. Write failing exact trees, then register exec_sql_statement beside exec_cics_statement per D-02. Anchor only on EXEC SQL; require END_EXEC outside the body per D-13; keep punctuation outside. Implement only D-03's locked roles: kind, whole qualified sql_table_name per D-05, sibling sql_table_alias per D-06, named sql_unparsed_tail, and structured CTE-definition/source-candidate nodes for D-18. Cover the three locked CTE shapes, adjacent EXEC SQL statements reusing the same CTE name, nested subqueries, role spans at statement start/end boundaries, two- and three-part names, lexer-safe delimited names, empty-source utility SQL, source order, collisions with ordinary COBOL DELETE/MERGE/ALTER/OPEN/CLOSE, and following-paragraph containment. Follow D-15 with invented neutral text. Do not add a scanner, query predicates, or deferred column/expression nodes. Generate src artifacts only with the pinned CLI. Commit only grammar-family paths per SQL-07/FORK-03.</action>
  <verify><automated>node_modules/tree-sitter-cli/tree-sitter generate &amp;&amp; node_modules/tree-sitter-cli/tree-sitter test -e '^comment$'</automated></verify>
  <acceptance_criteria>
    - SQL-01/D-01 and the SQL-05 retirement are recorded in the corpus header before any grammar edit.
    - Every locked D-03 role has an exact expected tree, including the three CTE shapes and adjacent same-name statements.
    - Ordinary COBOL DELETE, MERGE, ALTER, OPEN, and CLOSE retain their existing node types in a collision fixture.
    - No ERROR or MISSING node appears in any SQL fixture, and the sentence period stays outside the SQL node.
    - The commit contains only grammar-family paths.
  </acceptance_criteria>
  <done>Exact trees prove one bounded SELECT path, CTE role structure, complete identifier spans, separate aliases, and recovery containment.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Publish and compile the direct SQL capture contract</name>
  <read_first>
    queries/cics.scm:1-8,21-30 -- PATTERNS no-predicate, one-role-per-pattern direct capture convention
    queries/idms.scm:2-20 -- PATTERNS depth-independent named-node capture convention
    test/corpus/exec_sql.txt -- the Task 1 fixtures this query is compiled against
  </read_first>
  <files>queries/sql.scm</files>
  <behavior>
    - `(exec_sql_statement) @statement` emits the `start: (row, column)` / `end: (row, column)` range used to scope every role capture.
    - Direct patterns expose table, alias, CTE-definition, and source-candidate roles without predicates.
    - Query compilation emits distinguishable (row, column) statement ranges for adjacent statements and enclosing ranges for nested role captures.
  </behavior>
  <action>After Task 1 regenerates the grammar, create queries/sql.scm per D-17 with `(exec_sql_statement) @statement` and one direct named-node pattern for each role that exists after 04-01, with no predicates: sql_table_name, sql_table_alias, the structured CTE-definition node, and the source-candidate node. The @statement capture is executable identity: 04-02 assigns each role to the unique smallest containing statement range by lexicographic (row, column) comparison; tree-sitter-cli 0.24.5 query output emits (row, column) positions, not byte offsets. Capture raw D-18 candidates separately from CTE definitions; do not claim raw candidates are the final physical set. Leave D-12 include and D-08 dynamic-source patterns to the plans that create those node types so this query compiles at every committed stage. Keep this commit entirely in the grammar family per SQL-07/FORK-03.</action>
  <verify><automated>node_modules/tree-sitter-cli/tree-sitter generate &amp;&amp; Q="$(mktemp)" &amp;&amp; { node_modules/tree-sitter-cli/tree-sitter query queries/sql.scm test/corpus/exec_sql.txt &gt; "$Q" &amp;&amp; grep -q 'capture: .* - statement,' "$Q" &amp;&amp; grep -q 'capture: .* - table,' "$Q" &amp;&amp; grep -q 'capture: .* - alias,' "$Q" &amp;&amp; grep -q 'capture: .* - cte_definition,' "$Q" &amp;&amp; grep -q 'capture: .* - source_candidate,' "$Q"; ST=$?; rm -f "$Q"; test "$ST" -eq 0; }</automated></verify>
  <acceptance_criteria>
    - The query compiles against the regenerated grammar with no predicates.
    - statement, table, alias, cte_definition, and source_candidate captures all appear in the compiled output.
    - Include and dynamic-source patterns are absent, so the query compiles at this committed stage.
    - The commit contains only queries/sql.scm.
  </acceptance_criteria>
  <done>The published query compiles and emits every raw role needed for exact fork-local physical-table filtering.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| COBOL/SQL source -> parser | Untrusted tokens control nesting, termination, and names. |
| AST -> query captures | Named roles become extraction evidence. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Tampering | SQL boundary | high | mitigate | Exact terminator/following-paragraph fixtures and required END_EXEC. |
| T-04-02 | Spoofing | CTE/table roles | high | mitigate | Distinct statement-scoped definitions/candidates and exact query compilation. |
| T-04-03 | Information Disclosure | corpus | high | mitigate | Invented neutral fixtures per D-15 plus the installed pre-push estate guard. |
| T-04-SC | Tampering | supply chain | low | accept | No package installation; use lockfile-pinned CLI. |
</threat_model>

## Artifacts this phase produces

- Grammar nodes/rules: `exec_sql_statement`, `exec_sql_declare_section`, `exec_sql_include`, `sql_table_name`, `sql_table_alias`, `sql_include_name`, `sql_dynamic_source`, `sql_unparsed_tail`, structured CTE-definition and source-candidate rules, plus private clause helpers.
- Repository paths: `test/corpus/exec_sql.txt`, `queries/sql.scm`, `run_sql_query_capture.sh`, `run_sql_tail_census.sh`, `docs/sql-tail-census.md`, `.githooks/commit-separability.sh`.
- Generated/vendored paths: `src/parser.c`, `src/grammar.json`, `src/node-types.json`, `forest-shim/cobol/parser.c`, `forest-shim/cobol/grammar.json`, `forest-shim/cobol/sql.scm`.
- Cross-repo path under FORK-03: `/Users/e1001547-mbp-it/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go`, committed only in gortex.
<verification>Run generation, full non-comment corpus tests, and the exact raw capture command above.</verification>
<success_criteria>The bounded SQL AST and published raw capture roles are stable, queryable, and committed only with grammar-family files.</success_criteria>
<output>Create `.planning/phases/04-exec-sql-blocks/04-01-SUMMARY.md` when done.</output>
