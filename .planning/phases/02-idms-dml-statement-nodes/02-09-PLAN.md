---
phase: 02-idms-dml-statement-nodes
plan: 09
type: execute
wave: 8
depends_on: ["02-08"]
files_modified:
  - grammar.js
  - src/parser.c
  - src/grammar.json
  - src/node-types.json
  - test/corpus/idms_navigation.txt
  - test/corpus/idms_session.txt
  - test/corpus/idms_accept.txt
  - queries/idms.scm
autonomous: true
gap_closure: true
requirements: [IDMS-01, IDMS-02, IDMS-06, IDMS-07]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "T-02-R06/CR-03 is closed: all 14 IDMS statements emit one exact token-valued `@verb`, including ACCEPT as exactly `ACCEPT` (D-04, IDMS-02)."
    - "READY's optional operand is `idms_area_name`; OWNER and CONNECT/DISCONNECT are set contexts using `idms_set_name`; navigation WITHIN/CURRENT WITHIN uses neutral `idms_scope_name` because syntax cannot distinguish set from area."
    - "No identifier-spelling heuristic labels a navigation scope as a set or area, and neither `idms_area_name` nor `idms_scope_name` emits `@record` or `@set`."
    - "The gate-owned temporary positive subject has no parse error or missing-node marker, and two exact-gate runs produce byte-identical ordered capture output."
    - "ANY, DUPLICATE, FIRST, LAST, PRIOR, numeric selectors, and PAGE-INFO are corpus- and exact-gate-covered; valid forms never degrade into a false record/set edge."
    - "The existing four public statement classes and record/set node names remain stable; generated artifacts are owned sequentially after 02-08 and committed with grammar-family files only."
  artifacts:
    - path: "grammar.js"
      provides: "Visible verb fields and syntax-grounded area/set/scope operands"
      contains: "idms_scope_name"
    - path: "queries/idms.scm"
      provides: "Exact verb plus semantically safe record/set capture contract"
      contains: "@verb"
    - path: "test/corpus/idms_navigation.txt"
      provides: "OWNER set and neutral WITHIN scope cases"
    - path: "test/corpus/idms_session.txt"
      provides: "READY area case"
    - path: "test/corpus/idms_accept.txt"
      provides: "Visible ACCEPT verb-token case"
    - path: "src/parser.c"
      provides: "Generated semantic parser"
    - path: "src/node-types.json"
      provides: "Public verb fields, area node, and scope node"
  key_links:
    - from: "run_idms_query_capture.sh"
      to: "queries/idms.scm"
      via: "the gate-owned temporary subject validates exact 14-verb and operand-role output"
      pattern: "tree-sitter.*query"
    - from: "queries/idms.scm"
      to: "grammar.js"
      via: "published query patterns bind the direct visible verb fields and named operand nodes"
      pattern: "verb: \(_\) @verb"
    - from: "test/corpus/idms_navigation.txt"
      to: "grammar.js"
      via: "OWNER proves a set context while WITHIN proves neutral scope semantics"
      pattern: "idms_scope_name"
    - from: "grammar.js"
      to: "src/parser.c"
      via: "pinned generation updates parser.c, grammar.json, and node-types.json after 02-08"
      pattern: "idms_scope_name"
---

<objective>
Close T-02-R06 and the minimum operand-role correctness required for IDMS-01/02 after update syntax is safe.

Purpose: Consume Wave 7's corrected update parser and Wave 6's exact RED gate. This Wave 8 grammar/query plan exposes every verb token exactly and uses syntax, not identifier spelling, to distinguish READY area, known set contexts, and ambiguous navigation scope.

Output: Corrected published query contract, semantic corpus fixtures, and sequentially regenerated source artifacts in grammar-family-only commits.
</objective>

<execution_context>
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-idms-dml-statement-nodes/02-CONTEXT.md
@.planning/phases/02-idms-dml-statement-nodes/02-RESEARCH.md
@.planning/phases/02-idms-dml-statement-nodes/02-04-SUMMARY.md
@.planning/phases/02-idms-dml-statement-nodes/02-07-SUMMARY.md
@.planning/phases/02-idms-dml-statement-nodes/02-08-SUMMARY.md
@.planning/phases/02-idms-dml-statement-nodes/02-REVIEW.md
@.planning/phases/02-idms-dml-statement-nodes/02-SECURITY.md
@.planning/phases/02-idms-dml-statement-nodes/02-VERIFICATION.md
</context>

## Artifacts this phase produces

- New public nodes `idms_area_name` and `idms_scope_name`.
- Modified `idms_navigation_statement`, `idms_session_statement`, and `idms_accept_statement` verb/operand shapes; 02-08's corrected `idms_update_statement` remains intact except for any direct visible verb-field normalization required across all four classes.
- Modified `queries/idms.scm` exact verb and record/set patterns.
- Updated navigation/session/accept corpus fixtures.
- Regenerated `src/parser.c`, `src/grammar.json`, and `src/node-types.json`.
- No tracked exact-query fixture: `run_idms_query_capture.sh` remains sole fixture/oracle authority.

## Multi-Source Coverage Audit

| SOURCE | ID | Item | Plan | Status | Notes |
|--------|----|------|------|--------|-------|
| GOAL | -- | Queryable, semantically safe IDMS nodes | 02-09 | COVERED | Exact verbs and safe operands. |
| REQ | IDMS-01 | Valid named statement nodes | 02-08, 02-09 | COVERED | Update then other classes. |
| REQ | IDMS-02 | Correct verb/record/set query | 02-09 | COVERED | Exact gate. |
| REQ | IDMS-06 | Corpus regression | 02-09, 02-11 | COVERED | Focused then final. |
| REQ | IDMS-07 | Neutral fixtures/separable delivery | 02-09 | COVERED | Grammar family only. |
| CONTEXT | D-01/D-02/D-04/D-10 | Four classes, named operands, fork query, neutral source | 02-09 | COVERED | Stable contracts retained. |
| VERIFICATION | T-02-R06/CR-03 | Missing/oversized verbs | 02-09 | COVERED | Exact token stream. |
| VERIFICATION | minimum WR-01/WR-06 | False area/set edges | 02-09 | COVERED | Syntax-grounded subset only. |

<tasks>

<task type="tracer">
  <name>Task 1: Make every IDMS verb a visible exact field</name>
  <precondition>02-08 completed; `--invalid-only` is GREEN; and `./run_idms_query_capture.sh --expect-red=verbs` succeeds only for exact pre-fix verb diagnostics while tolerating the enumerated role defects.</precondition>
  <reversibility rating="costly">The four public statement names and exact verb stream form the published query/shim contract exercised by the exact gate.</reversibility>
  <files>grammar.js, queries/idms.scm, test/corpus/idms_accept.txt, src/parser.c, src/grammar.json, src/node-types.json</files>
  <read_first>
    - `run_idms_query_capture.sh` exact oracle and staged `--expect-red=verbs` / `--expect-red=roles` contracts
    - `queries/idms.scm` current missing/oversized verb patterns
    - `grammar.js` IDMS statement rules and ACCEPT hidden token
    - `test/corpus/idms_accept.txt`
    - `.planning/phases/02-idms-dml-statement-nodes/02-REVIEW.md` CR-03
  </read_first>
  <behavior>
    - Each of the 14 valid statements yields exactly one `@verb` whose text is the verb token.
    - ACCEPT yields only `ACCEPT`, not its statement span.
    - Update/session statements no longer omit verbs.
    - Standard COBOL ACCEPT trees remain unchanged.
  </behavior>
  <action>
    Run `--expect-red=verbs` first. Give every statement alternative a visible direct verb field. Expose ACCEPT with a generator-supported alias of the hidden token while leaving standard `accept_statement` byte-stable. Rewrite `queries/idms.scm` so each statement pattern captures its direct verb child. Update the ACCEPT corpus expected tree and regenerate. Then require `--expect-red=roles` to succeed: all verb expectations are now GREEN and only the exact enumerated role/selector/PAGE-INFO defects remain. Do not require normal mode yet; Task 2 owns role GREEN. Do not alter gate fixture authority or add a tracked query sample.
  </action>
  <acceptance_criteria>
    - Exactly 14 ordered one-token verb rows match the required values.
    - ACCEPT spans exactly six characters/text `ACCEPT`.
    - Positive subject has no parse error or missing-node marker.
    - Standard ACCEPT corpus remains green.
    - Generated node types expose a verb field on all four statement classes.
    - `shasum -a 256 run_idms_query_capture.sh` equals the 02-07 recorded hash for both verbs-RED and roles-RED evidence.
    - `--expect-red=roles` exits 0 after verb repair and proves exactly the enumerated role/selector/PAGE-INFO defects remain; normal mode is not claimed GREEN until Task 2.
  </acceptance_criteria>
  <verify>
    <automated>node_modules/tree-sitter-cli/tree-sitter generate &amp;&amp; node_modules/.bin/tree-sitter test -i 'idms accept' &amp;&amp; ./run_idms_query_capture.sh --expect-red=roles</automated>
  </verify>
  <done>T-02-R06 verb capture is GREEN for all 14 exact tokens, including token-scoped ACCEPT.</done>
</task>

<task type="auto">
  <name>Task 2: Encode syntax-grounded area, set, and neutral scope roles</name>
  <files>grammar.js, queries/idms.scm, test/corpus/idms_navigation.txt, test/corpus/idms_session.txt, src/parser.c, src/grammar.json, src/node-types.json</files>
  <read_first>
    - `grammar.js` session/navigation rules
    - `test/corpus/idms_navigation.txt` and `test/corpus/idms_session.txt`
    - `run_idms_query_capture.sh` role oracle
    - `.planning/phases/02-idms-dml-statement-nodes/02-RESEARCH.md` READY and navigation syntax
    - `.planning/phases/02-idms-dml-statement-nodes/02-VERIFICATION.md` operand-role blocker
  </read_first>
  <behavior>
    - READY operand is `idms_area_name`, never record/set.
    - OWNER WITHIN and CONNECT/DISCONNECT retain `idms_set_name`/`@set` because syntax defines a set context.
    - Navigation WITHIN/CURRENT WITHIN becomes `idms_scope_name` and emits no record/set because syntax allows set or area identically.
    - CALC/ANY/DUPLICATE record forms emit the actual record as `@record`; FIRST/LAST/NEXT/PRIOR/numeric WITHIN forms emit an optional record plus neutral scope, never a guessed set; DB-KEY PAGE-INFO emits its record but neither DB-key nor page-info operand as record/set.
    - No suffix/name heuristic exists.
    - Two exact-gate runs are byte-identical and retain all 14 verbs.
  </behavior>
  <action>
    Add `idms_area_name` for READY and `idms_scope_name` for navigation WITHIN/CURRENT WITHIN. Preserve `idms_set_name` only in syntactically known set contexts, including OWNER and 02-08's CONNECT/DISCONNECT. Model and corpus-test the documented ANY, DUPLICATE, FIRST, LAST, PRIOR, numeric-selector, and PAGE-INFO alternatives with syntax-grounded operand nodes; each valid form must parse cleanly and the exact gate must prove it cannot create false record/set captures. Exclude area/scope nodes from record/set query patterns and restrict session record capture structurally to BIND record. Start by requiring `--expect-red=roles`, then update neutral corpus expectations, regenerate sequentially after Task 1, and run normal exact mode twice with `IDMS_CAPTURE_OUT` files in external scratch, byte-comparing them before cleanup. Add no identifier-suffix/name matching anywhere.
  </action>
  <acceptance_criteria>
    - Generated node types contain named area and scope nodes.
    - READY area and navigation scope produce zero record/set rows.
    - OWNER and CONNECT/DISCONNECT preserve set rows; valid records remain captured.
    - Search of grammar/gate logic finds no suffix-based role classification.
    - Corpus and exact gate include separate named cases for ANY, DUPLICATE, FIRST, LAST, PRIOR, numeric selector, and PAGE-INFO, with exact expected captures and no false record/set values.
    - Exact output is byte-identical across two runs; it includes all 14 one-token verb values in required source order, exact record/set controls, area/scope negatives, and no parse error/missing marker.
    - Aggregate IDMS corpus passes and commits contain only grammar-family paths.
    - Normal GREEN evidence uses the same recorded gate-byte hash as both staged RED transitions.
  </acceptance_criteria>
  <verify>
    <automated>node_modules/tree-sitter-cli/tree-sitter generate &amp;&amp; node_modules/.bin/tree-sitter test -i 'idms navigation|idms session|idms accept' &amp;&amp; OUT="$(mktemp -d)" &amp;&amp; { IDMS_CAPTURE_OUT="$OUT/a.tsv" ./run_idms_query_capture.sh &amp;&amp; IDMS_CAPTURE_OUT="$OUT/b.tsv" ./run_idms_query_capture.sh &amp;&amp; cmp "$OUT/a.tsv" "$OUT/b.tsv"; ST=$?; rm -rf "$OUT"; test "$ST" -eq 0; }</automated>
  </verify>
  <done>Operand captures are syntax-grounded: area and ambiguous scope cannot become false record/set edges, while known set contexts remain stable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AST to published query | Missing, oversized, or falsely typed captures violate the query/shim contract. |
| Proprietary corpus to public tests | Only neutral gate-owned/corpus fixtures are permitted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-R06 | Tampering | `queries/idms.scm` and verb fields | high | mitigate | Exact all-14 token stream and ACCEPT span are blocking gate evidence. |
| T-02-R07 | Tampering | operand roles | medium | mitigate | Syntax-grounded area/set/scope nodes, with no spelling heuristic. |
| T-02-02 | Information Disclosure | fixtures | high | mitigate | Hand-written neutral source only; gate scratch remains external. |
| T-02-SC | Supply-chain Tampering | generator | low | accept | Existing pinned CLI only. |
</threat_model>

<verification>
1. Exact gate normal mode passes twice byte-identically.
2. All 14 verbs are exact one-token rows and ACCEPT is token-scoped.
3. Area/scope negatives and known set/record controls match exactly.
4. Focused and aggregate corpus pass; generated artifacts are stable.
</verification>

<success_criteria>
- T-02-R06 is fixed in source and exact evidence.
- No false area/scope record/set capture remains.
- Public class/record/set contracts remain stable for valid contexts.
</success_criteria>

<output>
Create `.planning/phases/02-idms-dml-statement-nodes/02-09-SUMMARY.md` with exact verb rows, role matrix, deterministic output hashes, generator status, and grammar-only commit ranges.
The SUMMARY must include `## Threat Flags` entries for T-02-R01/R03/R05/R06: T-02-R06 has resolved evidence and R01/R03/R05 cite their prior plan evidence; use `none` only for additional threats.
</output>
