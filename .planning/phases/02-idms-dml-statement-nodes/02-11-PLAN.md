---
phase: 02-idms-dml-statement-nodes
plan: 11
type: execute
wave: 10
depends_on: ["02-10"]
files_modified:
  - .planning/phases/02-idms-dml-statement-nodes/02-11-closeout.sh
  - .planning/phases/02-idms-dml-statement-nodes/02-11-EVIDENCE.md
  - .planning/phases/02-idms-dml-statement-nodes/02-SECURITY.md
  - .planning/phases/02-idms-dml-statement-nodes/02-VERIFICATION.md
  - .planning/phases/02-idms-dml-statement-nodes/02-VALIDATION.md
autonomous: false
gap_closure: true
requirements: [IDMS-01, IDMS-02, IDMS-03, IDMS-06, IDMS-07]
# IDMS-04/05/08 are regression evidence only and intentionally omitted from
# gap-plan requirement ownership; independent verification retains their prior status.

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Raw successful-command evidence exists for T-02-R01, T-02-R03, T-02-R05, and T-02-R06; independent agents own closure verdicts."
    - "DCC and approved copied-estate differentials use baseline `39886c7`, explicit roots, fresh external DIFF_TMP, and unconditional cleanup without exposing proprietary rows."
    - "Focused/full corpus, exact NIST final line and allowlist, shim query exercise, target-worktree Gortex cascade, estate guard, and concrete FORK-03 ranges have raw results."
    - "Gortex module resolution is proven to the target worktree shim through isolated GOWORK, never main or gortex's existing workspace."
    - "`02-SECURITY.md` and `02-VERIFICATION.md` are unchanged; execution halts at a blocking human-action checkpoint requiring independent `/gsd-secure-phase 2` to produce `threats_open: 0` before independent phase verification can resume."
  artifacts:
    - path: ".planning/phases/02-idms-dml-statement-nodes/02-11-EVIDENCE.md"
      provides: "Raw commands, statuses, aggregate counts, GOWORK resolution, and commit ranges"
      contains: "39886c7"
    - path: ".planning/phases/02-idms-dml-statement-nodes/02-11-closeout.sh"
      provides: "Reusable executable DCC/estate/NIST/GOWORK/guard closeout harness"
      contains: "cleanup"
  key_links:
    - from: ".planning/phases/02-idms-dml-statement-nodes/02-07-SUMMARY.md"
      to: ".planning/phases/02-idms-dml-statement-nodes/02-11-EVIDENCE.md"
      via: "final reruns map differential threat tests to raw evidence"
      pattern: "T-02-R0(1|3)"
    - from: ".planning/phases/02-idms-dml-statement-nodes/02-10-SUMMARY.md"
      to: ".planning/phases/02-idms-dml-statement-nodes/02-11-EVIDENCE.md"
      via: "delivery and CI results seed target-shim closeout evidence"
      pattern: "refresh|shim|query"
    - from: ".planning/phases/02-idms-dml-statement-nodes/02-05-SUMMARY.md"
      to: ".planning/phases/02-idms-dml-statement-nodes/02-11-EVIDENCE.md"
      via: "the prior cascade contract is rerun under isolated target-worktree module resolution"
      pattern: "TestErrorCascade"
---

<objective>
Produce independent-ready raw closeout evidence without self-certifying security or verification.

Purpose: Wave 10 runs the complete environment-safe gauntlet after tooling, grammar, query, shim, and CI plans. Research questions were resolved during this planning revision, before execution. The executor cannot edit the security/verifier-owned verdict artifacts.

Output: `02-11-EVIDENCE.md` plus mandatory handoff to `/gsd-secure-phase 2`, gsd-verifier, and `/gsd-validate-phase 2`.
</objective>

<execution_context>
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-idms-dml-statement-nodes/02-CONTEXT.md
@.planning/phases/02-idms-dml-statement-nodes/02-07-SUMMARY.md
@.planning/phases/02-idms-dml-statement-nodes/02-10-SUMMARY.md
@.planning/phases/02-idms-dml-statement-nodes/02-SECURITY.md
@.planning/phases/02-idms-dml-statement-nodes/02-VERIFICATION.md
</context>

## Artifacts this phase produces

- New `02-11-EVIDENCE.md` containing raw aggregate evidence only.
- New executable planning-local `02-11-closeout.sh` containing every mandatory closeout command and one signal-safe cleanup trap.
    - `02-SECURITY.md`, `02-VERIFICATION.md`, and validation lifecycle fields remain byte-identical during Task 1; Task 2's independent workflows then own their updates.

## Multi-Source Coverage Audit

| SOURCE | ID | Item | Plan | Status | Notes |
|--------|----|------|------|--------|-------|
| GOAL | -- | Independent-ready Phase 2 evidence | 02-11 | COVERED | No self-verdict. |
| REQ | IDMS-01/02/03/06/07 | Affected closeout requirements | 02-11 | COVERED | Raw reruns for verifier. |
| REQ | IDMS-04/05/08 | Existing verified requirements | 02-11 | COVERED | Regression only, no reimplementation. |
| CONTEXT | D-07/D-08/D-10 | Full differential, manual estate run, privacy | 02-11 | COVERED | Aggregate evidence only. |
| VERIFICATION | T-02-R01/R03/R05/R06 | Four high blockers | 02-11 + independent audits | COVERED | Secure-phase/verifier own verdict. |

<tasks>

<task type="tracer">
  <name>Task 1: Run environment-safe closeout and write raw evidence</name>
  <precondition>02-07 through 02-10 summaries exist; gortex exists at `$HOME/repos/mine/GoApps/gortex`; and commit ranges resolve from summaries. Corpus roots are asserted by the task's halt-capable preflight before any full differential.</precondition>
  <files>.planning/phases/02-idms-dml-statement-nodes/02-11-closeout.sh, .planning/phases/02-idms-dml-statement-nodes/02-11-EVIDENCE.md</files>
  <read_first>
    - `02-07-SUMMARY.md` through `02-10-SUMMARY.md`
    - `02-SECURITY.md` and `02-VERIFICATION.md` read-only
    - `02-04-SUMMARY.md` baseline/differential contract
    - `run_nist_cobol85.sh`, `skip_tests.txt`, and gortex `go.mod`/existing `go.work`
  </read_first>
  <behavior>
    - All four threat tests and fast shim/query tests have raw statuses.
    - DCC/estate use baseline 39886c7 and external scratch.
    - Gortex resolves target shim Dir before cascade.
    - Security, verification, and validation hashes remain unchanged through raw-evidence generation.
    - Missing DCC or approved estate fails preflight with a named setup requirement before any full differential starts.
    - One cleanup trap is installed before all scratch/worktree/link creation and preserves DCC, estate, NIST, GOWORK, and guard statuses.
  </behavior>
  <action>
    Create executable planning-local `02-11-closeout.sh` and evidence slots marked UNVERIFIED. The harness owns all mandatory closeout behavior below, accepts optional `ESTATE_ROOT`, writes aggregate results to `02-11-EVIDENCE.md`, and exits nonzero on any missing/failed gate. Run fast gates first: differential self-test, exact query, refresh self-test, source/shim cmp, shim-query exact gate, shim Go tests. Stop and record raw failure before corpus work if any fails.

    Install one EXIT/INT/TERM cleanup trap before creating any closeout scratch, detached worktree, symlink, NIST capture, allowlist files, or temporary GOWORK. The trap removes only paths this task created and preserves each command's captured status for evidence. Preflight `DCC_ROOT="$HOME/repos/mine/cobolCode/cam-corpus-dcc/DCC"`. Resolve `ESTATE_ROOT` from explicit environment first, then approved sibling copy `/Users/e1001547-mbp-it/repos/mine/devDeps/tree-sitter-cobol-upgrade/estate`. Require DCC readable and estate to contain `endevor/`; if DCC is missing, HALT with its exact required path, and if estate is absent HALT with: `Set ESTATE_ROOT to the approved existing copied estate outside the target worktree; do not copy proprietary content into this repository.`

    For the authoritative estate path, create a detached temporary worktree for the current implementation ref under `/var/folders/ks/sc4623gx7c144xx9_sxj53b00000gn/T/opencode`, record that ref, and create only that worktree's untracked `estate` symlink pointing to the canonical external copy. Pass exactly `$TEMP_WORKTREE/estate` to the differential so its `$TOP_DIR/estate` branch activates. Before comparing parsers, run the selector preflight and require the exact recorded population 3,781 declared members / 1,369 compilable programs (and the established 2,391 ACCEPT denominator in default mode); halt on any mismatch. Never create a link in the target worktree or copy/stage proprietary bytes. The single trap removes the symlink and detached worktree on every path.

    Run exactly: `DCC_TMP=$(mktemp -d); DIFF_TMP="$DCC_TMP" sh run_accept_differential.sh run 39886c7 "$DCC_ROOT"; DCC_ST=$?` and, from the detached current worktree, `ESTATE_TMP=$(mktemp -d); DIFF_TMP="$ESTATE_TMP" sh "$TEMP_WORKTREE/run_accept_differential.sh" run 39886c7 "$TEMP_WORKTREE/estate"; ESTATE_ST=$?`. Preserve both statuses, let the single trap clean all scratch, and require both are 0. Record aggregate summary counts only.

    Run `node_modules/.bin/tree-sitter test -i 'idms'` and `node_modules/.bin/tree-sitter test -e '^comment$'`. Capture `NIST_ST` from `sh run_nist_cobol85.sh` plus its output; require `NIST_ST=0` and exact final line `382 tests. (Success: 371, Fail: 0, Skip: 11)`. Require exactly 11 nonblank skip entries; write the canonical names NC205A, SM101A, SM103A, SM105A, SM107A, SM201A, SM203A, SM205A, SM206A, SM208A, SM401M to a temporary expected file, sort nonblank `skip_tests.txt` rows into a temporary actual file, byte-compare them, and let the single trap clean all scratch.

    Create `TMP_GOWORK_DIR=$(mktemp -d)` under the already-installed single cleanup trap. Run `(cd "$TMP_GOWORK_DIR" &amp;&amp; go work init "$HOME/repos/mine/GoApps/gortex" "/Users/e1001547-mbp-it/repos/mine/devDeps/tree-sitter-cobol-phase2-fixes/forest-shim/cobol")`. Then run `(cd "$HOME/repos/mine/GoApps/gortex" &amp;&amp; GOWORK="$TMP_GOWORK_DIR/go.work" go list -m -json github.com/alexaandru/go-sitter-forest/cobol)` and require `.Dir` equals `/Users/e1001547-mbp-it/repos/mine/devDeps/tree-sitter-cobol-phase2-fixes/forest-shim/cobol`. Only after that run `(cd "$HOME/repos/mine/GoApps/gortex" &amp;&amp; GOWORK="$TMP_GOWORK_DIR/go.work" go test ./internal/parser/forest/cobolprobe/ -run TestErrorCascade -count=2 -args -enhanced-parser)`. Never alter gortex's existing go.work; the task-level trap restores/removes all temporary state.

    Derive each range deterministically from SUMMARY commit lists: `FIRST` is the first implementation commit listed for that plan, `LAST` is the last implementation commit listed, and the inclusive guard range is `$(git rev-parse "$FIRST^")..$LAST`; verify every listed plan commit is contained before running guards. Assign the resulting literals as `RANGE_02_07`, `RANGE_02_08`, `RANGE_02_09`, and `RANGE_02_10`, record them, and run `sh .githooks/commit-separability.sh --range "$RANGE_02_07"` plus `sh .githooks/estate-guard.sh --range "$RANGE_02_07"`, repeating those exact two commands for the other three variables; 02-08/09 must be grammar-family, 02-07/10 fork-local. Run `bash .githooks/estate-guard-selftest.sh`. Executable corpus, exact-query, shim, Go cascade, and guard commands are sufficient evidence for these non-signature edits.

    Before creating evidence, capture SHA-256 values of `02-SECURITY.md`, `02-VERIFICATION.md`, and `02-VALIDATION.md`. Write raw commands/statuses/counts only and confirm those hashes are unchanged through Task 1. End with mandatory `/gsd-secure-phase 2`, then independent Phase 2 gsd-verifier, then `/gsd-validate-phase 2`; do not assign pass/closed/validated status yourself. Final plan acceptance explicitly requires all independent workflows to complete successfully after this executor returns; until then Phase 2 remains blocked.
  </action>
  <acceptance_criteria>
    - Evidence contains exact DCC/estate commands literally, resolved roots, baseline 39886c7, cleanup statuses, and aggregate results.
    - `bash 02-11-closeout.sh` is the canonical automated command and includes every DCC/estate/NIST/GOWORK/guard behavior described in this task.
    - Evidence contains no proprietary relative file path, member/program name, source line, or inventory row.
    - Missing estate halts with the exact setup requirement and no proprietary provision inside target worktree.
    - Estate input passed to the tool is exactly `$TEMP_WORKTREE/estate`; selector output proves 3781/1369 and default ACCEPT denominator before evidence is accepted.
    - Exact NIST final line and canonical 11-name byte comparison succeed.
    - Captured NIST runner status is exactly 0 independently of final-line text.
    - GOWORK module JSON Dir equals target shim before cascade; cleanup is unconditional.
    - One signal-safe task cleanup trap removes every owned scratch/worktree/link while retaining command statuses in evidence.
    - Literal ranges for 02-07 through 02-10 pass both guards with correct families.
    - `bash .githooks/estate-guard-selftest.sh` exits 0 before range evidence is accepted.
    - Security, verification, and validation hashes equal pre-task values through Task 1.
    - Evidence ends with mandatory independent audit/verifier gates.
    - Before the checkpoint, the closeout-harness/evidence implementation commit contains only `02-11-closeout.sh` and `02-11-EVIDENCE.md`; later changes to SECURITY/VERIFICATION/VALIDATION come only from the independent workflows named by the checkpoint.
  </acceptance_criteria>
  <verify>
    <automated>bash .planning/phases/02-idms-dml-statement-nodes/02-11-closeout.sh</automated>
  </verify>
  <done>Raw target-worktree evidence is complete, verdict artifacts are untouched, and closeout remains blocked until independent secure-phase, verifier, and validate-phase runs succeed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Independent security and verification ownership gate</name>
  <what-built>Plans 02-07 through 02-10 and Task 1's raw evidence/summaries exist; the executor has not changed `02-SECURITY.md`, `02-VERIFICATION.md`, or validation lifecycle fields.</what-built>
  <files>.planning/phases/02-idms-dml-statement-nodes/02-SECURITY.md, .planning/phases/02-idms-dml-statement-nodes/02-VERIFICATION.md, .planning/phases/02-idms-dml-statement-nodes/02-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/02-idms-dml-statement-nodes/02-11-EVIDENCE.md`
    - `.planning/phases/02-idms-dml-statement-nodes/02-SECURITY.md`
    - `.planning/phases/02-idms-dml-statement-nodes/02-VERIFICATION.md`
    - `.planning/phases/02-idms-dml-statement-nodes/02-VALIDATION.md`
  </read_first>
  <behavior>
    - Security runs first and must produce `threats_open: 0`.
    - Phase verification cannot run or be accepted before that security condition.
    - Validate-phase runs after verification and alone changes validation lifecycle fields.
  </behavior>
  <action>Stop execution. The user must run independent `/gsd-secure-phase 2`. That workflow must rewrite `02-SECURITY.md` from current implementation/evidence and report zero open blocking threats; this checkpoint is not auto-approvable. After that file check passes, run the independent Phase 2 gsd-verifier so it alone rewrites `02-VERIFICATION.md`. Then run `/gsd-validate-phase 2`; only that workflow may set validated/Nyquist-compliant lifecycle fields in `02-VALIDATION.md`. Roadmap progress is reconciled by phase completion after those independent gates, never by this executor.</action>
  <instructions>
    1. Run `/gsd-secure-phase 2` and wait for its independent audit to finish.
    2. Confirm `02-SECURITY.md` contains `threats_open: 0`; do not resume otherwise.
    3. Run the independent Phase 2 gsd-verifier and wait for it to rewrite `02-VERIFICATION.md`.
    4. Run `/gsd-validate-phase 2`; do not edit validation lifecycle fields manually.
    5. Resume only when the automated checkpoint verification succeeds.
  </instructions>
  <verify>
    <automated>test "$(grep -c '^threats_open: 0$' .planning/phases/02-idms-dml-statement-nodes/02-SECURITY.md)" = 1 &amp;&amp; ! grep -q '^status: gaps_found$' .planning/phases/02-idms-dml-statement-nodes/02-VERIFICATION.md &amp;&amp; grep -q '^status: validated$' .planning/phases/02-idms-dml-statement-nodes/02-VALIDATION.md &amp;&amp; grep -q '^nyquist_compliant: true$' .planning/phases/02-idms-dml-statement-nodes/02-VALIDATION.md</automated>
  </verify>
  <verification>Independent security first, then independent phase verification, then validate-phase; all file-state assertions in the automated check must pass.</verification>
  <acceptance_criteria>
    - `/gsd-secure-phase 2` independently rewrote security evidence and reports zero open blocking threats.
    - Only after security passes, the independent Phase 2 verifier rewrote `02-VERIFICATION.md` from current code/evidence.
    - `/gsd-validate-phase 2` independently set validated/Nyquist-compliant status.
    - This executor did not edit any of those three verdict artifacts.
    - Phase-completion routing, not this executor, reconciles ROADMAP plan/progress state after all three independent gates.
    - Every external workflow invocation occurs only after Task 1 created `02-11-EVIDENCE.md` and `02-11-SUMMARY.md` with raw results.
  </acceptance_criteria>
  <resume-signal>Resume only after all three independent workflows completed and the automated check passes.</resume-signal>
  <done>Independent security, phase verification, and validation own the final verdicts; only then may phase-completion routing reconcile ROADMAP progress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Proprietary corpus to public evidence | Only aggregate results may be recorded. |
| Target shim to gortex | Module resolution must be exact and isolated. |
| Raw evidence to verdict | Executor cannot self-certify security/verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-R01 | Information Disclosure | differential closeout | high | mitigate | Four symlink tests precede full runs; aggregate-only records. |
| T-02-R03 | Tampering / Repudiation | comparator closeout | high | mitigate | Strict TSV/duplicate tests precede full runs. |
| T-02-R04 | Repudiation | differential regex path | medium | mitigate | Malformed snapshot/run regex and extraction failure tests precede full runs. |
| T-02-R05 | Tampering | update grammar | high | mitigate | Invalid matrix rerun through corpus/exact gate. |
| T-02-R06 | Tampering | exact query | high | mitigate | Source and shim all-14 exact gates rerun. |
| T-02-R12 | Repudiation | evidence | medium | mitigate | Exact commands, statuses, aggregate counts, module Dir, and commit ranges; independent verdict owners. |
| T-02-SC | Supply-chain Tampering | verification | low | accept | No dependency added. |
</threat_model>

<verification>
1. Fast threat/delivery gates succeed.
2. DCC/estate runs complete from approved roots with cleanup.
3. Corpus/NIST/allowlist and target-shim cascade succeed.
4. Commit-range guards and isolated target-shim Go module/cascade checks succeed.
5. Verdict artifacts stay unchanged; independent reruns remain mandatory.
</verification>

<success_criteria>
- Four blockers have raw rerunnable evidence.
- No proprietary detail enters tracked evidence.
- Target worktree shim is proven in gortex.
- Executor issues no final verdict.
</success_criteria>

<output>
Create `.planning/phases/02-idms-dml-statement-nodes/02-11-SUMMARY.md` with raw results, literal ranges, GOWORK resolution, unchanged verdict hashes, and mandatory `/gsd-secure-phase 2` → independent verifier → `/gsd-validate-phase 2` routing. Do not state final pass.
The SUMMARY must include `## Threat Flags` entries for all T-02-R01/R03/R05/R06 pointing to raw evidence and explicitly state that independent security owns final resolution; use `none` only for additional threats.
Record the canonical `bash .planning/phases/02-idms-dml-statement-nodes/02-11-closeout.sh` invocation and its status.
</output>
