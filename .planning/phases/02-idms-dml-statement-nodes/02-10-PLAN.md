---
phase: 02-idms-dml-statement-nodes
plan: 10
type: execute
wave: 9
depends_on: ["02-09"]
files_modified:
  - run_idms_query_capture.sh
  - run_accept_differential_selftest.sh
  - forest-shim/refresh.sh
  - forest-shim/refresh-selftest.sh
  - forest-shim/cobol/parser.c
  - forest-shim/cobol/grammar.json
  - forest-shim/cobol/idms.scm
  - forest-shim/cobol/smoke_test.go
  - .github/workflows/fork-checks.yml
autonomous: true
gap_closure: true
requirements: [IDMS-02, IDMS-03, IDMS-06, IDMS-07]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The exact 14-verb query contract from T-02-R06 is enforced by an executable fork-local gate and by CI, not only by an ad hoc command (IDMS-02, IDMS-06)."
    - "The hardened T-02-R01/T-02-R03 differential adversarial self-test is directly executable and runs in CI (IDMS-03, IDMS-06, IDMS-07)."
    - "The exact gate's invalid-update subject for T-02-R05 and all-14 verb/role subject for T-02-R06 run in CI."
    - "The corrected generated parser and `queries/idms.scm` are refreshed into `forest-shim/cobol/` only after plans 02-08 and 02-09 pass, and source/shim query identity plus embedded-query availability are tested."
    - "A failed generation or copy cannot proceed to test stale or mixed shim output; later refresh steps are skipped or a complete staged artifact set is atomically installed before validation (IDMS-07)."
    - "All work in this plan is fork-local/shim/CI and is committed separately from grammar-family plans 02-08 and 02-09 (FORK-03)."
  artifacts:
    - path: "run_idms_query_capture.sh"
      provides: "Executable exact 14-verb and semantic-role query gate"
      contains: "EXPECTED"
    - path: "run_accept_differential_selftest.sh"
      provides: "Directly executable adversarial self-test"
    - path: "forest-shim/refresh.sh"
      provides: "Fail-closed or staged refresh delivery flow"
    - path: "forest-shim/refresh-selftest.sh"
      provides: "Persistent forced-generation and copy/staging failure regression test"
      contains: "generation"
    - path: "forest-shim/cobol/idms.scm"
      provides: "Vendored exact IDMS query contract"
    - path: "forest-shim/cobol/smoke_test.go"
      provides: "Embedded IDMS query availability test"
    - path: ".github/workflows/fork-checks.yml"
      provides: "Continuous blocker regression gates"
  key_links:
    - from: "run_idms_query_capture.sh"
      to: "queries/idms.scm"
      via: "query the gate-owned temporary 14-verb fixture and byte-compare one exact ordered TSV oracle"
      pattern: "tree-sitter.*query"
    - from: "forest-shim/refresh.sh"
      to: "forest-shim/cobol/idms.scm"
      via: "copy only a coherent generated/query set after successful generation"
      pattern: "queries/.*scm"
    - from: "forest-shim/cobol/smoke_test.go"
      to: "forest-shim/cobol/binding.go"
      via: "the smoke test calls GetQuery for idms and idms.scm, while the exact gate separately compiles and exercises the copied query"
      pattern: "GetQuery\(\"idms"
    - from: ".github/workflows/fork-checks.yml"
      to: "forest-shim/refresh-selftest.sh"
      via: "blocking CI runs refresh failure injection alongside the differential and exact-query gates"
      pattern: "idms|differential|forest-shim"
---

<objective>
Deliver the blocker fixes through the forest shim and turn their synthetic evidence into continuous closeout gates.

Purpose: Wave 6 plan 02-07 establishes fork-local differential hardening and the exact RED gate; Waves 7-8 plans 02-08 and 02-09 fix grammar and published query semantics. This Wave 9 plan preserves FORK-03 by doing only dependent fork-local delivery work: GREEN gate confirmation, executable adversarial tests, fail-closed shim refresh, embedded-query proof, and CI wiring.

Output: A coherent refreshed shim and CI-enforced T-02-R01/T-02-R03/T-02-R06 evidence in effective Wave 9. No grammar.js, generated source-tree artifact, corpus fixture, or source query is edited here.
</objective>

<execution_context>
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-idms-dml-statement-nodes/02-CONTEXT.md
@.planning/phases/02-idms-dml-statement-nodes/02-05-SUMMARY.md
@.planning/phases/02-idms-dml-statement-nodes/02-09-SUMMARY.md
@.planning/phases/02-idms-dml-statement-nodes/02-REVIEW.md
@.planning/phases/02-idms-dml-statement-nodes/02-SECURITY.md
@.planning/phases/02-idms-dml-statement-nodes/02-VERIFICATION.md
@docs/vendoring.md
</context>

## Artifacts this phase produces

- Executable fork-local file `run_idms_query_capture.sh`, created RED in 02-07 and expected byte-unchanged here; Task 1 verifies staged summaries and current normal output under the same hash.
- Modified executable mode and cases: `run_accept_differential_selftest.sh` (mode `100755`, content inherited from 02-07).
- Modified delivery script: `forest-shim/refresh.sh` fail-closed/staged generation-copy-validation ordering.
- New persistent executable `forest-shim/refresh-selftest.sh` covering forced generation and copy/staging failures against isolated temporary shim copies.
- Refreshed shim artifacts: `forest-shim/cobol/parser.c`, `forest-shim/cobol/grammar.json`, and `forest-shim/cobol/idms.scm` (plus generator-copied `parser.h` only if source delivery changes it).
- Modified shim test: `forest-shim/cobol/smoke_test.go`, adding an IDMS embedded-query assertion.
- Modified fork-local workflow: `.github/workflows/fork-checks.yml`, adding exact query, differential adversarial, query parity, and shim Go-test steps.

## Multi-Source Coverage Audit

| SOURCE | ID | Gap-closure item | Plan | Status | Notes |
|--------|----|------------------|------|--------|-------|
| GOAL | -- | Correct IDMS parser/query is published through the shim contract | 02-09, 02-10 | COVERED | The fork-local exact gate and shim exercise are the factual consumers; no production graph reader is claimed. |
| REQ | IDMS-02 | Published query is delivered and executable | 02-09, 02-10 | COVERED | Exact gate plus embedded query. |
| REQ | IDMS-03 | Differential proof remains executable | 02-10, 02-11 | COVERED | Adversarial self-test in CI; full run remains manual per D-08. |
| REQ | IDMS-06 | Continuous regression gates | 02-10, 02-11 | COVERED | CI plus final corpus/NIST run. |
| REQ | IDMS-07 | Coherent, separable shim delivery | 02-10, 02-11 | COVERED | Fork-local commits only. |
| RESEARCH | delivery pipe responsibility map | Query/parser copied into forest shim before consumer tests | 02-10 | COVERED | Correct dependency order. |
| CONTEXT | D-04/D-08/D-10/D-11 | Fork query, manual corpus differential, neutral fixtures, fork CI | 02-10 | COVERED | No proprietary CI input. |
| VERIFICATION | WR-03/WR-04 | Executable self-test and continuous blocker gates | 02-10 | COVERED | Necessarily closes continuous enforcement. |
| VERIFICATION | WR-05 | Failed refresh validates stale/mixed output | 02-10 | COVERED | Necessarily covered for correct shim delivery. |

<tasks>

<task type="tracer">
  <name>Task 1: Reconfirm the committed exact 14-verb RED-to-GREEN contract</name>
  <precondition>Plan 02-07 committed `run_idms_query_capture.sh`; plans 02-08 and 02-09 completed update and verb/role corrections, so normal gate mode is expected to pass; `node_modules/tree-sitter-cli/tree-sitter --version` still reports exact 0.24.5.</precondition>
  <files>run_idms_query_capture.sh</files>
  <read_first>
    - `run_idms_query_capture.sh` as delivered RED by 02-07 (its complete ordered oracle, neutral scratch fixture, and area-role negatives)
    - `queries/idms.scm` as delivered by 02-09; the gate itself owns the temporary fixture and oracle
    - `.planning/phases/02-idms-dml-statement-nodes/02-09-SUMMARY.md` exact 14-verb and role evidence
    - `run_sql_query_capture.sh` lines 1-27, 47-97, 150-191, and 233-291 (fork-local scratch, query-output normalization, and exact ordered TSV comparison)
    - `run_cics_query_capture.sh` lines 27-43 and 105-133 (house style and counter-driven reporting)
    - `.githooks/commit-separability.sh` path-family regexes (confirm root `run_*.sh` is fork-local)
  </read_first>
  <behavior>
    - RED: 02-07's committed gate failed against the pre-fix query because update/session verbs were absent and ACCEPT was oversized.
    - GREEN: current query yields one exact ordered TSV containing all 14 verb capture values plus the intended record/set rows and explicit absence of area values from record/set roles.
    - Running the gate twice produces byte-identical normalized output.
    - A missing query, fixture, CLI, malformed capture row, duplicate verb, missing verb, changed value/range/order, or area leaked as record/set exits nonzero.
  </behavior>
  <action>
    Run the committed `run_idms_query_capture.sh` unchanged first against 02-09's current parser/query. It must now pass, providing the GREEN half of the cross-plan RED-to-GREEN cycle. Its existing complete oracle must still include all 14 exact `verb` rows and the intended record/set rows, with independent negative assertions that READY's area and navigation WITHIN's neutral scope never occur with role `record` or `set`.

    Keep the gate-owned temporary fixture outside tracked paths and clean it on success, failure, and signals. Do not switch authority to `test/idms/query-sample.cbl`; that tracked file is not the exact gate input. Keep explicit status checks and aggregate failure reporting consistent with repository shell style.

    Rely on the committed fail-first transitions recorded by 02-07 and 02-09 rather than replaying a detached historical parser. Verify their recorded `shasum -a 256 run_idms_query_capture.sh` values are identical, then re-run current normal mode twice and require identical validated output under that same gate hash. Do not add a historical replay ritual: it proves no requirement beyond the committed staged RED tests and would reintroduce the grammar-name cache hazard.
  </action>
  <acceptance_criteria>
    - `run_idms_query_capture.sh` is mode 100755 and exits 0 against current source without modification unless a position-only oracle correction is proven necessary.
    - CLI setup failure, wrong version, fixture failure, query failure, and normalizer failure still exit 2 through `HARNESS_ERROR`; GREEN cannot be obtained by weakening staged RED classification.
    - Its expected output has exactly 14 `verb` rows with the required values in source order and pins exact ranges/text, not mere role presence.
    - It pins intended record/set rows and independently rejects both area controls as record/set.
    - Its positive gate-owned subject contains no parse error or missing-node marker before exact captures are accepted.
    - 02-07/02-09 summaries contain the identical gate-byte hash and staged RED/GREEN evidence; current normal mode passes twice with that same hash.
    - Two current runs produce the same outcome and leave no tracked/untracked scratch artifact.
    - No grammar-family file is modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>bash -n run_idms_query_capture.sh &amp;&amp; ./run_idms_query_capture.sh &amp;&amp; ./run_idms_query_capture.sh</automated>
  </verify>
  <done>The all-14 exact verb/operand stream is a permanent executable gate, fails on the reproduced pre-fix contract, passes current source deterministically, and rejects area-to-record/set leakage.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh a coherent shim and prove the embedded IDMS query</name>
  <precondition>Plans 02-08 and 02-09 are GREEN, and Task 1 confirmed the source `queries/idms.scm` exact gate before delivery. `forest-shim/refresh-selftest.sh` created here must pass before the successful live refresh is accepted.</precondition>
  <files>forest-shim/refresh.sh, forest-shim/refresh-selftest.sh, forest-shim/cobol/parser.c, forest-shim/cobol/grammar.json, forest-shim/cobol/idms.scm, forest-shim/cobol/smoke_test.go</files>
  <read_first>
    - `forest-shim/refresh.sh` lines 91-109 (generation currently records failure but continues), 111-170 (copy/rewrite currently mutates live shim in place), and 196-215 (smoke build)
    - `forest-shim/cobol/smoke_test.go` (existing `GetQuery("sample")`, grammar JSON, and missing-query contracts)
    - `forest-shim/cobol/binding.go` and `forest-shim/cobol/plugin.go` `//go:embed grammar.json *.scm` and `GetQuery` behavior
    - `.planning/phases/02-idms-dml-statement-nodes/02-05-SUMMARY.md` "Root cause" and secondary failed-refresh finding
    - `.planning/phases/02-idms-dml-statement-nodes/02-REVIEW.md` WR-05 and WR-04
    - `docs/vendoring.md` regenerate-and-refresh order
  </read_first>
  <behavior>
    - SUCCESS: isolated refresh completes all stages, performs portable include rewrites, installs a coherent artifact set, and passes shim query/build tests.
    - RED 1: force generation failure; refresh exits nonzero, reports steps 3-6 skipped, and leaves every live shim artifact byte-identical.
    - RED 2: force a copy/staging failure; refresh exits nonzero, reports later steps skipped, and leaves the live shim coherent and unchanged.
    - GREEN delivery: successful refresh installs one coherent parser/grammar/query set; source and shim `idms.scm` are byte-identical.
    - Shim Go tests prove `GetQuery("idms")` and `GetQuery("idms.scm")` are equal, nonempty, and contain the corrected query contract.
  </behavior>
  <action>
    Create persistent executable `forest-shim/refresh-selftest.sh` first. It constructs an isolated temporary top/shim tree, invokes the real refresh script through existing environment overrides, and supplies deterministic fake generator/copy failure seams without touching the live shim. One case forces Step 2 generation failure; one forces Step 3 copy/staging failure. Capture the current RED fact that later steps run or live test artifacts change when they must not.

    Harden `forest-shim/refresh.sh` so failure cannot validate stale/mixed output. Abort after generation failure with explicit step3-step6 SKIPPED reporting, and abort after copy/staging failure with step4-step6 SKIPPED reporting. Stage parser.c, scanner.c, grammar.json, parser.h, and all queries in a temporary sibling directory, perform include rewrites and validation there, then replace the live artifact set only after the whole staged set is coherent. Implement include rewriting portably on both macOS and GNU/Linux: do not use BSD-only `sed -i ''`; write transformed temporary files and atomically rename them, or use a portable no-in-place pipeline. Preserve the forest Go-surface drift check and caller-facing environment overrides. Do not add packages.

    Extend `forest-shim/cobol/smoke_test.go` before the successful refresh so it fails against a missing/stale IDMS query as appropriate, then refresh from 02-09 source. Assert `GetQuery("idms")` and `GetQuery("idms.scm")` return identical nonempty bytes and match the source query after delivery. Compile and exercise the shim copy as a tree-sitter query, not merely a byte blob: add a Go test using the repository's existing go-tree-sitter API only if already available without changing `go.mod`; otherwise invoke the pinned CLI from the persistent refresh self-test against `forest-shim/cobol/idms.scm` and its gate-owned neutral subject. Both syntax compilation and matching must succeed. Run Go tests, not only `go build`.

    Keep every change in fork-local/shim paths. Generated source-tree files belong to grammar plans 02-08/02-09 and must not be recommitted here.
  </action>
  <acceptance_criteria>
    - Forced generation failure leaves live shim hashes unchanged, exits nonzero, and marks steps 3-6 skipped.
    - Forced copy/staging failure leaves live shim hashes unchanged/coherent, exits nonzero, and does not run include rewrite, drift success, or smoke success against stale output.
    - `forest-shim/refresh-selftest.sh` is mode 100755, runs successful refresh plus forced generation and copy/staging failure cases, exits 0, and remains the permanent regression test used locally and on Ubuntu CI.
    - Source contains no BSD-only in-place `sed` invocation; include rewrites pass on macOS locally and Ubuntu CI.
    - The Ubuntu workflow explicitly runs the successful refresh-selftest path plus both forced-failure paths, proving Linux portability rather than relying on local macOS evidence.
    - Successful refresh exits 0; a second run is idempotent.
    - `cmp queries/idms.scm forest-shim/cobol/idms.scm` exits 0.
    - `go test ./...` in `forest-shim/cobol` exits 0 and executes the IDMS `GetQuery` assertion; the shim copy of `idms.scm` is also compiled and exercised against the gate-owned synthetic subject.
    - The refreshed shim parser carries 02-09's node contract and does not contain a mixed parser/grammar/query generation.
    - Only `forest-shim/` files are changed by this task.
  </acceptance_criteria>
  <verify>
    <automated>bash -n forest-shim/refresh.sh forest-shim/refresh-selftest.sh &amp;&amp; ./forest-shim/refresh-selftest.sh &amp;&amp; bash forest-shim/refresh.sh &amp;&amp; cmp queries/idms.scm forest-shim/cobol/idms.scm &amp;&amp; IDMS_QUERY_FILE="$PWD/forest-shim/cobol/idms.scm" ./run_idms_query_capture.sh &amp;&amp; (cd forest-shim/cobol &amp;&amp; go test ./...) &amp;&amp; bash forest-shim/refresh.sh</automated>
  </verify>
  <done>The corrected parser/query is delivered as one coherent shim set, forced generation/copy failures cannot validate stale output, and the embedded IDMS query is executed under Go test.</done>
</task>

<task type="auto">
  <name>Task 3: Run blocker gates continuously in fork CI</name>
  <files>run_accept_differential_selftest.sh, .github/workflows/fork-checks.yml</files>
  <read_first>
    - `.github/workflows/fork-checks.yml` (current generate and non-comment corpus-only job)
    - `run_accept_differential_selftest.sh` from 02-07 (all T-02-R01/T-02-R03 synthetic cases and current file mode)
    - `run_idms_query_capture.sh` from 02-07 and GREEN confirmation from Task 1
    - `forest-shim/cobol/smoke_test.go` from Task 2
    - `forest-shim/refresh-selftest.sh` from Task 2
    - `.planning/phases/02-idms-dml-statement-nodes/02-REVIEW.md` WR-03/WR-04
    - `.planning/phases/02-idms-dml-statement-nodes/02-SECURITY.md` T-02-R15 and the four blocking evidence requirements
  </read_first>
  <action>
    Make `run_accept_differential_selftest.sh` mode 100755 and preserve `bash script` compatibility. Extend the existing fork workflow after generation and corpus testing with blocking steps that execute `./run_accept_differential_selftest.sh`, `./run_idms_query_capture.sh` in full normal mode (both the invalid-update subject and all-14 verb/role subject), `./forest-shim/refresh-selftest.sh`, compare source and shim `idms.scm`, exercise the shim query via `IDMS_QUERY_FILE`, and run `go test ./...` in `forest-shim/cobol` so the embedded IDMS query assertion executes.

    Preserve the existing main-only/workflow_dispatch triggers, read-only permissions, lockfile-pinned install posture, and annotated comment-fixture exclusion. Do not edit upstream `.github/workflows/test.yml` or `check-workflows.yml`. Do not add the full DCC/estate differential to CI; D-08 keeps corpus-scale runs manual, while its synthetic adversarial self-test is fast and belongs here.

    Run every CI command locally in workflow order. Validate the YAML with the existing actionlint path when available; otherwise rely on the existing workflow checker and record the local absence without weakening the command gates.
  </action>
  <acceptance_criteria>
    - `test -x run_accept_differential_selftest.sh` succeeds and both direct invocation and `bash` invocation exit 0.
    - Fork CI runs the differential adversarial self-test and exact IDMS query gate as separate blocking steps.
    - Fork CI checks source/shim query identity and runs shim Go tests.
    - Fork CI runs the persistent forced-generation/copy refresh self-test and compiles/exercises `forest-shim/cobol/idms.scm`.
    - Existing triggers, `permissions: contents: read`, pinned dependency install, and comment exclusion annotation remain intact.
    - Upstream workflow files remain byte-unchanged.
    - Local execution of all workflow commands in order exits 0.
    - This task's CI commit contains only fork-local paths and passes commit separability; Task 2's shim commit is separately fork-local and never mixed with grammar-family paths.
  </acceptance_criteria>
  <verify>
    <automated>./run_accept_differential_selftest.sh &amp;&amp; bash run_accept_differential_selftest.sh &amp;&amp; ./run_idms_query_capture.sh &amp;&amp; ./forest-shim/refresh-selftest.sh &amp;&amp; cmp queries/idms.scm forest-shim/cobol/idms.scm &amp;&amp; IDMS_QUERY_FILE="$PWD/forest-shim/cobol/idms.scm" ./run_idms_query_capture.sh &amp;&amp; (cd forest-shim/cobol &amp;&amp; go test ./...) &amp;&amp; node_modules/.bin/tree-sitter test -e '^comment$'</automated>
  </verify>
  <done>The four blocker regressions are continuously exercised at the appropriate layer: fast synthetic differential and exact query gates in CI, plus query parity and embedded-query Go tests; corpus-scale differential remains the manual closeout gate per D-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grammar/query source tree to forest shim | Partial or failed delivery can leave parser, grammar JSON, and embedded queries from different generations. |
| Repository code to GitHub Actions | Fast blocker gates must run with least privilege and without pulling proprietary corpora into CI. |
| Query captures to CI verdict | Role-presence checks are too weak; exact text/ranges/count/order are required to catch oversized ACCEPT and missing verbs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-R01 | Information Disclosure | differential self-test in CI | high | mitigate | Task 3 runs 02-07's external-symlink refusal cases on every fork check; tests use only owned temporary directories and neutral synthetic files. |
| T-02-R03 | Tampering / Repudiation | differential self-test in CI | high | mitigate | Task 3 continuously runs strict-TSV and duplicate-key order cases; any false-clean behavior fails the workflow. |
| T-02-R04 | Repudiation | differential self-test in CI | medium | mitigate | Task 3 continuously runs malformed NODE_TYPE_REGEX snapshot/run and extraction-pipeline failure cases. |
| T-02-R05 | Tampering | `run_idms_query_capture.sh` invalid-update subject | high | mitigate | Task 3 runs the exact gate in full normal mode, including zero graph-bearing captures for every prohibited update shape. |
| T-02-R06 | Tampering | `run_idms_query_capture.sh` | high | mitigate | Task 1 pins exact 14-verb and role output, and Task 3 runs it as a blocking CI step. |
| T-02-R09 | Tampering | `forest-shim/refresh.sh` | medium | mitigate | Task 2 fails closed or stages atomically so a failed generation/copy cannot validate stale or mixed output; synthetic failure evidence is required. |
| T-02-R10 | Elevation of Privilege | `.github/workflows/fork-checks.yml` | low | mitigate | Preserve main-only/workflow_dispatch trigger and `contents: read`; no privileged pull-request event or secret is added. |
| T-02-R15 | Repudiation | CI coverage | low | mitigate | Exact query, differential adversarial, query identity, and embedded-query checks become explicit named steps. |
| T-02-SC | Supply-chain Tampering | package installation | low | accept | No dependency is added; CI retains existing lockfile-pinned installation and existing action majors. |
</threat_model>

<verification>
1. Run exact IDMS query gate twice.
2. Run the directly executable differential adversarial self-test and its Bash invocation.
3. Exercise forced refresh generation/copy failures and compare live shim hashes.
4. Run successful refresh twice, source/shim query comparison, and shim Go tests.
5. Execute fork workflow commands locally in order and validate workflow syntax.
6. Verify all commits are fork-local/shim/CI only and separate from 02-08/02-09 grammar-family commits.
</verification>

<success_criteria>
- The corrected query and parser are coherently present in the shim.
- Exact query and differential blocker tests are executable and continuous.
- Failed delivery cannot make stale/mixed output look current.
- CI remains least-privileged and proprietary-corpus-free.
- FORK-03 commit separability is preserved.
</success_criteria>

<output>
Create `.planning/phases/02-idms-dml-statement-nodes/02-10-SUMMARY.md` when done. Record exact-gate RED/GREEN outcomes, refresh failure-injection evidence and unchanged hashes, shim query/embed checks, CI commands, and fork-local-only commit paths.
The SUMMARY must include `## Threat Flags` entries for T-02-R01/R03/R05/R06 with their continuous-gate evidence or explicit unresolved status; use `none` only for additional threats.
</output>
