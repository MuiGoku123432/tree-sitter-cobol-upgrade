---
phase: 02-idms-dml-statement-nodes
plan: 05
type: execute
wave: 4
depends_on: ["02-04"]
files_modified:
  - forest-shim/refresh.sh
  - forest-shim/cobol/parser.c
  - forest-shim/cobol/grammar.json
  - forest-shim/cobol/idms.scm
  - ~/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go
autonomous: true
requirements: [IDMS-04, IDMS-07]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`TestErrorCascade` in `cobolprobe` contains an IDMS DML injection case. It did not before this phase — the test injects only `EXEC CICS` and `SCHEMA SECTION` today, despite being named as the IDMS-04 gate (IDMS-04)."
    - "With an IDMS DML statement injected after the first paragraph, the remaining paragraphs and all data items survive at parity with the clean control file — the cascade is eliminated, not merely reduced (IDMS-04)."
    - "A DML statement immediately followed by a paragraph header, and a DML statement immediately followed by a data item, both leave the following construct intact (edge: IDMS-04 adjacency)."
    - "The `clean` control case — the same program with zero DML injected — yields the same paragraph and data-item counts after the grammar change as before it, so the parity comparison is against an unmoved baseline (edge: IDMS-04 empty)."
    - "`TestErrorCascade` is deterministic: the same input yields the same paragraph, data-item and error counts on every run (edge: IDMS-04 ordering)."
    - "`go test -run TestErrorCascade -count=2` reports identical counts on both iterations, and the test holds no shared mutable state across parallel runs (edge: IDMS-04 concurrency)."
    - "The RED baseline is captured BEFORE the refreshed grammar reaches gortex: run against the pre-IDMS shim, the injected DML case must show a cascade. A case that passes both before and after proves nothing (IDMS-04)."
    - "`forest-shim/refresh.sh` carries `queries/*.scm` into `forest-shim/cobol/`, so `queries/idms.scm` reaches gortex through the same pipe as `parser.c` — `forest-shim/cobol/plugin.go` and `binding.go` both declare `//go:embed grammar.json *.scm`, so a query left behind in `queries/` is invisible to the consumer (IDMS-02, IDMS-04)."
    - "Every commit this plan makes in THIS repository touches only fork-local paths (`forest-shim/`); the `cascade_test.go` change is committed in the `gortex` repository and never enters this fork's history (IDMS-07, FORK-03)."
  artifacts:
    - forest-shim/refresh.sh
    - forest-shim/cobol/idms.scm
    - ~/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go
  key_links:
    - "`forest-shim/refresh.sh` step 3 (copy-and-flatten) — today it copies `parser.c`, `scanner.c`, `grammar.json` and `parser.h` but NOT `queries/*.scm`. `forest-shim/cobol/sample.scm` exists only because forest's own module-cache drop-in carried it."
    - "`//go:embed grammar.json *.scm` in `forest-shim/cobol/plugin.go:25` and `binding.go:25` — the mechanism that makes an `.scm` file inside the shim reachable by gortex, and the reason a query outside the shim is not."
    - "`TestErrorCascade`'s `countAll` helper and its `clean` control program — the parity baseline every injection case is measured against."
  prohibitions:
    - requirement_id: IDMS-04
      category: transparency
      statement: "Never treat IDMS-04 as satisfied because `TestErrorCascade` passes. As of this phase it injects only `EXEC CICS` and `SCHEMA SECTION` and contains no assertions at all — it logs. It cannot fail on IDMS DML because it never exercises it, so a green run before an IDMS case is added is silence, not evidence."
    - requirement_id: IDMS-07
      category: privacy
      statement: "The injected COBOL in the cross-repo cascade case is hand-written from neutral invented names. This fork's `.githooks/estate-guard.sh` does NOT run in the `gortex` repository, so the estate discipline there is unenforced by machinery and rests entirely on how the case is written."
---

<objective>
Carry the new grammar through the Phase 1 delivery pipe to gortex, and close IDMS-04 — which
research found is **not** covered by the test CONTEXT.md names as its gate.

Two things this plan makes explicit rather than assuming. First, `TestErrorCascade` in the external
`gortex` repository injects only `EXEC CICS` and `SCHEMA SECTION` today; there is no IDMS DML case
and the function contains no assertions at all, only `t.Logf` calls. IDMS-04 therefore requires new
test code, in a different repository, outside this fork's FORK-03 boundary — planned here as an
explicitly marked cross-repo task, not deferred to an unowned follow-up.

Second, `forest-shim/refresh.sh` does not copy `queries/*.scm` into the shim, and
`forest-shim/cobol/plugin.go` embeds `grammar.json *.scm` from the shim directory only. Without a
carry-through step, `queries/idms.scm` exists in this fork and is invisible to gortex.

Purpose: IDMS-04, and the delivery of `queries/idms.scm` to its consumer.
Output: a fork-local `forest-shim/` commit, and a separate commit in the `gortex` repository.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-idms-dml-statement-nodes/02-CONTEXT.md
@.planning/phases/02-idms-dml-statement-nodes/02-RESEARCH.md
@.planning/phases/02-idms-dml-statement-nodes/02-04-SUMMARY.md
@docs/vendoring.md
</context>

## Artifacts this phase produces

Every symbol below is **new in this phase**. Nothing in this list exists in the codebase today, so
none of it can be drift against an existing implementation.

**New grammar node types (exported, the cross-repo query contract):**
`idms_navigation_statement`, `idms_update_statement`, `idms_session_statement`,
`idms_accept_statement`, `idms_record_name`, `idms_set_name`, `idms_unparsed_tail`.

**New private grammar rules:** `_idms_navigation_body`, `_idms_update_body`, `_idms_session_body`,
`_idms_accept_body`, `_idms_operand_token`.

**New `_KEYWORD` terminals:** `_OBTAIN`, `_FIND`, `_BIND`, `_READY`, `_FINISH`, `_STORE`,
`_MODIFY`, `_CONNECT`, `_DISCONNECT`, `_GET`, `_WITHIN`, `_CALC`, `_OWNER`, `_CURRENT`, `_PRIOR`,
`_KEEP`, `_DB_KEY`, `_RUN_UNIT`, `_TASK`, `_USAGE_MODE`, `_RETRIEVAL`, `_PROTECTED`, `_MEMBERS`,
`_PERMANENT`, `_SELECTIVE`, `_DUPLICATE`, `_PAGE_INFO`.

**New exported keyword nodes:** `OBTAIN`, `FIND`, `BIND`, `READY`, `FINISH`, `STORE`, `MODIFY`,
`CONNECT`, `DISCONNECT`, `GET`, `WITHIN`, `CALC`, `OWNER`, `CURRENT`, `PRIOR`, `KEEP`, `DB_KEY`,
`RUN_UNIT`, `TASK`, `USAGE_MODE`, `RETRIEVAL`, `PROTECTED`, `MEMBERS`, `PERMANENT`, `SELECTIVE`,
`DUPLICATE`, `PAGE_INFO`, plus `COMMIT` and `ROLLBACK`, and conditionally `ACCEPT` and `CURRENCY`.

**New file paths:** `queries/idms.scm`, `forest-shim/cobol/idms.scm`, `test/idms/query-sample.cbl`,
`test/corpus/idms_navigation.txt`, `test/corpus/idms_update.txt`, `test/corpus/idms_session.txt`,
`test/corpus/idms_accept.txt`, `run_accept_differential.sh`,
`run_accept_differential_selftest.sh`, `.github/workflows/fork-checks.yml`.

**New CLI surface:** `run_accept_differential.sh` sub-commands `snapshot`, `compare`, `run`; env
overrides `BASELINE_REF`, `CORPUS_DIR`, `TREE_SITTER`, `DIFF_TMP`.

**Modified fork-local files:** `.githooks/commit-separability.sh`, `forest-shim/refresh.sh`,
`docs/baseline.md`.

**Modified cross-repo file:**
`~/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go` — a new IDMS DML
injection case plus the first assertions the function has ever carried.

<flagged_assumptions>
- **`TestErrorCascade` has no assertions today.** Reading the function this session confirms it is
  entirely `t.Logf` — it reports counts and always passes. Adding an IDMS injection case that also
  only logs would satisfy the letter of "add a case" while leaving IDMS-04 unenforceable. This plan
  adds assertions to the IDMS case specifically. It does NOT retrofit assertions to the existing
  `EXEC CICS` and `SCHEMA SECTION` cases — those are Phase 3's and preprocessing's gates
  respectively, and adding a hard assertion to a case this phase does not fix would fail the build
  on a known-open problem.

- **Cross-repo commit hygiene is unmachined.** This fork's `.githooks/estate-guard.sh` and
  `commit-separability.sh` do not run in the `gortex` repository. The neutral-names discipline for
  the injected COBOL rests on how the case is written, and the SUMMARY should say so plainly rather
  than implying the guard covered it.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: RED — add the IDMS DML cascade case and capture the pre-refresh baseline (cross-repo)</name>
  <precondition>`forest-shim/cobol/parser.c` still holds the PRE-IDMS parser — no plan in this phase has run `forest-shim/refresh.sh` yet. Confirm with `git log --oneline -1 -- forest-shim/cobol/parser.c` showing a Phase 1 commit, and with `go work edit -json` (run in `~/repos/mine/GoApps/gortex`) listing this fork's `forest-shim/cobol` as a workspace member. If the shim already carries the new grammar, the RED baseline is unobtainable and this task must halt rather than record a meaningless result.</precondition>
  <files>~/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go</files>
  <read_first>
    - `~/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go` — the WHOLE file. Note: `countAll` is the counting helper; `TestErrorCascade` builds a `clean` 20-data-item / 20-paragraph program, then injects `EXEC CICS SEND MAP('M') END-EXEC.` after the first paragraph and a `SCHEMA SECTION` before the data division. It contains **no** assertion — every outcome is a `t.Logf`. There is no IDMS DML case anywhere in it.
    - `~/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/README.md` (the published method and the `-corpus` / `-neut-corpus` flags)
    - `~/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/neutralize_test.go` lines 16-44 (`reDML`'s verb list and its `[^.]*\.` statement-boundary assumption — the same 14 verbs this phase's grammar now models)
    - `docs/vendoring.md` §3 (turning the pipe on and off via `go.work`) and §4 (the six refresh steps)
    - `.planning/phases/02-idms-dml-statement-nodes/02-RESEARCH.md` § "Common Pitfalls" Pitfall 3
  </read_first>
  <action>
    This task edits a file in a DIFFERENT REPOSITORY — `~/repos/mine/GoApps/gortex`. It is
    deliberately outside this fork's FORK-03 boundary, which is scoped to this repository's own
    commits. Commit it in `gortex`, on its own, and never stage it from this repo.

    Add a third injection case to `TestErrorCascade`, mirroring the existing `EXEC CICS` case's
    shape. Inject an IDMS navigation statement — an `OBTAIN` naming an invented neutral record and
    set — after the first paragraph, exactly where the `EXEC CICS` case injects, so the two are
    directly comparable. Add a second injection placing a DML statement immediately before a data
    item as well, so both adjacency directions from IDMS-04 are exercised: a DML statement followed
    by a paragraph header, and a DML statement followed by a data item.

    Use invented neutral names only — `CUSTOMER-REC`, `CUST-ORDER-SET` — matching the fixtures this
    phase already wrote. Nothing estate-derived, not even a paraphrased shape. This fork's
    estate-guard does not run in the `gortex` repository, so this discipline is unenforced by
    machinery here.

    Do NOT add assertions yet. In this task the case logs its counts exactly as the existing two
    cases do, and it is run against the shim as it stands — which still carries the PRE-IDMS parser,
    because no plan in this phase has run `forest-shim/refresh.sh`. Capture the output verbatim.
    That is the RED evidence: the injected DML statement should show a cascade, with the paragraphs
    or data items following it lost relative to the clean control. If the counts come back at
    parity before the grammar change, the case is not discriminating and must be reshaped before
    Task 3 turns it into a gate — a case that passes both before and after proves nothing.

    Do not retrofit assertions onto the existing `EXEC CICS` or `SCHEMA SECTION` cases. `EXEC CICS`
    is Phase 3's gate and `SCHEMA SECTION` belongs to preprocessing per locked decision D3; hard-
    asserting either here would fail the build on a known-open problem this phase does not fix.
  </action>
  <acceptance_criteria>
    - Source: `cascade_test.go` contains an injection case naming `OBTAIN`, `CUSTOMER-REC` and `CUST-ORDER-SET`; `grep -c 'OBTAIN' cascade_test.go` returns at least 1 where it returned 0 before.
    - Source: `grep -cE 'estate|DCC|DCCSS|CAMSC' cascade_test.go` shows no new estate-derived identifier introduced by this task (the pre-existing `SCHEMA SECTION` case's names are untouched).
    - CLI output: `go test ./internal/parser/forest/cobolprobe/ -run TestErrorCascade -v` (run in `~/repos/mine/GoApps/gortex`) exits 0 and its log lines report paragraph and data-item counts for the clean control, the `EXEC CICS` case, the `SCHEMA SECTION` case, and the two new IDMS cases.
    - Behavior: the RED baseline is recorded verbatim in the SUMMARY, and it shows the IDMS injection losing paragraphs or data items relative to the clean control. If it does not, the task halts and the case is reshaped — a non-discriminating case is not accepted as a baseline.
    - Behavior: `git -C ~/repos/mine/GoApps/gortex log --oneline -1` shows this change committed in the `gortex` repository, and `git status --porcelain` in THIS repository shows no `gortex` path staged.
  </acceptance_criteria>
  <verify>
    <automated>cd ~/repos/mine/GoApps/gortex &amp;&amp; go test ./internal/parser/forest/cobolprobe/ -run TestErrorCascade -v 2>&amp;1 | grep -q 'IDMS'</automated>
  </verify>
  <done>`TestErrorCascade` carries two IDMS DML injection cases, and their pre-refresh output — showing the cascade the grammar has not yet fixed — is recorded verbatim as the falsifiable RED baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Carry queries/*.scm through the shim and refresh the pipe</name>
  <files>forest-shim/refresh.sh, forest-shim/cobol/parser.c, forest-shim/cobol/grammar.json, forest-shim/cobol/idms.scm</files>
  <read_first>
    - `forest-shim/refresh.sh` — the WHOLE file, in particular step 3 (copy-and-flatten). Confirm by reading it that it copies `src/parser.c`, `src/scanner.c`, `src/grammar.json` and `src/tree_sitter/parser.h` and does **not** copy anything from `queries/`. `forest-shim/cobol/sample.scm` is byte-identical to `queries/sample.scm` only because forest's own module-cache drop-in already carried it.
    - `forest-shim/cobol/plugin.go` lines 20-40 and `forest-shim/cobol/binding.go` lines 20-40 — `//go:embed grammar.json *.scm` at line 25 of each, and the `strings.TrimSuffix(kind, ".scm") + ".scm"` lookup. This is why an `.scm` file must live INSIDE the shim directory to be reachable by gortex.
    - `docs/vendoring.md` §2 (the shim and the include-rewrite callout), §4 (the six steps, the overridable env vars, and the `npm ci` gate on `$TREE_SITTER`'s presence rather than on `npm ci`'s exit code), §5 (the open `SYMBOL_COUNT`/`TOKEN_COUNT` 1215/585 vs 1153/523 question)
    - `queries/idms.scm` (as 02-04 left it)
  </read_first>
  <action>
    Extend `forest-shim/refresh.sh` step 3 to copy every `queries/*.scm` into `$SHIM_DIR`, keeping
    the same house style the rest of the script uses: an explicit `$?` check after the copy, a
    `report` line naming each file copied, and a `FAIL_COUNTER` increment on failure rather than a
    shell-level abort-on-error option, which this repo's scripts never use. `refresh.sh` is
    fork-local, so this edit belongs in its own commit, separate from every grammar commit this
    phase produced.

    Then run the refresh with the same invocation `docs/vendoring.md` §4 documents. The regenerate
    step will now carry the four IDMS node types, the operand rules and the tail absorber into
    `forest-shim/cobol/parser.c`, and the new copy step will carry `queries/idms.scm` into
    `forest-shim/cobol/idms.scm`. Step 5's drift check compares the shim's Go surface against the
    forest module-cache copy it impersonates — a new `.scm` file is data the shim embeds, not part
    of that Go surface, so the drift check should stay clean; if it flags the addition, read its
    output before reaching for `REFRESH_ALLOW_DRIFT`, because that variable downgrades a real
    contract check to a warning.

    Expect `forest-shim/cobol/parser.c` to change substantially — this is the first time the pipe
    carries a real grammar change rather than Phase 1's synthetic rename probe. The
    `SYMBOL_COUNT`/`TOKEN_COUNT` shift relative to the previously committed shim is the already-
    recorded `docs/vendoring.md` §5 open question, not a new defect.

    Finally, confirm the query actually reached the consumer: `forest-shim/cobol/idms.scm` must
    exist and be byte-identical to `queries/idms.scm`, and the shim must still build.
  </action>
  <acceptance_criteria>
    - Source: `grep -v '^#' forest-shim/refresh.sh | grep -c 'scm'` returns at least 1 — the copy step is implemented, not merely described in the header comment.
    - Source: with comment lines filtered out, `grep -v '^#' forest-shim/refresh.sh | grep -cE '^[[:space:]]*set -[eu]'` returns 0 — the house style is preserved. The filter is required: `refresh.sh`'s own header comment legitimately explains this convention, so an unfiltered count is non-zero against the file as it already ships.
    - Test command: `bash forest-shim/refresh.sh` exits 0, and `forest-shim/refresh.log` records all six steps with no FAIL line.
    - Behavior: `cmp queries/idms.scm forest-shim/cobol/idms.scm` exits 0 — the query reached the shim byte-identically.
    - Behavior: `cmp queries/sample.scm forest-shim/cobol/sample.scm` exits 0 — the pre-existing query was not disturbed by the new copy step.
    - Test command: `go build ./...` inside `forest-shim/cobol/` exits 0 (this is refresh.sh step 6's own gate; assert it independently).
    - Behavior: running `bash forest-shim/refresh.sh` a second time leaves `git status --porcelain forest-shim/cobol` empty — the script is idempotent, as Phase 1 plan 01-03 proved for its pre-`.scm` form.
    - Behavior: every commit this plan makes in THIS repository touches only `forest-shim/` paths — `git show --name-only --format= <sha>` lists nothing under `grammar.js`, `src/`, `test/`, `queries/`, `docs/`, `.planning/` or `.githooks/`.
  </acceptance_criteria>
  <verify>
    <automated>bash forest-shim/refresh.sh &amp;&amp; cmp queries/idms.scm forest-shim/cobol/idms.scm &amp;&amp; cmp queries/sample.scm forest-shim/cobol/sample.scm &amp;&amp; bash forest-shim/refresh.sh &amp;&amp; [ -z "$(git status --porcelain forest-shim/cobol)" ]</automated>
  </verify>
  <done>`refresh.sh` carries `queries/*.scm` into the shim, the refreshed shim holds the new IDMS grammar and `idms.scm`, it builds, and a second run is a no-op.</done>
</task>

<task type="auto">
  <name>Task 3: GREEN — assert cascade parity on the refreshed grammar (cross-repo)</name>
  <precondition>`forest-shim/cobol/parser.c` now holds the IDMS grammar (Task 2 complete), and `go work edit -json` run in `~/repos/mine/GoApps/gortex` still lists this fork's `forest-shim/cobol` as a workspace member — otherwise the test measures forest v1.9.1's parser and the result is meaningless.</precondition>
  <files>~/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go</files>
  <read_first>
    - `~/repos/mine/GoApps/gortex/internal/parser/forest/cobolprobe/cascade_test.go` (as Task 1 left it — the two new IDMS injection cases and their log lines)
    - `.planning/phases/02-idms-dml-statement-nodes/02-05-SUMMARY.md` draft or Task 1's recorded output — the RED baseline counts the assertions must now improve on
    - `.planning/ROADMAP.md` Phase 2 criterion 3 and Phase 3 criterion 3 (the parity language, and the measured `EXEC CICS` baseline of 1 of 20 paragraphs surviving that Phase 3 will be held to)
  </read_first>
  <behavior>
    - IDMS DML injected after paragraph 1: paragraphs counted equals the clean control's count, and data items counted equals the clean control's count. Assert equality, not "at least most".
    - IDMS DML injected immediately before a data item: data items counted equals the clean control's count.
    - The clean control's own counts are asserted against the fixed program shape the test builds (20 data items, 20 paragraphs), so a regression that moved the baseline cannot make a broken parity check look green.
    - `-count=2` yields identical numbers on both iterations.
  </behavior>
  <action>
    Add assertions to the two IDMS injection cases so IDMS-04 becomes a gate rather than a log line.
    Assert paragraph-count parity and data-item-count parity against the clean control, and assert
    the clean control's own counts against the program the test constructs — without that second
    assertion, a regression that lowered the baseline would make a broken parity check pass.

    Assert only on the IDMS cases. Leave `EXEC CICS` and `SCHEMA SECTION` as logging cases:
    `EXEC CICS` is Phase 3's gate and `SCHEMA SECTION` belongs to preprocessing per locked decision
    D3. Hard-asserting either here would fail the build on a problem this phase is not fixing.

    Run the test against the refreshed shim and compare the numbers to Task 1's RED baseline. The
    green run must show the cascade eliminated where the red run showed it present. Record both
    sets of numbers side by side — that pairing is the evidence for IDMS-04, in exactly the shape
    Phase 1's 01-06 / 01-07 red-then-green gap closure used.

    Re-run with `-count=2` to confirm determinism, and commit in the `gortex` repository — never
    staged from this fork.
  </action>
  <acceptance_criteria>
    - Source: `cascade_test.go` contains at least two assertion calls (`t.Errorf`/`t.Fatalf`) inside the IDMS cases where the file previously contained zero assertions of any kind.
    - Source: the `EXEC CICS` and `SCHEMA SECTION` cases still contain no assertion — `grep -c 't.Errorf\|t.Fatalf' cascade_test.go` equals the number of IDMS assertions added, no more.
    - Test command: `go test ./internal/parser/forest/cobolprobe/ -run TestErrorCascade -v` exits 0 in `~/repos/mine/GoApps/gortex`.
    - Test command: `go test ./internal/parser/forest/cobolprobe/ -run TestErrorCascade -count=2 -v` exits 0 and its two iterations report identical paragraph, data-item and error counts.
    - Behavior: the SUMMARY records the RED counts from Task 1 and the GREEN counts from this task side by side, showing paragraphs and data items moving to parity with the clean control.
    - Behavior: reverting the workspace link (removing `go.work`) and re-running makes the new assertions FAIL — proving the test is measuring this fork's grammar through the pipe and not forest v1.9.1's cached parser. Restore `go.work` afterward, as Phase 1 plan 01-01 did.
    - Behavior: `git -C ~/repos/mine/GoApps/gortex log --oneline -1` shows this change committed in the `gortex` repository; `git status --porcelain` in THIS repository shows no `gortex` path staged.
  </acceptance_criteria>
  <verify>
    <automated>cd ~/repos/mine/GoApps/gortex &amp;&amp; go test ./internal/parser/forest/cobolprobe/ -run TestErrorCascade -count=2 -v</automated>
  </verify>
  <done>`TestErrorCascade` asserts paragraph and data-item parity after an IDMS DML injection, passes against the refreshed grammar, failed against the pre-refresh one, and is deterministic across repeated runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proprietary corpora → the `gortex` repository | The cross-repo cascade case adds hand-written COBOL to a different repository where this fork's `.githooks/estate-guard.sh` does not run. The estate discipline there is unenforced by machinery. |
| this fork's `forest-shim/cobol/` → gortex's module resolution | A `go.work` `use` directive removes the module from checksum-verified resolution (`docs/vendoring.md` §3). Anything placed in the shim is loaded by the consumer without a checksum gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Information Disclosure | `cascade_test.go` injected COBOL, `forest-shim/cobol/` copied artifacts | high | mitigate | The injected COBOL uses only the invented neutral names this phase's fixtures already established (`CUSTOMER-REC`, `CUST-ORDER-SET`); an acceptance criterion greps for the absence of any newly introduced estate-derived identifier. `refresh.sh` copies only generated artifacts and `queries/*.scm` — both tracked, non-proprietary, and already reviewed. The SUMMARY carries counts, never corpus content. |
| T-02-04 | Tampering | `forest-shim/refresh.sh` step 5 drift check, `REFRESH_ALLOW_DRIFT` | medium | mitigate | The new `.scm` copy step must not be allowed to normalise a real drift failure into a warning. The action instructs reading step 5's output before reaching for `REFRESH_ALLOW_DRIFT`, and the acceptance criteria require `refresh.log` to record all six steps with no FAIL line — the variable is not set. |
| T-02-01 | Denial of Service (ASVS V5, parser resource exhaustion) | the parser shipped into gortex via the shim | low | mitigate | Whatever conflict-resolution mechanism 02-04 selected now reaches a consumer that parses 1,564 real files. 02-04's constraint that any `conflicts:` array holds exactly the pairing the generator named bounds the GLR forking cost; this plan changes nothing about it and inherits the mitigation. |
| T-02-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | Zero new packages. `refresh.sh` step 1 runs `npm ci` against the existing lockfile, gated on `$TREE_SITTER`'s presence rather than on `npm ci`'s exit code. The `gortex` side adds no Go dependency — `cascade_test.go` uses only what the package already imports. |
</threat_model>

<verification>
1. Task 1's RED run shows the IDMS injection losing paragraphs or data items against the clean control, recorded verbatim.
2. `bash forest-shim/refresh.sh` exits 0 and a second run is a no-op.
3. `cmp queries/idms.scm forest-shim/cobol/idms.scm` exits 0.
4. `go build ./...` inside `forest-shim/cobol/` exits 0.
5. `go test -run TestErrorCascade -count=2 -v` exits 0 in `gortex` with identical counts on both iterations.
6. Removing `go.work` makes the new assertions fail; restoring it makes them pass.
7. Every commit in THIS repository touches only `forest-shim/`; the `cascade_test.go` change is committed in `gortex`.
</verification>

<success_criteria>
- `TestErrorCascade` has an IDMS DML case with real assertions where it had neither.
- Paragraphs and data items following an injected DML statement survive at parity with the clean
  control, proven red-then-green across the shim refresh.
- `queries/idms.scm` reaches gortex through the shim's `//go:embed *.scm`.
- The fork-local `forest-shim/` commit and the cross-repo `gortex` commit are separate and neither
  contaminates the other's history.
</success_criteria>

<output>
Create `.planning/phases/02-idms-dml-statement-nodes/02-05-SUMMARY.md` when done. Record: the RED
and GREEN paragraph, data-item and error counts side by side for both IDMS injection cases; the
`SYMBOL_COUNT`/`TOKEN_COUNT` the refreshed shim reports against `docs/vendoring.md` §5's open
question; the `gortex` commit SHA; and an explicit note that this fork's estate-guard does not run
in that repository, so the neutral-names discipline there was manual.
</output>
