---
phase: 02-idms-dml-statement-nodes
plan: 08
type: execute
wave: 7
depends_on: ["02-07"]
files_modified:
  - grammar.js
  - src/parser.c
  - src/grammar.json
  - src/node-types.json
  - test/corpus/idms_update.txt
autonomous: true
gap_closure: true
requirements: [IDMS-01, IDMS-02, IDMS-06, IDMS-07]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "T-02-R05/CR-04 is closed: the shared public `idms_update_statement` remains stable while STORE, MODIFY, ERASE, CONNECT, DISCONNECT, and GET each admit only their syntax-grounded operand form (D-01, IDMS-01)."
    - "Only GET may omit a record; TO belongs only to CONNECT, FROM only to DISCONNECT, and member options only to ERASE."
    - "Every prohibited update shape is RED before implementation, then yields neither a clean graph-bearing update AST nor a record/set capture after GREEN (IDMS-02)."
    - "Generated artifacts are produced by the pinned CLI and owned sequentially by this plan before 02-09 changes the same files."
    - "All fixtures are synthetic, neutral, hand-written, and grammar-family commits remain separate from fork-local work (D-10, FORK-03)."
  artifacts:
    - path: "grammar.js"
      provides: "Verb-specific update alternatives under one public statement node"
      contains: "idms_update_statement"
    - path: "src/parser.c"
      provides: "Generated parser for corrected update syntax"
    - path: "src/grammar.json"
      provides: "Generated grammar representation"
    - path: "src/node-types.json"
      provides: "Stable public update node and operand types"
    - path: "test/corpus/idms_update.txt"
      provides: "Positive controls and complete CR-04 negative matrix"
  key_links:
    - from: "test/corpus/idms_update.txt"
      to: "grammar.js"
      via: "RED/GREEN corpus cases exercise every legal and prohibited idms_update_statement alternative"
      pattern: "idms update"
    - from: "grammar.js"
      to: "src/parser.c"
      via: "the pinned tree-sitter generator produces parser.c, grammar.json, and node-types.json"
      pattern: "idms_update_statement"
    - from: "run_idms_query_capture.sh"
      to: "grammar.js"
      via: "the invalid-update subject parses through the generated grammar and asserts no graph-bearing captures"
      pattern: "invalid"
---

<objective>
Close T-02-R05/CR-04 alone, before touching the wider query/operand-role contract.

Purpose: Keep `idms_update_statement` public and stable while replacing its optional cross-verb body with complete verb-specific syntax. This Wave 7 grammar plan consumes 02-07's negative gate and owns generated artifacts first; Wave 8 plan 02-09 then builds query semantics on the corrected parser.

Output: RED/GREEN update corpus, corrected update grammar, and regenerated `src/`, all in grammar-family-only commits.
</objective>

<execution_context>
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-idms-dml-statement-nodes/02-CONTEXT.md
@.planning/phases/02-idms-dml-statement-nodes/02-RESEARCH.md
@.planning/phases/02-idms-dml-statement-nodes/02-03-SUMMARY.md
@.planning/phases/02-idms-dml-statement-nodes/02-06-SUMMARY.md
@.planning/phases/02-idms-dml-statement-nodes/02-07-SUMMARY.md
@.planning/phases/02-idms-dml-statement-nodes/02-REVIEW.md
@.planning/phases/02-idms-dml-statement-nodes/02-SECURITY.md
@.planning/phases/02-idms-dml-statement-nodes/02-VERIFICATION.md
</context>

## Artifacts this phase produces

- Modified `grammar.js` symbols: `idms_update_statement` and `_idms_update_body` (or verb-specific private update-body rules).
- Regenerated `src/parser.c`, `src/grammar.json`, and `src/node-types.json`.
- Expanded `test/corpus/idms_update.txt` with the complete synthetic CR-04 negative matrix.
- No query, shim, CI, docs, planning, or fork-local tooling implementation change.

## Multi-Source Coverage Audit

| SOURCE | ID | Item | Plan | Status | Notes |
|--------|----|------|------|--------|-------|
| GOAL | -- | Semantically valid walkable update nodes | 02-08 | COVERED | One public class, legal forms only. |
| REQ | IDMS-01 | Valid update syntax | 02-08 | COVERED | Verb-specific alternatives. |
| REQ | IDMS-02 | No false record/set operands | 02-08, 02-09 | COVERED | Parser first, exact query gate next. |
| REQ | IDMS-06 | Corpus regression | 02-08, 02-11 | COVERED | Focused then final. |
| REQ | IDMS-07 | Synthetic fixtures/separable delivery | 02-08 | COVERED | Grammar-family only. |
| CONTEXT | D-01/D-02/D-03/D-10 | Stable class/operands, countable tails, neutral source | 02-08 | COVERED | Public names retained. |
| VERIFICATION | T-02-R05/CR-04 | Invalid cross-verb combinations | 02-08 | COVERED | Both tasks. |

<tasks>

<task type="tracer">
  <name>Task 1: Commit the complete invalid-update RED matrix</name>
  <precondition>Plan 02-07 completed; `run_idms_query_capture.sh --expect-red=verbs` succeeds only on the exact known verb diagnostics; `--invalid-only` reproduces the named update mismatches; and the lockfile-pinned CLI reports tree-sitter 0.24.5.</precondition>
  <files>test/corpus/idms_update.txt</files>
  <read_first>
    - `test/corpus/idms_update.txt` (existing legal controls and expected-tree format)
    - `grammar.js` lines 2696-2723 (current shared optional body)
    - `.planning/phases/02-idms-dml-statement-nodes/02-REVIEW.md` CR-04 reproduced forms
    - `.planning/phases/02-idms-dml-statement-nodes/02-RESEARCH.md` lines 485-507 legal syntax
    - `run_idms_query_capture.sh` invalid-update subject from 02-07
  </read_first>
  <behavior>
    - Bare STORE, MODIFY, ERASE, CONNECT, and DISCONNECT are rejected as clean update statements; bare GET remains legal.
    - CONNECT FROM and DISCONNECT TO are rejected.
    - STORE/MODIFY/GET with TO or FROM set clauses are rejected.
    - STORE/MODIFY/CONNECT/DISCONNECT/GET with ERASE member options are rejected.
    - Existing legal STORE, MODIFY, ERASE, CONNECT, DISCONNECT, GET-record, and GET-bare controls remain unchanged.
  </behavior>
  <action>
    Add one neutral corpus case per prohibited category before editing grammar. Expected trees must encode the safe result: no clean legal-looking `idms_update_statement` carrying false record/set operands. Capture both command status and output. Accept RED only when the command fails and output names every newly added `idms update invalid ...` case as an expected-tree mismatch; a missing CLI/fixture, generator/load error, crash, or failure in any pre-existing legal case is `HARNESS_ERROR`/unrelated failure and halts. Run `--invalid-only` with the same exact named-case discipline. Commit only `test/corpus/idms_update.txt`; do not modify grammar in this task. This RED commit is grammar-family because `test/corpus/` belongs to that family.
  </action>
  <acceptance_criteria>
    - Every CR-04 category has a named synthetic corpus case.
    - Existing legal controls remain present.
    - The focused run fails before production edits for the intended semantic mismatches.
    - Captured output contains every exact new invalid-case name and no unrelated/harness failure; arbitrary nonzero status is rejected.
    - Fixture source lines are at most 72 bytes and use neutral invented names.
    - The RED commit contains only `test/corpus/idms_update.txt`.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(awk 'length($0) &gt; 72' test/corpus/idms_update.txt)" &amp;&amp; RED_OUT="$(mktemp)" &amp;&amp; { node_modules/.bin/tree-sitter test -i 'idms update' &gt; "$RED_OUT" 2&gt;&amp;1; RED_ST=$?; test "$RED_ST" -ne 0 &amp;&amp; grep -q 'idms update invalid bare store' "$RED_OUT" &amp;&amp; grep -q 'idms update invalid connect from' "$RED_OUT" &amp;&amp; grep -q 'idms update invalid disconnect to' "$RED_OUT" &amp;&amp; grep -q 'idms update invalid cross-verb option' "$RED_OUT" &amp;&amp; ! grep -qE 'No such file|command not found|panic|segmentation|Failed to load language' "$RED_OUT"; ST=$?; rm -f "$RED_OUT"; test "$ST" -eq 0; }</automated>
  </verify>
  <done>The full invalid-update matrix is committed RED separately from implementation and reproduces T-02-R05.</done>
</task>

<task type="auto">
  <name>Task 2: Implement verb-specific update syntax and regenerate</name>
  <reversibility rating="costly">The public update statement and operand node names are a published query/shim contract; this task preserves those names and narrows only invalid language.</reversibility>
  <files>grammar.js, src/parser.c, src/grammar.json, src/node-types.json, test/corpus/idms_update.txt</files>
  <read_first>
    - `grammar.js` lines 2696-2723 and 2824-2842
    - `test/corpus/idms_update.txt` after Task 1
    - `.planning/phases/02-idms-dml-statement-nodes/02-06-SUMMARY.md` measured ERASE option extension
    - `.planning/phases/02-idms-dml-statement-nodes/02-CONTEXT.md` D-01/D-02/D-03/D-10
  </read_first>
  <behavior>
    - STORE/MODIFY require record; ERASE requires record and owns member options; CONNECT requires record TO set; DISCONNECT requires record FROM set; GET alone permits optional record.
    - Invalid forms cannot become graph-bearing update captures in `run_idms_query_capture.sh`.
    - Stable public statement, record, and set node names remain in generated node types.
  </behavior>
  <action>
    Replace the shared optional update body with a choice of complete verb-specific sequences, each carrying a direct visible `verb` field while preserving `idms_update_statement`. Attach `idms_unparsed_tail` only where it cannot make an incompatible clause legal or create false operands. Run the pinned generator after the RED commit, never hand-edit generated artifacts, and drive all update corpus cases plus the gate's invalid subject GREEN. Commit grammar, generated artifacts, and any expected-tree adjustments together as grammar-family work.
  </action>
  <acceptance_criteria>
    - Only GET has an optional record.
    - TO/FROM/member options are confined to CONNECT/DISCONNECT/ERASE respectively.
    - All legal controls pass; all prohibited forms produce zero graph-bearing gate captures.
    - `idms_update_statement`, `idms_record_name`, and `idms_set_name` remain named generated types.
    - Two generator runs are byte-stable; focused and aggregate IDMS corpus pass.
    - Commit paths are limited to `grammar.js`, `src/`, and `test/corpus/idms_update.txt`.
  </acceptance_criteria>
  <verify>
    <automated>node_modules/tree-sitter-cli/tree-sitter generate &amp;&amp; node_modules/.bin/tree-sitter test -i 'idms update' &amp;&amp; node_modules/.bin/tree-sitter test -i 'idms' &amp;&amp; ./run_idms_query_capture.sh --invalid-only</automated>
  </verify>
  <done>T-02-R05 is GREEN under corpus and exact-query negative evidence while the shared public update contract remains stable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted COBOL to graph-bearing AST | Invalid update text must not become a valid-looking record/set statement. |
| Synthetic fixtures to public fork | Tests must be neutral and non-proprietary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-R05 | Tampering | `grammar.js` update alternatives | high | mitigate | Complete RED matrix then verb-specific GREEN alternatives; exact gate requires zero graph captures for prohibited forms. |
| T-02-02 | Information Disclosure | update corpus | high | mitigate | Hand-written neutral cases only, line-length and estate guard checks. |
| T-02-SC | Supply-chain Tampering | parser generation | low | accept | Existing lockfile-pinned CLI only; no new package. |
</threat_model>

<verification>
1. RED commit fails for reproduced invalid forms.
2. GREEN focused and aggregate IDMS corpus pass.
3. Exact gate's invalid subject emits no graph-bearing captures.
4. Generated artifacts are stable and commit-family boundaries pass.
</verification>

<success_criteria>
- Every update verb admits only legal syntax under the stable public node.
- Every reproduced invalid shape is permanently tested.
- Generated artifacts and corpus remain grammar-family-only.
</success_criteria>

<output>
Create `.planning/phases/02-idms-dml-statement-nodes/02-08-SUMMARY.md` with the RED/GREEN invalid-shape matrix, generated-artifact status, and exact commit paths.
The SUMMARY must include `## Threat Flags` entries for T-02-R01/R03/R05/R06: T-02-R05 has resolved evidence, R01/R03 carry 02-07 evidence, and R06 remains staged for 02-09; use `none` only for additional threats.
</output>
