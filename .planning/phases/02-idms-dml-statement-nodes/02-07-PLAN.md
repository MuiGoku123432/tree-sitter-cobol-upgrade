---
phase: 02-idms-dml-statement-nodes
plan: 07
type: execute
wave: 6
depends_on: ["02-06"]
files_modified:
  - run_accept_differential.sh
  - run_accept_differential_selftest.sh
  - run_idms_query_capture.sh
autonomous: true
gap_closure: true
requirements: [IDMS-03, IDMS-07]

estimate:
  tokens: 43000
  raw_tokens: 43000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "T-02-R01/CR-01 is closed: parent symlinks, existing final output symlinks, dangling final symlinks, and DIFF_TMP symlinks are resolved or refused fail-closed before an output or run artifact is created (IDMS-07)."
    - "T-02-R03/CR-02 is closed: compare accepts only strict four-column TSV rows and rejects duplicate `(relative path, start row/column)` keys in either inventory before calculating any verdict (IDMS-03)."
    - "The reproduced external-symlink and duplicate-key exploit cases start RED against the existing scripts, then pass only after the production differential is hardened."
    - "All differential fixtures are synthetic, hand-written, and neutral; no proprietary corpus source or source excerpt is read, copied, quoted, or committed (D-10, IDMS-07)."
    - "T-02-R04 is closed: malformed NODE_TYPE_REGEX and extraction-pipeline failures fail before output instead of producing a false empty census."
    - "The existing clean/trailing_error, valid custom node-regex, custom prefilter, denominator, and caller-owned scratch behavior remains green."
  artifacts:
    - path: "run_accept_differential.sh"
      provides: "Canonical path containment plus validated, duplicate-free inventory comparison"
      contains: "path_is_under_repo"
    - path: "run_accept_differential_selftest.sh"
      provides: "Synthetic adversarial regression coverage for T-02-R01 and T-02-R03"
      contains: "duplicate"
    - path: "run_idms_query_capture.sh"
      provides: "Synthetic exact-output RED gate consumed by 02-08 invalid checks and 02-09 verb/role GREEN"
      contains: "EXPECTED_VERBS"
  key_links:
    - from: "run_accept_differential_selftest.sh"
      to: "run_accept_differential.sh"
      via: "the self-test invokes cmd_snapshot and cmd_run, which call path_is_under_repo before creating output or scratch children"
      pattern: "path_is_under_repo.*SNAP_OUT_FILE"
    - from: "run_accept_differential_selftest.sh"
      to: "run_accept_differential.sh"
      via: "duplicate and malformed four-column inventories exercise cmd_compare before maps or verdict counters are populated"
      pattern: "NF.*4|duplicate"
    - from: "run_idms_query_capture.sh"
      to: "queries/idms.scm"
      via: "the gate-owned temporary 14-verb fixture is queried and normalized into an exact capture oracle"
      pattern: "tree-sitter.*query"
---

<objective>
Close the two reproduced differential-integrity blockers and establish the permanent RED query-contract gate without changing grammar-family files.

Purpose: Restore IDMS-03's differential as trustworthy evidence and IDMS-07's proprietary-output boundary by fixing T-02-R01/CR-01 and T-02-R03/CR-02. This plan is the fork-local tracer: an external symlink aimed back at the repository and a duplicate-key false-clean inventory are first made permanent RED cases, then driven through the real `snapshot`, `run`, and `compare` entry points to GREEN. It also adds the hand-written exact-output test that reproduces T-02-R06 before the grammar/query plan consumes it.

Output: Hardened fork-local differential scripts, a failing exact 14-verb query gate, and synthetic threat evidence in effective Wave 6, with no grammar, generated parser, corpus, query, shim, docs, or planning edit in the implementation commits.
</objective>

<execution_context>
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/e1001547-mbp-it/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-idms-dml-statement-nodes/02-CONTEXT.md
@.planning/phases/02-idms-dml-statement-nodes/02-02-SUMMARY.md
@.planning/phases/02-idms-dml-statement-nodes/02-04-SUMMARY.md
@.planning/phases/02-idms-dml-statement-nodes/02-REVIEW.md
@.planning/phases/02-idms-dml-statement-nodes/02-SECURITY.md
@.planning/phases/02-idms-dml-statement-nodes/02-VERIFICATION.md
</context>

## Artifacts this phase produces

- Modified `run_accept_differential.sh` symbols: `path_is_under_repo`, `cmd_snapshot`, `cmd_compare`, and `cmd_run`.
- Modified `run_accept_differential_selftest.sh` synthetic cases: snapshot-output symlink refusal, run-scratch symlink refusal, duplicate-before rejection, duplicate-after rejection in both row orderings, and malformed TSV rejection.
- New `run_idms_query_capture.sh`: a fork-local exact-output gate whose invented fixture covers all 14 verbs plus READY area and neutral navigation-scope controls; update negatives turn GREEN in 02-08, verb/role output in 02-09, then shim/CI delivery occurs in 02-10.
- No new package, external service, proprietary fixture, grammar node, query capture, or generated parser artifact.

## Multi-Source Coverage Audit

| SOURCE | ID | Gap-closure item | Plan | Status | Notes |
|--------|----|------------------|------|--------|-------|
| GOAL | -- | Walkable, queryable IDMS nodes plus trustworthy ACCEPT containment | 02-07 through 02-11 | COVERED | This plan restores the differential evidence boundary; later plans close semantics, delivery, and evidence. |
| REQ | IDMS-03 | Trustworthy ACCEPT differential | 02-07, 02-11 | COVERED | Strict TSV, duplicate rejection, symlink-safe output, full rerun. |
| REQ | IDMS-07 | Proprietary safety and separable delivery | 02-07 through 02-11 | COVERED | Physical containment and path-family commits. |
| RESEARCH | D-08/D-10 | Manual reusable differential; neutral fixtures only | 02-07, 02-11 | COVERED | Full corpora remain manual; synthetic cases are tracked. |
| CONTEXT | D-07/D-08/D-10 | Fail-proven comparison, fork-local script, no estate-derived fixtures | 02-07 | COVERED | Existing decisions retained. |
| VERIFICATION | T-02-R01/CR-01 | Symlink output/scratch bypass | 02-07 | COVERED | Task 1. |
| VERIFICATION | T-02-R03/CR-02 | Duplicate-key false-clean verdict | 02-07 | COVERED | Task 2. |
| VERIFICATION | T-02-R06/CR-03 RED | Exact all-verb query test missing | 02-07, 02-09 | COVERED | Task 3 creates RED; 02-09 turns it GREEN. |

<tasks>

<task type="tracer">
  <name>Task 1: Reproduce and close the symlink containment bypass through snapshot and run</name>
  <reversibility rating="reversible">The containment implementation is private to one fork-local shell tool; reverting it changes no published AST or query contract.</reversibility>
  <files>run_accept_differential.sh, run_accept_differential_selftest.sh</files>
  <read_first>
    - `run_accept_differential.sh` lines 113-177 (`TOP_DIR`, `report`, and `path_is_under_repo`; the current logical `pwd` plus textual-prefix check is T-02-R01's root cause)
    - `run_accept_differential.sh` lines 180-230 (`cmd_snapshot` preconditions and the current containment check before scratch creation)
    - `run_accept_differential.sh` lines 839-917 (`cmd_run`, DIFF_TMP ownership, derived run paths, and the current check before worktree creation)
    - `run_accept_differential_selftest.sh` lines 1-51 (scratch lifecycle and real `snapshot` tests)
    - `run_accept_differential_selftest.sh` lines 79-246 (production-function sourcing and real `cmd_run` tracer seam)
    - `.planning/phases/02-idms-dml-statement-nodes/02-REVIEW.md` CR-01 and `.planning/phases/02-idms-dml-statement-nodes/02-SECURITY.md` T-02-R01
    - `.planning/phases/02-idms-dml-statement-nodes/02-CONTEXT.md` D-08 and D-10 (fork-local placement, manual full runs, and proprietary-source prohibition)
  </read_first>
  <behavior>
    - RED 1, parent symlink: create an external parent symlink whose physical target is a repository directory; `snapshot` given a new filename below it must exit nonzero and leave the target absent.
    - RED 2, existing final symlink: create the requested OUT_FILE itself as a symlink whose existing target is inside the repository; `snapshot` must resolve the final component strictly, refuse it, and leave the target byte-identical.
    - RED 3, dangling final symlink: create OUT_FILE itself as a dangling symlink; `snapshot` must fail closed before replacing, truncating, or creating either link target or output.
    - RED 4, DIFF_TMP symlink: create an external directory symlink whose physical target is a repository directory; the public `run` subcommand with it as `DIFF_TMP` must exit nonzero before creating its worktree, inventories, selected-path list, or helper binaries.
    - Control: a real external directory under the self-test's owned scratch remains accepted for snapshot output and run scratch.
    - Failure case: a nonexistent or otherwise uncanonicalizable output parent/directory fails closed and creates nothing.
  </behavior>
  <action>
    First add all four exploit cases to `run_accept_differential_selftest.sh` using only the existing synthetic parser/corpus seams and an external temporary directory: parent-component symlink, existing final output symlink into the repository, dangling final output symlink, and DIFF_TMP directory symlink. Run them against the current production script and require the self-test to fail for the expected case labels, then capture that RED behavior without reading any real corpus or quoting real source.

    Replace `path_is_under_repo`'s logical-path construction with a fail-closed physical-path check. Canonicalize `TOP_DIR` physically once. For a file destination: if the final path lexically exists or is a symlink (`-e` or `-L`), resolve that final component strictly through all links and reject it when it lands under the physical repository; a dangling final symlink is an immediate failure because strict resolution cannot prove its destination. Only when the final component does not exist and is not a symlink may the implementation strictly canonicalize the existing parent, append the basename, and apply path-aware containment. For `DIFF_TMP`, strictly resolve the existing directory itself, including a symlink final component. Use `pathlib.Path.resolve(strict=True)` and `os.path.commonpath` or equivalent path-aware primitives; do not use identifier spelling or textual-prefix heuristics.

    Preserve the call order: `cmd_snapshot` must finish canonicalization and containment validation before `mktemp`, truncation, or opening `SNAP_OUT_FILE`; `cmd_run` must validate the canonical `RUN_DIFF_TMP` before deriving or creating any child. Treat canonicalization failure as refusal, not as evidence that the path is outside. After creating script-owned mktemp scratch, revalidate its physical location before use. Keep caller-owned scratch preservation and script-owned cleanup behavior from 02-02 intact.

    Run the new cases to GREEN and retain the existing self-test matrix. Keep all implementation and tests in these two fork-local files so the task's commit cannot mix with grammar-family paths (FORK-03).
  </action>
  <acceptance_criteria>
    - Parent-symlink snapshot exits nonzero and confirms the repository target file was never created.
    - Existing-final-symlink snapshot exits nonzero and confirms both the symlink and its repository target are unchanged.
    - Dangling-final-symlink snapshot exits nonzero and confirms it neither replaces the link nor creates its missing target.
    - Existing final output components that resolve outside the repository remain usable controls; strict resolution is not a blanket rejection of pre-existing external files.
    - DIFF_TMP-symlink run exits nonzero before any `accept-differential-worktree`, inventory, selected-path list, or helper appears under the repository target.
    - A regular external scratch directory remains accepted, proving the guard does not reject every destination.
    - An uncanonicalizable parent/directory exits nonzero and creates no output.
    - `bash run_accept_differential_selftest.sh` exits 0 after retaining all pre-existing cases.
    - `bash -n run_accept_differential.sh run_accept_differential_selftest.sh` exits 0.
    - Only `run_accept_differential.sh` and `run_accept_differential_selftest.sh` are changed by this task; no grammar-family path is present in the task commit.
  </acceptance_criteria>
  <verify>
    <automated>bash -n run_accept_differential.sh run_accept_differential_selftest.sh &amp;&amp; bash run_accept_differential_selftest.sh</automated>
  </verify>
  <done>T-02-R01/CR-01 has a real-entry-point RED-to-GREEN test, both snapshot and run reject symlink-resolved repository destinations before creating output, and ordinary external scratch still works.</done>
</task>

<task type="auto">
  <name>Task 2: Reject malformed and duplicate inventory keys before differential verdicts</name>
  <files>run_accept_differential.sh, run_accept_differential_selftest.sh</files>
  <read_first>
    - `run_accept_differential.sh` lines 470-563 (`cmd_compare`; maps are currently populated before schema/uniqueness validation and order arrays reread overwritten values)
    - `run_accept_differential.sh` lines 17-65 (the documented four-column inventory and compare contract that must remain stable)
    - `run_accept_differential_selftest.sh` lines 248-296 (the six existing compare cases and exact report helper style)
    - `.planning/phases/02-idms-dml-statement-nodes/02-REVIEW.md` CR-02 and `.planning/phases/02-idms-dml-statement-nodes/02-SECURITY.md` T-02-R03
    - `.planning/phases/02-idms-dml-statement-nodes/02-02-SUMMARY.md` "Inventory contract" and "Compare direction proof"
  </read_first>
  <behavior>
    - RED 1: a duplicate key in before inventory exits nonzero before a RECLASSIFIED/CONVERTED/NEW verdict.
    - RED 2: after inventory contains `idms_accept_statement` then `accept_statement` at one key; compare exits nonzero rather than reporting a false clean result.
    - RED 3: reverse those two after rows; compare still exits nonzero, proving rejection is order-independent.
    - RED 4: rows with fewer than four fields, more than four fields, an invalid position, an unsupported node type, or an unsupported qualifier each exit nonzero and identify the inventory and line.
    - RED 5: malformed NODE_TYPE_REGEX in standalone snapshot and run both fail before output; a forced extraction-pipeline error is not converted to a zero-match success.
    - Control: valid empty inventories and the six existing valid comparison cases preserve their established outcomes and counts.
  </behavior>
  <action>
    Add the duplicate-before, both duplicate-after orderings, and malformed-row cases to the synthetic self-test first. Run the new cases and require the self-test to fail on the current comparator, including the reproduced false-clean ordering, before changing production code.

    Refactor `cmd_compare` so validation happens before any comparison maps, counters, or verdict output are populated. Enforce exactly four tab-separated columns with nonempty relative path, `row,column` numeric position, a node type admitted by the differential contract, and qualifier `clean` or `trailing_error`. Validate both files even when one is empty. Reject a repeated `(path, position)` key separately within before and after inventories, name the offending input side and line/key, and exit nonzero without printing `RECLASSIFIED_COUNT: 0` or another success-looking verdict. Also validate `NODE_TYPE_REGEX` in both standalone `snapshot` and `run` before output/scratch creation using the selected regex engine; malformed regex or extraction-pipeline status greater than the no-match status fails closed, removes partial owned output, and reports T-02-R04.

    Count valid rows directly during the validated read rather than iterating duplicate-prone order arrays whose keys can point at overwritten map entries. Keep the stable public four-column TSV shape and the existing semantics for clean reclassification, trailing-error conversion, new-only rows, absent-after rows, and both-empty inventories. Do not add an end-position or ordinal column: the required fix is strict four-column validation plus duplicate-key rejection.

    Run the whole self-test to GREEN. The self-test must prove both after-row orderings fail, not only the originally reproduced ordering.
  </action>
  <acceptance_criteria>
    - Duplicate keys in either inventory cause `compare` to exit nonzero before any differential verdict; the diagnostic names before/after and the duplicate key.
    - Both conflicting after-row orderings exit nonzero, so row order cannot select a false-clean verdict.
    - Every nonempty accepted row has exactly four TSV fields, numeric `row,column`, an admitted node type, and an admitted qualifier.
    - Valid counts are derived once from validated rows; duplicate map overwrite cannot corrupt summary counts.
    - Existing cases retain their outcomes: identical and both-empty pass, clean reclassification and absent-after fail, trailing-error conversion and new-only pass.
    - `bash run_accept_differential_selftest.sh` exits 0 and emits one explicit result line for each new adversarial case.
    - Malformed `NODE_TYPE_REGEX` tests for both `snapshot` and `run` exit nonzero before output creation, with no partial inventory; extraction pipeline errors propagate nonzero.
    - `bash -n run_accept_differential.sh run_accept_differential_selftest.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n run_accept_differential.sh run_accept_differential_selftest.sh &amp;&amp; bash run_accept_differential_selftest.sh</automated>
  </verify>
  <done>T-02-R03/CR-02 is closed: compare validates strict four-column inventories, rejects duplicate keys on both sides independent of ordering, and cannot emit a false-clean result from overwritten map entries.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the exact 14-verb and semantic-role RED query gate</name>
  <files>run_idms_query_capture.sh</files>
  <read_first>
    - `package-lock.json` and `package.json` (the existing lockfile-pinned CLI contract; restore local packages with `npm ci` only when the binary is absent)
    - `queries/idms.scm` (the reproduced defect: update/session verb patterns are absent and ACCEPT's statement node is captured as `@verb`)
    - `run_sql_query_capture.sh` lines 1-27, 47-97, 150-191, and 233-291 (owned scratch, query-output normalization, and exact ordered TSV comparison)
    - `run_cics_query_capture.sh` lines 27-43 and 105-133 (counter-driven shell reporting)
    - `.planning/phases/02-idms-dml-statement-nodes/02-REVIEW.md` CR-03 and WR-01/WR-06
    - `.planning/phases/02-idms-dml-statement-nodes/02-SECURITY.md` T-02-R06/T-02-R07
    - `.planning/phases/02-idms-dml-statement-nodes/02-CONTEXT.md` D-01, D-02, D-04, and D-10
  </read_first>
  <behavior>
    - Setup is separate from the expected RED: if the repository CLI is absent, run `npm ci`; then require its version output to identify exact tree-sitter 0.24.5 before querying.
    - The script owns the sole exact-query fixture authority: it writes a neutral, hand-authored 14-verb COBOL program under an external `mktemp -d`, never under `test/idms/` or another tracked path.
    - The temporary program contains exactly one statement for each verb in this order: BIND, OBTAIN, FIND, READY, ERASE, ACCEPT, GET, STORE, FINISH, MODIFY, CONNECT, ROLLBACK, COMMIT, DISCONNECT.
    - Additional neutral navigation statements cover ANY, DUPLICATE, FIRST, LAST, PRIOR, a numeric selector, and PAGE-INFO with exact expected record/scope/non-edge captures.
    - It normalizes query output to capture name, start/end row-column, and exact text, then compares one complete ordered expected TSV rather than checking role presence.
    - Interface: `IDMS_QUERY_FILE` optionally selects the query (default `queries/idms.scm`); `IDMS_CAPTURE_OUT` optionally requests a caller-owned copy of the validated normalized TSV; unset variables use gate-owned scratch only.
    - It requires exactly one `verb` row per statement with the exact token value; ACCEPT must equal only `ACCEPT`.
    - It pins syntactic record/set controls and separately rejects READY's area plus navigation WITHIN's ambiguous scope operand from `record`/`set` roles.
    - A second neutral invalid-update subject covers the prohibited bare, reversed-preposition, cross-verb set-clause, and non-ERASE option shapes; none may emit a graph-bearing `verb`, `record`, or `set` row once GREEN.
    - Against current source the gate exits nonzero only after the query command completed and the normalizer completed, and only because diagnostics report the exact pre-fix verb-contract mismatch: missing update/session verb rows and ACCEPT text wider than `ACCEPT`.
    - Missing CLI after install, wrong CLI version, scratch/fixture creation failure, query nonzero status, output-normalizer failure, or script/setup failure exits through a distinct harness-error path and is never accepted as the expected RED.
  </behavior>
  <action>
    Create `run_idms_query_capture.sh` at repository root as mode 100755 fork-local test tooling. Before fixture setup, locate `node_modules/tree-sitter-cli/tree-sitter`; if absent, run `npm ci` and then require the binary. This is a lockfile-pinned local installation preflight, not a new dependency or package change. Capture `--version` status/output and require exact 0.24.5. A dependency, version, scratch, fixture, query, or normalizer error must emit `HARNESS_ERROR` and exit a status distinct from the expected contract mismatch. Never treat an arbitrary nonzero as RED success.

    Follow the existing SQL gate's deterministic normalization: install one EXIT/INT/TERM cleanup trap immediately after creating gate-owned external `mktemp -d`, create the sole authoritative synthetic fixture there, invoke the pinned repository CLI, capture query status/output, derive exact source text from reported row-column ranges, emit an ordered TSV in owned scratch, and byte-compare it with one complete embedded expected TSV. Use only invented neutral identifiers and published generic forms; do not read any corpus and do not use or mention `test/idms/query-sample.cbl` as an input to this gate.

    Define the stable interface exactly. `IDMS_QUERY_FILE` defaults to `$TOP_DIR/queries/idms.scm`, must resolve to a readable regular file, and is never deleted. `IDMS_CAPTURE_OUT` is optional; when unset no capture artifact survives cleanup. When set, require its existing parent to canonicalize outside the repository, reject symlink/dangling destinations with the same fail-closed posture as the differential, write via a temporary file plus atomic rename, and treat the resulting validated TSV as caller-owned -- never remove or overwrite it during trap cleanup except for the atomic successful replacement. Gate-owned fixture/raw-query/normalized scratch is always removed by the trap. Exit 0 means normal contract GREEN or the requested exact staged RED matched; exit 1 means contract mismatch; exit 2 means `HARNESS_ERROR` (setup/version/path/fixture/query/normalizer/output failure).

    The expected stream must include all 14 exact verb tokens, every intended record/set capture used as a control, and no record/set row for READY or navigation WITHIN's ambiguous scope control. Add a separate invented invalid-update subject containing the CR-04 matrix and require zero `verb`, `record`, or `set` capture rows from those invalid statements; the same fork-local gate will therefore turn GREEN only when both T-02-R05 and T-02-R06 are safe end to end. Validate count, names, values, ranges, multiplicity, and source order. Treat malformed/duplicate normalized rows as failure. Clean scratch on success, failure, and signals, and follow the repository's explicit-status shell style.

    Add `--invalid-only` for the T-02-R05 negative subject and explicit staged modes `--expect-red=verbs` and `--expect-red=roles`. `--expect-red=verbs` runs at the current pre-fix state and succeeds only when setup/query/normalization are healthy and exact current verb defects are present: every enumerated missing update/session verb row plus oversized ACCEPT. It explicitly tolerates only a separately enumerated role-defect set (READY-as-record, ambiguous WITHIN-as-set, and documented selector/PAGE-INFO miscaptures) so those cannot make the verb stage fail for the wrong reason. After verb repair, `--expect-red=roles` requires all 14 verb rows to be exact while asserting precisely that separately enumerated role-defect set and no extra mismatch. Normal mode requires invalid-update, verb, selector, PAGE-INFO, area/set/scope, record/set, range, count, and order expectations all GREEN. Unexpected/missing diagnostics fail in every staged mode. Commit this test separately; 02-08 drives `--invalid-only` GREEN, 02-09 Task 1 moves `verbs` RED to GREEN then Task 2 moves `roles` RED to normal GREEN, and 02-10 owns shim/CI delivery.
  </action>
  <acceptance_criteria>
    - `bash -n run_idms_query_capture.sh` exits 0.
    - The CLI preflight runs `npm ci` only when the binary is absent, captures version status/output, and rejects anything other than exact 0.24.5.
    - `bash run_idms_query_capture.sh --expect-red=verbs` exits 0 only after query and normalization statuses are 0, exact missing-verb/oversized-ACCEPT diagnostics match, and only the enumerated role mismatches are tolerated; normal mode exits 1 on the current contract.
    - `--expect-red=roles` is defined to require verbs GREEN and the exact enumerated role defects only; invoking it before verb repair exits 1, not 0.
    - `bash run_idms_query_capture.sh --invalid-only` specifically exercises the complete CR-04 negative subject and currently fails until 02-08.
    - Deliberately hiding the CLI or forcing fixture/query/normalizer failure makes both staged RED modes exit 2 with `HARNESS_ERROR`, proving setup failures cannot masquerade as RED.
    - The expected TSV contains exactly 14 `verb` rows with all required exact values in source order.
    - `IDMS_QUERY_FILE` and `IDMS_CAPTURE_OUT` follow the declared defaults, validation, ownership, and cleanup contract; no undefined query/output override exists.
    - Exit codes are exactly 0 for a satisfied requested mode, 1 for a contract mismatch, and 2 for `HARNESS_ERROR`.
    - Independent assertions reject both area controls from `record` and `set` roles.
    - The invalid-update subject encodes every CR-04 category and expects zero graph-bearing captures.
    - The fixture and oracle are wholly synthetic and neutral, and the run leaves no repository scratch artifact.
    - The task commit contains only `run_idms_query_capture.sh`, a fork-local path.
    - `chmod +x run_idms_query_capture.sh`, `test -x run_idms_query_capture.sh`, and Git mode 100755 prove the gate is executable.
    - `shasum -a 256 run_idms_query_capture.sh` is recorded in `02-07-SUMMARY.md`; later staged RED/GREEN evidence cites the same hash.
  </acceptance_criteria>
  <verify>
    <automated>bash -n run_idms_query_capture.sh &amp;&amp; test -x run_idms_query_capture.sh &amp;&amp; ./run_idms_query_capture.sh --expect-red=verbs</automated>
  </verify>
  <done>T-02-R06 has a committed exact-output RED test covering all 14 verbs and area-role negatives; it fails against the reproduced current defect and is ready for plan 02-09 to turn GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller-selected output/scratch path to public working tree | A logical path outside the worktree can physically resolve through a symlink into the public repository and expose proprietary path inventories. |
| Inventory TSV to differential verdict | Synthetic or parser-produced rows are untrusted evidence; malformed or duplicate identities can corrupt counts and hide reclassification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-R01 | Information Disclosure | `path_is_under_repo`, `cmd_snapshot`, `cmd_run` | high | mitigate | Task 1 strictly resolves existing final components, canonicalizes parents for new files, fails closed on dangling finals, resolves DIFF_TMP itself, and adds all four refusal cases. ASVS L1 blocks completion until every case leaves targets unchanged/absent. |
| T-02-R03 | Tampering / Repudiation | `cmd_compare` inventory ingestion | high | mitigate | Task 2 validates exactly four TSV fields and rejects duplicate `(path, position)` keys before maps, counters, or verdicts; duplicate-before and both duplicate-after orderings are blocking evidence. |
| T-02-R04 | Repudiation | snapshot/run node regex | medium | mitigate | Task 2 validates malformed NODE_TYPE_REGEX fail-closed before output, propagates extraction failures, and adds adversarial snapshot/run cases while already editing the differential. |
| T-02-R06 | Tampering | `queries/idms.scm` exact capture contract | high | mitigate | Task 3 commits the permanent all-14 exact-output RED gate; plan 02-09 is the dependent GREEN mitigation. This plan must not claim the threat closed while the expected failure remains. |
| T-02-SC | Supply-chain Tampering | package installation | low | accept | No package is added or installed by this plan; tests use existing shell, awk, Python 3, and repository tooling. |
</threat_model>

<verification>
1. Run `bash run_accept_differential_selftest.sh`; all legacy and new adversarial cases pass.
2. Confirm the self-test covers parent, existing-final, dangling-final, and DIFF_TMP symlinks, duplicate-before, both duplicate-after orderings, and malformed four-column rows.
3. Run `bash -n` over both scripts.
4. Run `run_idms_query_capture.sh` and capture its expected RED result for T-02-R06.
5. Inspect the task commits with `.githooks/commit-separability.sh`; each contains only fork-local scripts.
6. Record T-02-R01, T-02-R03, and T-02-R06 RED evidence in `02-07-SUMMARY.md`, using synthetic paths and aggregate outcomes only.
</verification>

<success_criteria>
- Physical path containment is fail-closed before output creation for both differential entry points.
- Inventory validation and duplicate-key rejection precede every differential verdict.
- The reproduced high-severity exploits are permanent synthetic tests and are GREEN.
- The exact 14-verb query contract has a permanent RED gate ready for its dependent grammar/query fix.
- No proprietary source or grammar-family file is touched.
</success_criteria>

<output>
Create `.planning/phases/02-idms-dml-statement-nodes/02-07-SUMMARY.md` when done. Include a T-02-R01/T-02-R03 evidence table with each synthetic case, RED result, GREEN result, and command, plus T-02-R06's expected exact-query RED result. Do not include proprietary corpus paths or source.
The SUMMARY must include `## Threat Flags` entries for T-02-R01, T-02-R03, and T-02-R04 with resolved evidence, T-02-R06 as staged RED handed to 02-09, and T-02-R05 as staged invalid-subject RED handed to 02-08; use explicit `none` only if no additional threat surfaced.
</output>
